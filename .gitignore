# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
vh-solara

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# OS specific files
.DS_Store
.AppleDouble
.LSOverride

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.log

refs

# Repo-scoped agent/test artifacts (Playwright traces, reports, screenshots).
tmp/

# Node / web build deps (the built SPA in pkg/web/dist IS committed for embed).
node_modules/
web/test-results/
web/playwright-report/

# Agent-local tooling / config.
.claude/

# vh-solara per-project local data (message attachments, etc.).
.vh-solara/