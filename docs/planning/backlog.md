# Backlog

Canonical task-status source of truth for the vh-solara repo. One row per
task; status drives where it lives. Active work stays in `Now` / `Next` /
`Later`; completed/cancelled work moves to `Done` / `Cancelled` and is archived
under `docs/planning/archive/` by
`.opencode/scripts/normalize-backlog.js`.

Conventions live in [`AGENTS.md`](../../AGENTS.md) → "Backlog tracking rules".
Run `/backlog-cleanup` (or
`vh-agent-harness exec node .opencode/scripts/normalize-backlog.js`) after any
status change that completes, cancels, or reorders work, so the active sections
stay active-only and history is archived.

Row format: `| ID | Status | Area | Task | Owner | Notes | Links |`.
Statuses: `todo`, `in_progress`, `blocked`, `done`, `cancelled`. Put a
machine-readable completion date (`YYYY-MM-DD`) in `Notes` for `done` /
`cancelled` rows so the normalizer can file them into the right archive quarter.

## Area Legend

| Code | Boundary | Notes |
| --- | --- | --- |
| `WEB` | `web/` | SolidJS SPA (Vite + TS), embedded via `//go:embed`. |
| `API` | `pkg/web/`, `pkg/server/` | The `/vh/*` HTTP API + daemon handlers. |
| `TUNNEL` | `pkg/tunnel/` | Controller ↔ worker multiplexed WebSocket tunnel (yamux). |
| `AGG` | `pkg/aggregator/`, `pkg/state/`, `pkg/opencode/` | OpenCode state aggregation + session store. |
| `AUTH` | `pkg/auth/` | Controller auth / worker enrollment. |
| `PWA` | `web/public/`, manifest, service worker | Installable PWA surface. |
| `REL` | `.github/`, `install.sh`, `Dockerfile*`, `Makefile` | Tag-driven release + packaging. |
| `INFRA` | `Dockerfile*` (dev/test), toolchain + browser image | Containerized test/dev environment across all lanes; environment consistency + isolation by default. |
| `DOCS` | `docs/` | Durable repo guidance. |
| `HARNESS` | `.opencode/`, `.vh-agent-harness/` | Agent harness integration. |

## Archive Index

Older `done` and `cancelled` history lives under [docs/planning/archive/index.md](archive/index.md) and is meant for on-demand reading instead of auto-loading into the active backlog context.

- No archive files yet.

## Now

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |

## Next

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P0-API-001 | todo | API | Tighten CSP `script-src` to `'self'` (drop `unsafe-inline` / `eval`) once stable. |  | From TODO at pkg/web/server.go:601. |  |
| P0-API-002 | todo | API | Map opencode session alias / share slug into `shapeSessions` once the pinned opencode version exposes it in session JSON. |  | From TODO at pkg/web/sessions.go:283. |  |
| P1-WEB-002 | todo | WEB | Extract isCursorAhead (the monotonic read-cursor invariant) into a pure orderAhead(cand, stored, order) helper in web/src/lib/scroll.ts and add unit-test cases (stored missing → true, cand===stored → false, cand newer, cand older, both absent). Currently a private closure in ChatView.tsx:505-510, untested. |  | D-F2 / A-F2 from commit-review of acdef8e. Model the tests on the existing bottommostRead tests in web/tests/unit/scroll.test.ts. |  |
| P1-WEB-003 | todo | WEB | Add Playwright e2e covering the browser-smoke gap for the scroll read-position feature: (a) stream into a following viewport, scroll up mid-stream, assert the .jump "↓ Latest" button appears; (b) reload with a stored read anchor and assert the viewport lands on the anchored [data-mid] row. |  | C-F1 / D-F3 from commit-review of acdef8e. Web e2e suite runs via `cd web && npx playwright test`; webServer is scripts/fixture-web.sh. |  |
| P1-WEB-004 | todo | WEB | Perf-safe synchronous read-cursor snapshot on session switch — close the <400ms debounce-window loss for the from-bottom scroll-up case (the gap P1-WEB-001's old comment hid). Must avoid a per-frame layout sweep (Firefox/WebRender heat). |  | Refs ChatView.tsx:547-577 (session-switch effect + scheduleReadCursor debounce), web/src/lib/scroll.ts. Likely approach: throttle bottommostReadFromDom() to ~5/sec and capture (sid,candidate) at debounce-arm time so the switch flush has the correct session's geometry without re-measuring. |  |
| P1-API-001 | todo | API | Re-add code-fence info-string attribute support (hl_lines / linenos / linenostart / nohl / hl_style) to the custom render NodeRenderer if it ever becomes user-facing; intentionally dropped today. |  | F2 from commit-review of 28d32c68. pkg/render/renderer.go renderFencedCodeBlock treats the info string as a language name only; the compact chroma-routing renderer replaced stock goldmark-highlighting/v2. The dropped attributes are documentation-style and not authored in OpenCode chat content. |  |
| P1-DOCS-001 | todo | DOCS | Fix the AGENTS.md restore-placeholder verb so it matches shell-guard (`commit-gate.sh revert`, not `git checkout`). The "Web frontend" section of the generated `AGENTS.md` (sourced from `AGENTS.mission.md`) instructs agents to run `git checkout -- pkg/web/dist/index.html` to restore the placeholder after a local build, but `shell-guard` blocks `git checkout` for non-committer agents and `.opencode/docs/git-execution-routing.md` sanctions `.opencode/scripts/commit-gate.sh revert <paths>` as the in-session restore-to-HEAD path. Correct the mission overlay text so the instruction matches the enforced guard. |  | Surfaced while fixing/committing 84251be (P1-WEB-009). `AGENTS.md` is generated, so edit `AGENTS.mission.md` — the line "When a local build dirties `pkg/web/dist/index.html`, `git checkout -- pkg/web/dist/index.html` before committing." — not the rendered file. Verify: grep `AGENTS.md` for the corrected verb and confirm no `git checkout -- pkg/web/dist` instruction remains. |  |
| P1-WEB-013 | todo | WEB | Stuck reasoning timer (`Part.tsx:549` `live()` + `selectors.ts:180`) — cosmetic, self-heals on turn completion. Fix: shared read-side helper `reasoningEnded(part, message) = !part.time?.end && hasSuccessorPart(message, part.id)` mirroring Go's alerts-engine inference (`pkg/alerts/engine.go:255-258`); needs fixture extension (today's fixtures at `pkg/fixtures/opencode.go:75,146` always supply `time.end`). Rejected: upstream per-message-flag (protocol change) and client-side time.end reconciliation (fights part snapshots). |  | Concept A from the 419ea39 / P1-WEB-011 live-autofollow slice — explicitly out-of-scope there, pre-scoped for deferred pickup. Deferred: do NOT implement until a fixture extension that can emit an in-flight reasoning part WITHOUT `time.end` (so the stuck-timer state is reproducible end-to-end). |  |

## Later

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P1-INFRA-001 | todo | INFRA | Dockerize the entire test/dev environment (all lanes, not just `make e2e`): SPA build, Go toolchain, Playwright browsers, fixture server, and the e2e `webServer` lane. |  | Operator-requested future intent (captured only; not started). Reason: environment consistency + isolation by default — today only the e2e lane is containerized via `scripts/fixture-web.sh`, while the Go toolchain, node, and browser binaries are host-dependent, causing cross-machine divergence. |  |
| P1-WEB-010 | todo | WEB | Pin `bottommostReadFromDom`'s scroll-origin fallback in vitest. The mid-history-anchor regression (P1-WEB-009 / 84251be) is currently covered by exactly one serial e2e path (`unread-dot.spec.ts:173`). No isolated unit test pins `bottommostReadFromDom`'s `rows[0]?.id` fallback at the scroll origin (first row sits at relative top=+16 due to `.chat-scroll` `padding:16px`). A future refactor of `bottommostRead`'s threshold (`web/src/lib/scroll.ts`) could regress silently until the serial e2e suite reruns. Add a vitest unit test asserting `bottommostReadFromDom` returns `rows[0].id` when the first row sits at top=+padding (scrollTop=0) and `nearBottom` is false. | web | D-F1 from commit-review of 84251be (advisory, dropped on P1-WEB-009). Deferred. Trigger to un-defer: `web/src/lib/scroll.ts` `bottommostRead` threshold touched. Locate the existing scroll unit-test home (likely `web/tests/unit/scroll.test.ts`, modeled on the `bottommostRead` tests referenced in P1-WEB-002) and add the case there. |  |
| P1-HARNESS-001 | todo | HARNESS | Harden the commit-gate message-builder so per-line echo can't silently drop body lines. While committing 84251be, the committer agent's per-line `echo >>` scratch construction for the commit-message file silently dropped two body lines on the first pass (file came out 9 lines instead of 11) and one echo hit a UUID-typo'd filename. Both were caught by a read-back verification BEFORE acquire, so the committed artifact is clean, but the per-line-echo path is fragile. Switch the message-builder to a single heredoc / Write-tool path instead of per-line echo appends, eliminating the silent-line-drop class. |  | Flagged by the committer agent on commit 84251be. Likely fix site: `.opencode/scripts/commit-gate.sh` and/or the committer agent prompt under `.vh-agent-harness/overlays/` (the harness source, since `.opencode/` is generated). Verify: construct a multi-line commit message via the builder and assert the line count matches the input. |  |

## Done

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P1-WEB-014 | done | WEB | Connection-health + server-slow regression slice (combined). (1) FE connection-health indicators consuming already-emitted server freshness primitives: a `stale` status-dot state + `isUpdating` (anti-spam) surfaced in the Sidebar, fed by `isStale()`/`isUpdating()` in sync/stream.ts (a fast reconnect-free health tick + `healthNow` so a silent-but-open socket surfaces staleness before the 10s watchdog reconnects) + conn-vs-server latency surfacing; loading-states so the chat/selection UX does not look stale/empty mid-hydrate. (2) seedColdLastAgents server regression fix: move the per-reconnect cold-session message-tail fetch storm OFF the hydrate critical path — `startColdSeed` runs it on a background goroutine (at most one in flight; non-blocking w.r.t. hydrate) and each cold session is seeded at most once per aggregator lifetime (store-memoized), so the storm no longer scales with session-count × reconnect-frequency. Implements the memo's "Combine (1)+(2)" recommendation + the stale-indicator memo's S3+S4+S1 / U3 / L1 recommendation. | build | 2026-06-30. build session. Source packets: `researches/decisions/server-slow-regression-seedColdLastAgents.md`, `researches/decisions/stale-and-latency-indicators.md`. Changed: `pkg/aggregator/aggregator.go` + `aggregator_test.go` (`startColdSeed` background + memoize, `waitColdSeed` test helper), `pkg/state/store.go` + `store_test.go` (cold-seed memo: `seeded` map + `ColdSeedNeeded`/`MarkColdSeeded` + `deleteSessionLocked` invalidation + `SetLastAgents` comment fix), `web/src/sync/stream.ts` + `web/src/sync.ts` (`isStale`/`isUpdating`/`STALE_MS`/`UPDATING_DEBOUNCE_MS`, `healthNow` tick, watchdog), `web/src/components/Sidebar.tsx` (stale/syncing status-dot facets), `ServersPanel.tsx`/`ConnectionToast.tsx`/`SessionTree.tsx`/`ChatView.tsx`/`GitView.tsx`/`SettingsDialog.tsx`/`index.tsx`/`styles.css`/`sync/store.ts`/`sync/actions.ts`/`types.ts` (loading-states + health wiring), `web/tests/unit/health.test.ts` (new, 11 tests). Verified: `gofmt -l pkg cmd main.go` clean; `go build ./...` ok; `go test ./...` all ok; `go test -race ./pkg/aggregator/... ./pkg/state/...` ok; `npm run typecheck` clean; `npx vitest run` 127/127. `pkg/web/dist/index.html` committed placeholder left intact (no local `vite build`). Out of scope / HELD: fork #3 (first-open upstream `Messages` fetch inside the `ensureMessages`/`handleStream` path) remains a separate deeper architecture decision, not resolved by this slice. | [server-slow-regression memo](../../researches/decisions/server-slow-regression-seedColdLastAgents.md); [stale-and-latency memo](../../researches/decisions/stale-and-latency-indicators.md) |
| P1-WEB-009 | done | WEB | Fix the mid-history-anchor read-cursor regression exposed by un-skipping the GUARD e2e in P1-WEB-007. The e2e (`web/tests/e2e/unread-dot.spec.ts` test 3, "opening a session at a mid-history anchor keeps the dot") failed deterministically at line 220: scrolling an overflowing transcript in `other` to TOP (scrollTop=0) and switching away did NOT persist a read anchor, so reopening `other` bottom-pinned + acked (cleared the unread dot) instead of restoring to the anchor. Root cause: `bottommostReadFromDom` (ChatView.tsx ~:505-517) computes each row's relative top via border-box `getBoundingClientRect`; `.chat-scroll` has `padding:16px` (web/src/styles.css:1038), so at scrollTop=0 the first message row sits at relative-top +16 (>0), and `bottommostRead` (web/src/lib/scroll.ts) returns `undefined` when no row has `top<=0` — i.e. at the scroll origin no anchor was ever written. The test scrolled to the absolute top, so `flushReadCursor`'s candidate was `undefined` → `maybeRestore` on reopen took the no-anchor bottom-pin + `ackSession` branch (~:559-570). Fix: after `bottommostRead` returns undefined, fall back to `rows[0]?.id` (the topmost visible message); only reachable when not at bottom because `flushReadCursor`'s `nearBottom` guard returns first. |  | 2026-06-30. Changed: `web/src/components/ChatView.tsx` — `bottommostReadFromDom` fallback `return rows[0]?.id;` after the `bottommostRead` miss (~:505-517) + a detailed comment citing the 16px padding / scroll-origin geometry. No other lanes touched (no changes to `maybeRestore` branches, the self-pin sentinel, `attendingNow`, `onScrolled`, or the Go backend). Verified: `gofmt -l pkg cmd main.go` clean; `go vet ./...` ok; `go test ./...` ok; `npm run typecheck` clean; `npm run test:unit` 101 passed; `npm run build` ok; full `npm run test:e2e` 106 passed (no collateral breakage — was 105 pass + 1 fail); `unread-dot.spec.ts` + `scroll-follow.spec.ts` reran 3x = 11 passed each time (deterministic). `pkg/web/dist/index.html` restored to placeholder. |  |
| P1-WEB-011 | done | WEB | Live auto-follow ("Live" pill, concept B `chat-live`) self-heal on turn/resume + finished-gate + composer-grow e2e. `following()` (web/src/components/ChatView.tsx) had NO engage site on turn-start/resume — only open/switch/Latest/scroll-back/`maybeRestore` engaged it, so any Live loss was permanent until manual scroll-back; the pill gate also lacked a finished/idle gate. Fix (JS-only, NO CSS): (1) intent latch `userScrolledUp` armed ONLY on genuine user scroll-up — at the contentEl ResizeObserver guard (`scrollTop<pinnedTop && !shrank`) and `onScrolled` (`!atBottom && !shrank`); system pins (own-pin bail), clamps, and content-shrinks do NOT arm it; (2) self-heal `createEffect` on the `working()` false→true busy edge → `setFollowing(true)+pin()` unless the latch is set (intent-latch decision: re-engage on new turn/resume but do NOT yank a deliberate reader; latch deliberately NOT cleared on the busy edge so the not-yanked case holds); (3) pill Show gate gains `&& working()` so it hides on finished/idle turns. Tests added to web/tests/e2e/scroll-follow.spec.ts: composer-auto-grow keeps following (9), deliberate-scroll-up reader not yanked on new turn (10b), pill hides when finished (11); 10a (spurious-loss re-engage) is `test.skip` — under the fix content-shrinks no longer drop `following`, so it needs a fixture hook. Existing tests 1/5/6/7/8/openDemo switched from `.chat-live`-visible to geometry-first `expectFollowingTail` (idle demo fixture has `working()`=false, so the pill is hidden by the new gate). |  | 2026-06-30. build session (live-autofollow). Source packet: researches/sources/live-autofollow-lost-on-resize.md. NOTE: the task brief said `P1-WEB-010`, but that ID is already taken (scroll-origin vitest fallback, backlog.md Later) — used `P1-WEB-011` instead. Out-of-scope: Concept A `reasoning-time.live` in Part.tsx was NOT touched. Changed: web/src/components/ChatView.tsx, web/tests/e2e/scroll-follow.spec.ts, docs/planning/backlog.md, researches/sources/live-autofollow-lost-on-resize.md. Verified: vitest 103 passed / 13 files; typecheck clean; playwright scroll-follow 11 passed / 1 intentional skip (test 10a — spurious-loss positive branch, pending a content-shrink fixture hook). |  |
| P1-WEB-012 | done | WEB | Fix the busy-at-reopen-anchor "yank" regression introduced by 419ea39 (the P1-WEB-011 Live auto-follow self-heal slice), advisory finding A-F3 (leaves b+d). `maybeRestore()`'s anchor-found branch (web/src/components/ChatView.tsx) restored the viewport to a stored mid-history read anchor and called `setFollowing(false)` WITHOUT arming the `userScrolledUp` intent latch — the ONLY `setFollowing(false)` site that didn't manage the latch (the stale-anchor and no-anchor restore siblings arm `false` because they land AT the bottom; the onScrolled scroll-away arm and the RO-shrink re-pin guard arm `true` because they land AWAY from the bottom). So on reopen of a BUSY session, the self-heal effect added by 419ea39 (the `working()` false→true busy edge gated on `!userScrolledUp()`) fired on the first `ready()` flip and yanked the reader off the restored anchor to the tail (`setFollowing(true)+pin()`); the ack effect then cleared the unread dot. Reachable because a mid-turn session stays `busy` across away/back (`markSessionIdle` flips idle only on abort, not on navigate-away). Fix = ONE line: arm the latch `setUserScrolledUp(true)` immediately after the `setFollowing(false)` in the anchor-found branch (mirrors the onScrolled scroll-away arm — restoring to a mid-history anchor is genuine read intent, lands away from the bottom). This suppresses the yank (self-heal gates `!userScrolledUp()`), preserves read position (`pin()` never called, `following` stays false), and keeps the unread dot correctly lit (the ack effect early-returns at `!following()`, and the user IS behind at a mid-history anchor — MORE correct than today). Does NOT strand the reader: three un-strand paths remain (the "↓ Latest" button shows whenever `!following()`; `jumpToLatest` clears the latch + re-glues; `onScrolled` clears the latch when scrolling back to the bottom). |  | 2026-06-30. A-F3 (leaves b+d) from commit-review of 419ea39. Changed: `web/src/components/ChatView.tsx` — one-line fix in `maybeRestore`'s anchor-found branch: added `setUserScrolledUp(true);` after the existing `setFollowing(false);`, with a comment mirroring the onScrolled scroll-away arm explaining why the latch is armed here (mid-history anchor = genuine read intent / away from bottom) and citing the busy-edge self-heal effect it suppresses. No other lanes touched. Verified: `npm run typecheck` clean; `npx vitest run` 109 passed (13 files, 0 failed); `npx playwright test e2e/scroll-follow.spec.ts` 12 passed / 1 skipped (the new test 13 "reopen of a busy session at a stored anchor is not yanked to the tail" passes at 658ms; pre-existing 11 pass / 1 skip preserved). The new e2e seeds a mid-history read anchor (m4) via `page.addInitScript` → localStorage `vh.scroll.v2`, drives a real busy turn with `[[stall]]` (working() is activity-driven, NOT message-heuristic, so m6's missing time.completed alone does NOT make the demo busy), reloads (prevWorking is per-instance, not reset on switch, so the busy edge needs a fresh mount), and asserts geometry-first that the viewport STAYS at the anchor (not yanked to tail) + button.jump visible (!following) + .chat-live hidden, then that jumpToLatest re-engages Live. |  |
| P1-WEB-008 | done | WEB | Fix the subsession reactive-ack gap (F2 from the commit-review of c66e089): the reactive unread-ack `createEffect` in ChatView.tsx gated on `state.unread[props.sessionId]` (the RAW session id), but `state.unread` is keyed by the ROOT session id (set server-side via `markUnreadLocked(root)` in pkg/state/store.go). So when viewing a SUBSESSION glued to its bottom while the root's subtree finished, `state.unread[<sub>]` was never set → the gate never opened → the reactive ack never fired → the root's unread dot lingered until a manual scroll. Inconsistent with `onScrolled` (which calls `ackSession(props.sessionId)` and resolves to root via `rootOf` inside `ackSession`, so a manual scroll from a subsession view DID clear the root's dot). Fix: gate on `state.unread[rootOf(props.sessionId)]` so the reactive path matches the onScrolled path and clears the root's dot from any session in its subtree viewed at the bottom. |  | 2026-06-29. F2 from commit-review of c66e089. Changed: `web/src/components/ChatView.tsx` — (a) added `rootOf` to the existing `../sync` import; (b) one-line gate change at the reactive-ack createEffect (~:638): `if (!state.unread[props.sessionId]) return;` → `if (!state.unread[rootOf(props.sessionId)]) return;`; (c) comment polish in the same block (notes the root-keyed gate now fires for subsession views too, and corrects the loop-safety reasoning — `ackSession` clears the very signal the effect tracks via `setState("unread", root, undefined)` + early-returns on `!state.unread[root]`, NOT the `attendingNow()` gate, which only governs notification `markRead`). The `ackSession(props.sessionId)` call is UNCHANGED — it correctly takes the raw id and resolves to root internally via `rootOf`. Does NOT touch the self-pin sentinel, `attendingNow()`, `nearBottom()` sampling, `onScrolled`, or any other lane. Verified: `npm run typecheck` clean; `npm run test:unit` 89 passed / 3 failed — the 3 failures are a concurrent editor's WIP in the untracked `web/tests/unit/SessionTree.test.tsx` (twisty label `"Subtree: temp (click to cycle)"`), NOT this change (no new regression; ChatView.tsx is the only file this task touched); `npx playwright test unread-dot.spec.ts` 3/3 (PRIMARY + REACTIVE + GUARD) pass — the exact paths exercising this fix. Subsession reactive-ack e2e SKIPPED (no flaky test): the only root-with-subsession in the fixture is `demo`→`sub` (pkg/fixtures/opencode.go:54,58), and `demo`'s busyCount drifts across the serial suite — the documented reason every unread-dot test uses `other` instead — so arming `demo`'s unread while viewing `sub` at its bottom is non-deterministic. Correctness backed by typecheck + the 3 root-path unread-dot tests + the one-line logical alignment with `onScrolled` (for root sessions `rootOf(id)===id`, so the gate is unchanged there). `pkg/web/dist/index.html` restored to placeholder (no diff). |  |
| P1-WEB-007 | done | WEB | Fix the latent scroll-restore-to-anchor bug: on a fresh `/?session=…` page load, stored read anchors were silently ignored — every fresh load landed at the bottom regardless of where the user last read to. Root cause: `maybeRestore()`'s "defer until the message snapshot arrives" guard (ChatView.tsx) keyed off object truthiness (`if (!sm())`), but `openSession` (sync/actions.ts) pre-initializes the message slot to a truthy-but-empty `{order:[],byId:{}}` the instant a session is selected, so the guard did NOT defer; `order.includes(anchor)` was then false on the empty order → the valid anchor was misclassified as "stale" → fell to the bottom-pin branch, and `restoredFor` was assigned BEFORE the staleness check so the later ResizeObserver retry returned early and never honored the anchor. Fix: key the defer off order length (`if (!sm()?.order?.length)`) so it waits while the snapshot is empty and proceeds once messages have landed. |  | 2026-06-29. Changed: `web/src/components/ChatView.tsx` — one-line guard change at maybeRestore's anchor branch (~:535, inside `if (anchor)`): `if (!sm()) return false` → `if (!sm()?.order?.length) return false`, plus an expanded comment documenting the empty-pre-init caveat. Does NOT touch the no-anchor ack branch (~:553-561, P1-WEB-005), the stale-anchor fallback, the self-pin sentinel in onScrolled, or attendingNow(). Also revived the previously-skipped GUARD e2e in `web/tests/e2e/unread-dot.spec.ts` (test 3, "opening a session at a mid-history anchor keeps the dot") with a real-user-flow body: builds an overflowing transcript in `other`, scrolls up to write a mid-history read anchor via the debounced flushReadCursor, switches to demo, prompts `other` (arms the dot while unselected), reopens `other`, and asserts BOTH (a) restored to the anchor (NOT at the bottom — `scrollTop < scrollHeight - clientHeight - 24`) and (b) the unread dot REMAINS visible (anchor branch never acks). Verified: `npm run typecheck` clean; `npm run test:unit` 89/89 (87 baseline + 2 from the pre-existing untracked `web/tests/unit/SessionTree.test.tsx` WIP — not this task); full `npm run test:e2e` serial suite 100/100 — PRIMARY + REACTIVE still pass, GUARD now active-and-passing (4.4s). `pkg/web/dist/index.html` restored to the committed placeholder (no diff). Related edge case P1-WEB-004 (perf-safe scroll snapshot on switch) deliberately untouched. |  |
| P1-WEB-006 | done | WEB | Playwright e2e regression for the finished-unread-dot clearing behavior (bug P1-WEB-005). Covers both fix paths: (1) opening a finished unread session already at the bottom clears the dot without scrolling (the `maybeRestore` no-anchor bottom-pin ack); (2) a session finishing while watched at the bottom clears the dot reactively (the new reactive ack `createEffect`). |  | 2026-06-29. Changed: `web/tests/e2e/unread-dot.spec.ts` (new — 2 active tests + 1 documented `test.skip`). Strategy: preferred (no fixture change) — the fixture's `other` session naturally produces an unread dot via a real busy→idle transition (prompt while unselected → root marks unread server-side). The mid-history-anchor guard case is recorded as `test.skip` with its root cause: `maybeRestore`'s `if(!sm())` defer keys off object truthiness, but `openSession` pre-initializes `state.messages[id]` to a truthy-but-empty object, so a valid stored read anchor is misclassified as stale and the bottom-pin (acking) branch runs instead — a latent scroll/anchor-restore bug SEPARATE from P1-WEB-005 (reported as follow-up, not fixed: out of scope). Verified: `npm run typecheck` clean; `npx vitest run` 87/87; full `npm run test:e2e` serial suite — PRIMARY + REACTIVE pass, GUARD skipped as intended. `pkg/web/dist/index.html` restored to placeholder. |  |
| P0-WEB-001 | done | WEB | Disable native pinch-zoom on mobile; keep the in-app UI-zoom slider as the only zoom (fixes the composer-floats / dead-space layout bug). |  | 2026-06-28 commit f301a94. |  |
| P0-WEB-010 | done | WEB | Fix in-browser terminal freeze inside full-screen TUIs (vim/less/htop): xterm.js v6's built-in DECRQM (`CSI [?] Ps $ p`) handler deadlocks the async write processor, so the screen freezes on the TUI's first frame while input still flows (user types blind). Register a no-op CSI handler swallowing DECRQM before the broken built-in runs. |  | 2026-06-28. Root cause pinned by byte-level bisection of vim's output stream; Go PTY bridge + tunnel confirmed clean (not the failing layer). Changed files: web/src/components/TerminalPane.tsx (fix — 2 registerCsiHandler calls swallowing DECRQM private+non-private), web/tests/e2e/terminal.spec.ts (regression e2e launching vim, typing in insert mode, asserting the marker renders + vim quits + shell returns; verified to FAIL without the fix). Verified: go build/test ./... ok, gofmt clean, npm run typecheck clean, npx vitest run 83/83, playwright terminal.spec.ts 3/3. pkg/web/dist/index.html restored to placeholder. |  |
| P0-WEB-002 | done | WEB | Raise UI-zoom ceiling to 200% (WCAG 1.4.4) and byte-mirror the initial viewport meta to runtime output. |  | 2026-06-28 commit 1221bcf. |  |
| P1-WEB-001 | done | WEB | Tighten or fix the dropped session-switch cursor save: a scroll-up→switch inside the 400ms debounce window fully misses (pending flushReadCursor cancelled before writing), so the scrolled-up position is lost entirely on reopen — comment overstates it as a one-idle-period lag. Either correct the comment, or add a synchronous cursor snapshot on switch for a stronger guarantee. |  | D-F1 from commit-review of acdef8e. ChatView.tsx session-switch effect (~:555-577) + scheduleReadCursor debounce (~:467-470). 2026-06-28 — comment corrected (Path A); the cancelled-flush loss case is now documented honestly. Real behavior fix (synchronous snapshot) tracked as P1-WEB-004. |  |
| P1-WEB-005 | done | WEB | Clear the finished-unread dot when opening a finished session already at the bottom. Root cause: `maybeRestore()`'s no-anchor branch pins to the bottom programmatically (no scroll event → `onScrolled`/`ackSession` never runs; even a synthetic scroll is skipped by the self-pin sentinel). Second facet: a session finishing while the user is already glued to the bottom also sticks the dot until a manual scroll. Fix: (a) explicit `ackSession` in the no-anchor bottom-pin branch of `maybeRestore`; (b) a reactive `createEffect` that acks when unread is set + following + at-bottom + ready (keys off signals, samples `nearBottom()` non-reactively). |  | 2026-06-28. Changed: web/src/components/ChatView.tsx — (a) explicit `ackSession(props.sessionId)` after `pin()` in the no-anchor else branch of maybeRestore (~:549-561, guarded by `!props.draft`); (b) new reactive ack createEffect (~:616-631) that acks when `state.unread[sid]` + `following()` + `nearBottom()` + `ready()` + `!props.draft`. Does NOT touch the self-pin sentinel (line ~678), the anchor-restore branch, or the Go /vh/ack backend. Verified: `npm run typecheck` clean; `npx vitest run` 87/87 unit pass; `npx playwright test tests/e2e/scroll-follow.spec.ts` 5/5 pass (scroll/follow regression guard). Could not manually repro the live "finish-while-watching" case (no live OpenCode backend in this env); the two code paths are covered by the e2e suite's existing scroll-follow coverage + typecheck. |  |

## Cancelled

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
