# Backlog

Canonical task-status source of truth for the vh-solara repo. One row per
task; status drives where it lives. Active work stays in `Now` / `Next` /
`Later`; completed/cancelled work moves to `Done` / `Cancelled` and is archived
under `docs/planning/archive/` by
`.opencode/scripts/normalize-backlog.js`.

Conventions live in [`AGENTS.md`](../../AGENTS.md) → "Backlog tracking rules".
Run `/backlog-cleanup` (or
`vh-agent-harness exec node .opencode/scripts/normalize-backlog.js`) after any
status change that completes, cancels, or reorders work, so the active sections
stay active-only and history is archived.

Row format: `| ID | Status | Area | Task | Owner | Notes | Links |`.
Statuses: `todo`, `in_progress`, `blocked`, `done`, `cancelled`. Put a
machine-readable completion date (`YYYY-MM-DD`) in `Notes` for `done` /
`cancelled` rows so the normalizer can file them into the right archive quarter.

## Area Legend

| Code | Boundary | Notes |
| --- | --- | --- |
| `WEB` | `web/` | SolidJS SPA (Vite + TS), embedded via `//go:embed`. |
| `API` | `pkg/web/`, `pkg/server/` | The `/vh/*` HTTP API + daemon handlers. |
| `TUNNEL` | `pkg/tunnel/` | Controller ↔ worker multiplexed WebSocket tunnel (yamux). |
| `AGG` | `pkg/aggregator/`, `pkg/state/`, `pkg/opencode/` | OpenCode state aggregation + session store. |
| `AUTH` | `pkg/auth/` | Controller auth / worker enrollment. |
| `QUOTA` | `pkg/quota/` | Leaf provider-neutral LLM-quota reporting package. |
| `PWA` | `web/public/`, manifest, service worker | Installable PWA surface. |
| `REL` | `.github/`, `install.sh`, `Dockerfile*`, `Makefile` | Tag-driven release + packaging. |
| `CI` | `.github/workflows/` | GitHub Actions CI pipelines (workflow YAML). |
| `INFRA` | `Dockerfile*` (dev/test), toolchain + browser image | Containerized test/dev environment across all lanes; environment consistency + isolation by default. |
| `DOCS` | `docs/` | Durable repo guidance. |
| `HARNESS` | `.opencode/`, `.vh-agent-harness/` | Agent harness integration. |

## Archive Index

Older `done` and `cancelled` history lives under [docs/planning/archive/index.md](archive/index.md) and is meant for on-demand reading instead of auto-loading into the active backlog context.

- [2026 Q3](archive/backlog-archive-2026-q3.md) - 75 done / 0 cancelled
- [2026 Q2](archive/backlog-archive-2026-q2.md) - 12 done / 0 cancelled
- [Undated](archive/backlog-archive-undated.md) - 1 done / 0 cancelled

## Now

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P1-DOCS-003 | blocked | DOCS | Create `docs/ai/shell-execution.md` (Slice C) — AGENTS.md cites it 13× but it doesn't exist (load-bearing for shell-guard). Min content: `vh-agent-harness exec` golden rule + Forbidden-patterns table (from `.opencode/repo-configs/forbidden-patterns.core.js` + `shell-guard-core.js`) + repo-relative-paths rationale. Do NOT hand-edit generated `.vh-agent-harness/AGENTS.core.md` or `.opencode/*` refs. |  | Deep-review R2 finding. BLOCKED on maintainer sign-off of forbidden-patterns wording. backlog-now (ready once wording approved). |  |

## Next

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P0-API-001 | todo | API | Tighten CSP `script-src` to `'self'` (drop `unsafe-inline` / `eval`) once stable. |  | From TODO at pkg/web/server.go:601. |  |
| P0-API-002 | todo | API | Map opencode session alias / share slug into `shapeSessions` once the pinned opencode version exposes it in session JSON. |  | From TODO at pkg/web/sessions.go:283. |  |
| P1-API-001 | todo | API | Re-add code-fence info-string attribute support (hl_lines / linenos / linenostart / nohl / hl_style) to the custom render NodeRenderer if it ever becomes user-facing; intentionally dropped today. |  | F2 from commit-review of 28d32c68. pkg/render/renderer.go renderFencedCodeBlock treats the info string as a language name only; the compact chroma-routing renderer replaced stock goldmark-highlighting/v2. The dropped attributes are documentation-style and not authored in OpenCode chat content. |  |
| P2-WEB-004 | todo | WEB | Add a pre-switch m1-marker presence assertion to `web/tests/e2e/sync.spec.ts` test 2 ('switching sessions re-targets the message stream'). The existing post-switch assertion is an ABSENCE check (demo's m1 marker 'Refactor the parser and explain the change.' must NOT appear after switching to `other`); commit-reviewer F3 (commit 182f2c67, dropped advisory) noted it would silently pass for the wrong reason if demo ever stopped rendering the marker (fixture-seed change at `pkg/fixtures/opencode.go:73` or a ChatView render regression). Add `await expect(page.getByText('Refactor the parser and explain the change.')).toBeVisible();` immediately after the existing `demoMsgs > 0` count block, BEFORE the switch click — so a marker disappearance fails loudly on the pre-switch side instead of the post-switch absence check vacuously passing. Test-only; no production code. Marker literal mirrors `pkg/fixtures/opencode.go:73` (the existing '// keep in sync' anchor comment documents the coupling). Do NOT change the post-switch absence assertion shape (content-based is correct) — this is purely a complementary pre-switch presence guard. |  | source:review-defer; trigger:path_touched(web/tests/e2e/sync.spec.ts) FIRED; studied:2026-07-07; promoted-from:.local/coordinator/tasks/defer-sync30-preswitch-marker-visibility.json; DoR passed 2026-08-02 (all 6 gates met). Validation: web e2e lane — sync.spec.ts --repeat-each=20 --retries=0 → 60/60; serial read-position.spec.ts + sync.spec.ts → 5/5. Low priority: belt-and-suspenders against future fixture-seed drift (test currently works correctly). |  |

## Later

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P1-INFRA-001 | todo | INFRA | Dockerize the entire test/dev environment (all lanes, not just `make e2e`): SPA build, Go toolchain, Playwright browsers, fixture server, and the e2e `webServer` lane. |  | Operator-requested future intent (captured only; not started). Reason: environment consistency + isolation by default — today only the e2e lane is containerized via `scripts/fixture-web.sh`, while the Go toolchain, node, and browser binaries are host-dependent, causing cross-machine divergence. |  |
| P1-AGG-008 | todo | AGG | Add a provably-correct causal freshness fence for warm-resync message deletion (upstream-dependent). OpenCode exposes NO causal signal today (no seq/etag/cursor on GET `/session/<id>/message` or on live events; SSE `id:`/`event:` discarded; "no replay"). So the warm-resync absence-inference fix (commit `a36dcddf`, Option A) shipped as a defensive mitigation: absence never deletes; deletions come only from explicit `message.removed`/`message.part.removed`/`session.deleted` events. The accepted tradeoff: a message/part whose explicit `*.removed` event is lost lingers until `session.deleted`. A provably-correct fence needs OpenCode to add a monotonic `seq` to every event envelope AND echo the current event-log `seq` on the GET response (one counter, two surfaces). Then the daemon can fence: `fetched.seq <= highestAppliedEventSeq` ⇒ discard fetched list as stale. Blocked on upstream OpenCode support. |  | See `docs/checkpoints/2026-07-26-warm-resync-absence-deletion-fix.md`. Investigation: 7 read-only dispatches (repo-explorer + 3 researcher + 2 solution-brief + protocol-fact lookup). |  |

## Done

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P1-WEB-019 | done | WEB | ReasoningPart stick-to-bottom has no resume re-pin guard — INVESTIGATED, IMMUNE (no fix required). There is no `ReasoningPart.tsx` file; it is a function in web/src/components/Part.tsx:572-641 with its own ResizeObserver on `.reasoning-body`. Unlike ChatView it uses a live `stick` boolean (Part.tsx:603-607) with NO stored `pinnedTop`/`pinnedScrollHeight` snapshot and NO intent latch, so the coalesced RO delivered on tab resume unconditionally re-pins when `stick===true`. Only a narrow theoretical edge (hidden settle-shrink + async server-HTML regrow + scroll-before-RO event ordering) could leave the bounded ≤320px "Thinking" panel slightly off-bottom, and it self-corrects on the next user scroll / part de-tail. Logged only to preserve the investigation so it is not re-opened. |  | Read-only study of follow-up A from commit 90f6ef12. Verdict: not a bug (immune). Do NOT implement unless an operator reports it. 2026-07-31. DONE. INVESTIGATED, IMMUNE — no defect. ReasoningPart (function in Part.tsx, not a separate file) has its own RO on .reasoning-body with a live stick boolean and no stored snapshot; self-corrects on next user scroll. No fix required. |  |
| P1-WEB-042 | done | WEB | No DOM/component-level regression test for the scrollEl ResizeObserver re-engage branch (the 2b "stuck-on-Latest" fix). The scrollEl ResizeObserver `else if (nearBottom())` re-engage branch in `web/src/components/ChatView.tsx` (a pure clientHeight grow fires no scroll event, so `onScrolled` cannot re-engage `following`) is exercised only by manual/Playwright, not by any component/unit test. Add a DOM-level/component regression that simulates a composer-shrink (clientHeight grow on `.chat-scroll`) while not following and at bottom, asserting `following` re-engages without a scroll event. Constraints: reuse the existing contentEl + scrollEl ResizeObservers — do NOT add a third RO on the scroll area; no mask-image/backdrop-filter/per-element content-visibility. |  | 2026-07-29. DONE. Commit `e8740aa` — routed scrollEl RO through classifyScrollDelta dual-axis reducer. Verified: ChatViewScrollResize.test.tsx 3 tests; 1733 web unit tests pass. (Promoted from .local/coordinator/tasks/defer-scroll-ro-reengage-dom-test; source:review-defer; trigger:path_touched(web/src/components/ChatView.tsx) FIRED.) |  |
| P2-API-003 | done | API | C5 follow-up: strengthen the archive-loop TOCTOU comment at `pkg/web/archive.go:130-134` from "out of C5's scope" into an explicit accept-decision. The comment records: (1) true atomicity is impractical across the OpenCode HTTP boundary (spawns originate in OpenCode, not the store, so a store lock cannot prevent them); (2) a re-Descendants guard per loop iteration is still racy per-id and adds N reads without eliminating the window; (3) the residual race is ACCEPTED — the orphan banner surfaces the straggler; (4) blast radius is one recoverable un-archived straggler, not data loss; (5) names the recovery path (web/src/components/OrphanBanner.tsx, fed by isOrphanLocked in pkg/state/tree_emitter.go via the tree.orphan facet). Docs-only; ZERO code logic change. | defer-build-c2c5 | 2026-07-27. DONE. Commit `9c80c415d53d221f35914fdbd6d8316bcd721db7` (code only; backlog separate). Comment block at archive.go:130-149 now an explicit accept-decision; the `if body.ExpectedFingerprint != ""` check + SetArchived loop byte-identical to HEAD. Verified: `go build ./pkg/web/ && go test ./pkg/web/` (ok 70.127s, `archive_drift_test.go` green). commit-reviewer APPROVE (unanimous, zero findings). Card: `.local/coordinator/tasks/c5-defer-archive-loop-toctou.json`. |  |
| P2-TEST-004 | done | WEB | Delete the dead matchMedia `vi.hoisted` stub at `web/tests/unit/codeCopy.test.ts:28-45`. The stub was dead: commit `1871625` rewired the import from `../../src/components/Part` → `../../src/lib/markdownEnhance`, removing the `Part→code/frame→layout→matchMedia` chain that justified it; `matchMedia` appears only in `web/src/lib/a11y.ts` (not in the `markdownEnhance→{pathlike,messageMarkdown}` import graph). Pure deletion (18 lines: 3-line comment + 15-line vi.hoisted block); `addCodeCopyButtons` import + `vi` import kept intact; no source change. | defer-build-c2c5 | 2026-07-27. DONE. Commit `9c80c415d53d221f35914fdbd6d8316bcd721db7` (code only; backlog separate). Verified: `npm --prefix web run test:unit` (119 files / 1543 tests pass, identical to baseline — proves the deleted stub was dead); `npm --prefix web run typecheck` clean. commit-reviewer APPROVE (unanimous, zero findings). Card: `.local/coordinator/tasks/df1-codecopy-matchmedia-stub.json`. |  |
| P1-WEB-048 | done | WEB | B1 — streaming URL-scrub diverges from classifyImageSrc: the post-parse `URL_SCRUB` in md.ts (renderStreamMd) + streamMd.ts (tokenHtml) neutralizes ALL `data:` schemes, silently undoing the classifier's raster-data keep (settled `rewriteImageSrcs` is correct). O3 (unify): export `RASTER_DATA_IMG` + extract `normalizeAttrs(html)` owned by messageMarkdown.ts; invoke from both streaming callers. `data:` kept only for allowlisted raster img MIME types on src AND href; `javascript:`/`vbscript:` still neutralized on both. Do NOT add `renderer.link`, do NOT change settled path. | defer-build-b1b4 | 2026-07-27. DONE. Commit `678e19d6fc1fc5ea4590f7fdd78ecb16c2e433bc` (code only; backlog separate). O3 (unify) taken — NO O1 fallback (extraction contained to the 2 existing callers, no broad refactor). Exported `RASTER_DATA_IMG` (was module-private) + added `normalizeAttrs(html)` in messageMarkdown.ts keyed on that regex; `md.ts` renderStreamMd + `streamMd.ts` tokenHtml now call it (inline `URL_SCRUB` removed). js:/vbscript: still neutralized on href+src; `data:` kept only for allowlisted raster MIME on src AND href (mirrors classifyImageSrc); settled `rewriteImageSrcs` untouched; no `renderer.link` added. Verified: `npm --prefix web run test:unit -- streamMd messageMarkdown` (123 passed, 3 files); full web unit suite 1543 passed (119 files); `npm --prefix web run typecheck` clean. commit-reviewer APPROVE (4/4 leaves, zero blockers, zero DEFER). Card: `.local/coordinator/tasks/defer-streaming-data-image-scrub-divergence.json`. |  |
| P2-TEST-003 | done | WEB | B4 — fill sidebar-pins.test.ts coverage gaps. Gap 1 (false-green): rewrite the "clears a prior advisory error on a confirmed doc" test (L105-110, comment admits it never plants `pinsLastError`) to FIRST drive an error state (network-failing `togglePin`) then assert the snapshot clears it. Gap 2 (missing race): add a test for `applyPinsSnapshot` (server-doc path) landing during an in-flight PUT. Gap 3 DROPPED (covered by DEFER1 at L604-638). | defer-build-b1b4 | 2026-07-27. DONE. Commit `678e19d6fc1fc5ea4590f7fdd78ecb16c2e433bc` (code only; backlog separate). Gap 1 (false-green REWRITE): "clears a prior advisory error on a confirmed doc" now async — drives a real `pin-network` error via network-failing `togglePin` FIRST (asserts `pinsLastError()==="pin-network"`), then asserts the confirmed snapshot clears it (load-bearing). Gap 2 (NEW): added "a pins.snapshot during an in-flight PUT reconciles without clobber" — `applyPinsSnapshot` (revision-exempt server-doc path) lands mid-PUT; asserts no clobber + revision monotonic (1→2→3) + `pinsLastError` null. Gap 3 DROPPED: 409-retry-rollback race is covered by the DEFER1 test at `sidebar-pins.test.ts` (~L633 after rewrite) — not duplicated. Verified: `npm --prefix web run test:unit -- sidebar-pins` (45 passed); full suite 1543 passed; typecheck clean. commit-reviewer APPROVE. Card: `.local/coordinator/tasks/p5-defer-sidebar-pins-test-gaps.json`. |  |
| REFAC-MAINT-005 | done | QUOTA | **[refactor-maintainability epic, Go slice 2]** Split `pkg/quota/providers.go` (345 LOC monolith) into 8 per-provider files. DEFER-triggered by F2/F6/F12/F13 from Go slice 1 review. Behavior-preserving verbatim move. | refactor-maintainability-coord | 2026-07-27. DONE. Commit `c516b9d44a94a3c47bce1155bc59ffd38872ecc9`. providers.go DELETED; 8 new files: provider_claude.go 44, provider_codex.go 63, provider_openrouter.go 40, provider_zai.go 35, provider_zhipuai.go 43, provider_kimi.go 59, provider_nanogpt.go 46, provider_copilot.go 66 (Copilot trio grouped — shared fetchCopilotUser). 10 functions moved byte-identical (reviewer ran git diff --no-index → zero diff). fetchCodex (~53 LOC) left as-is — credits-branch extraction not verbatim-safe. Verified: 14 quota tests, full go test ./... green, vet/gofmt clean, go build PASS. DEFER (re-captured): fetchCodex credits-extract, trigger `path_touched(pkg/quota/provider_codex.go)` (old trigger providers.go no longer exists). |  |
| REFAC-MAINT-004 | done | WEB | **[refactor-maintainability epic, TS slice 3]** Extract pin-authority CAS cluster from sidebar.ts into pins.ts. Behavior-preserving. Based on `.opencode/state/workstreams/refactor-maintainability/sidebar-cas-map.md` (study-cleared). | refactor-maintainability-coord | 2026-07-27. DONE. Commit `63ceea9838abc24b37e963d04477a67111bfed6b`. sidebar.ts 707→36 LOC (search signal + back-compat re-export shim); pins.ts 704 LOC NEW (entire pin CAS cluster). 8 files: sidebar.ts (M), pins.ts (NEW), 6 test files (import path updates). CAS invariant VERIFIED: 3 guards (adoptServerDoc-only revision bumps, rollbackOrderIfUnchanged issueRevision check, revision-monotonicity drops) co-located in pins.ts; sidebar.ts zero CAS refs. Module-singleton preserved. Verified: 1446 tests pass (identical baseline), typecheck clean, GPU-heat clean. DEFER (C-F3/D-F4): re-export shim leaves dual pin-import surface (5 consumers still import via ../sidebar); trigger `path_touched(web/src/sidebar.ts)`. |  |
| REFAC-MAINT-002 | done | WEB | **[refactor-maintainability epic, TS slice 2]** Extract tool-rendering helpers (`jsonPretty`/`looksXML` + `ToolBody`/`ToolPart`) from `Part.tsx` (now 639 LOC after REFAC-MAINT-001). Re-evaluate the `components/part/` namespace at slice-planning time — these ARE component-shaped (unlike slice 1's dep-free utils), so a `components/part/` sub-namespace may be justified there. Behavior-preserving; per-slice verification = full web test surface (`npm --prefix web run test:unit`) + typecheck + GPU-heat invariant grep. Pending operator GO/NO-GO on the refactor execution slices (workstream Phase fan-in). | refactor-maintainability-coord | 2026-07-27. DONE. Commit 5a4aff2f2b378b7b954a1b421d02e886860de94d (code only; backlog in separate commit per W1). Files: web/src/components/Part.tsx (639→333 LOC, −306), web/src/components/ToolPart.tsx (NEW, 339 LOC — toolIconName/ToolState/durationText/jsonPretty/looksXML/ToolBody/ToolPart extracted), web/tests/unit/toolRender.test.ts (NEW, 36 characterization tests). Verified: 1446 tests pass; typecheck clean; GPU-heat invariants preserved (CI-blind, grep-verified). Placement flat (components/ToolPart.tsx, NOT components/part/ — dir is 100% flat). Part.tsx↔ToolPart.tsx import cycle TDZ-safe (all cross-module refs inside component bodies). Line drift for future agents: Part.tsx now 333 LOC; FRAME_MS=L134, StreamMd.push=L145, ReasoningPart=L186, PartView=L257, ActivityGroup=L287. |  |
| REFAC-MAINT-003 | done | QUOTA | **[refactor-maintainability epic, Go slice 1]** Extract `pkg/quota/` DTOs + pure helpers into a focused, agent-addressable package. First Go refactor slice — parallel-eligible with REFAC-MAINT-002 once the TS pattern is proven (it now is, via REFAC-MAINT-001). Behavior-preserving; per-slice verification = full Go test surface (`go test ./...`) + gofmt + go vet. Pending operator GO/NO-GO on the refactor execution slices. | refactor-maintainability-coord | 2026-07-27. DONE. Commit 4603122c17737f712cc14c8af85e6ea3ba0d41e9. PREMISE CORRECTION: pkg/quota/ already existed as a 740-LOC monolith (quota.go); work was in-package decomposition, NOT cross-package extraction. Split into 6 focused files + 2 test files: quota.go 92 (orchestration), types.go 29 (DTOs), helpers.go 236 (pure helpers), auth.go 34 (auth.json I/O), http.go 34 (network), providers.go 345 (9 fetchXxx providers), helpers_test.go 277 (6 moved + 7 new), types_test.go 109 (DTO JSON round-trip). 36 top-level declarations partitioned 1:1 verbatim. Public API unchanged (Fetch/Report/ProviderResult/UsageWindow + JSON tags); pkg/web consumers unaffected. Verified: go test ./... green; go vet clean; gofmt clean; go build ./... PASS. DEFER (F2/F6/F12/F13): per-provider split of providers.go + fetchCodex ~53 LOC, trigger path_touched(pkg/quota/providers.go). |  |
| REFAC-MAINT-001 | done | WEB | **[refactor-maintainability epic, TS slice 1]** Extract markdown-enhance helpers from Part.tsx into `lib/markdownEnhance`. Pulled 4 dep-free DOM/string helpers (`linkifyPaths`, `tagInlineCodePaths`, `addCodeCopyButtons`, `splitMermaid`) verbatim out of the 757-LOC `Part.tsx` into a new `web/src/lib/markdownEnhance.ts`. Placement deviated from the brief (`lib/markdownEnhance.ts`, not `components/part/markdownEnhance.ts`) — adjudicated by `debate`: `components/` is flat and the helpers are dep-free DOM/string utils while the markdown-helper family already lives in `lib/`; slice 2 (tool-render) stays component-shaped. Behavior-preserving. **Epic context (NOT separate rows — these are hotspot targets, not tasks):** TS — `stream.ts` 3407, `ChatView.tsx` 3094, `Part.tsx` (now 639, this slice), `sidebar.ts` 707, `treeState.ts` 622; Go — `store.go` 4698, `server.go` 2064, `aggregator.go` 1067. Full briefs under `.opencode/state/workstreams/refactor-maintainability/`. | refactor-maintainability-coord | 2026-07-27. DONE. Commit `1871625f1adb3f7b6e9d4be118404aa16890c536` (code only; backlog in this separate commit per W1). Files: `web/src/components/Part.tsx` (757→639 LOC, −118), `web/src/lib/markdownEnhance.ts` (NEW — 4 helpers extracted verbatim), `web/tests/unit/codeCopy.test.ts`, `web/tests/unit/markdownEnhance.test.ts` (NEW — 18 characterization tests). Verified: 1410 tests pass; `npm run typecheck` clean; GPU-heat invariants preserved (CI-blind, grep-verified zero mask-image/backdrop-filter/per-element content-visibility declarations; protected regions shifted but unaltered). Line-range drift for future agents: `ReasoningPart` now at Part.tsx:492 (was 610, shifted −118); `FRAME_MS=200` at L178; `StreamMd.push` ref at L201. |  |
| P0-API-004 | done | API | SSRF image-proxy: add end-to-end regression tests for `imgDialContext` dial-time IP pinning + `imgFetch` redirect-hop re-validation. The validation predicate `imgAddrForbidden` has 29 unit cases, but the actual dial/redirect *behavior* (every resolved addr validated, dialed literal == validated IP, each redirect Location re-validated via `imgValidateURL`, HTTPS→HTTP downgrade + loop rejection) is untested. Release-gated (`always_before(release)`); trigger `path_touched(pkg/web/img.go)` FIRED in commit 6335859 (both img.go +473 + img_test.go +323). DoR met + operator override (ready to pick). | p0-api-004-img-ssrf-regression | 2026-07-27. DONE. Code commit `d14d0f70bbe4992d779c46f48ca744d417f93caf` (img.go seam extraction + img_test.go 11 tests; code only, backlog split). Seam: unexported `imgDialConfig{resolver,dial}` + `imgDialDefaults` (production) + `imgDialContextWith(c)`; `imgDialContext` → 2-line wrapper. DNS-timeout wrap relocated inline→imgDialDefaults.resolver (behavior-preserving; E4 covered by TestImgDialContext_DNSTimeoutRelocationPreservesCtx). E1 anti-rebind invariant comment at literal-dial. Policy (imgAddrForbidden/imgForbiddenV4) UNTOUCHED. Tests (TestImgDial*: RejectsMixedAddressList, PinsValidatedLiteral_NoRebind [THE crux — dialed==["93.184.216.34:80"] + resolverCalls==1], IPLiteralShortcut, ProductionDefaultsUnchanged, DNSTimeoutRelocationPreservesCtx; TestImgFetch*: RedirectToForbiddenIPLiteral_RejectedPerHop, HTTPStoHTTPDowngrade_Rejected, RedirectLoop_Rejected, TooManyRedirects_Rejected, MultiHopLegit_Succeeds, NeverForwardsUpstreamCredentials). Behavioral-closure: crux1(dial pinning)=proven, crux2(redirect re-validation)=proven. DEVNOTE: plan named 203.0.113.1 as "valid public" but it is TEST-NET-3/forbidden by imgForbiddenV4; substituted 93.184.216.34. Verified: `go test ./pkg/web/ -run TestImgDial \| TestImgFetch -v` (11 PASS), full predicate+new suite PASS, gofmt clean, go vet ./pkg/web/ clean. commit-reviewer APPROVE (unanimous). Card: `.local/coordinator/tasks/defer-img-ssrf-redirect-dial-regression.json`. |  |

## Cancelled

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P1-WEB-010 | cancelled | WEB | Pin `bottommostReadFromDom`'s scroll-origin fallback in vitest. The mid-history-anchor regression (P1-WEB-009 / 84251be) is currently covered by exactly one serial e2e path (`unread-dot.spec.ts:173`). No isolated unit test pins `bottommostReadFromDom`'s `rows[0]?.id` fallback at the scroll origin (first row sits at relative top=+16 due to `.chat-scroll` `padding:16px`). A future refactor of `bottommostRead`'s threshold (`web/src/lib/scroll.ts`) could regress silently until the serial e2e suite reruns. Add a vitest unit test asserting `bottommostReadFromDom` returns `rows[0].id` when the first row sits at top=+padding (scrollTop=0) and `nearBottom` is false. | web | 2026-07-11. Cancelled — relocated to .local/coordinator/tasks/ as trigger-gated DEFER candidate defer-bottommostreadfromdom-vitest (trigger: path_touched(web/src/lib/scroll.ts), not yet met). Original note: D-F1 from commit-review of 84251be (advisory, dropped on P1-WEB-009). Deferred. Trigger to un-defer: `web/src/lib/scroll.ts` `bottommostRead` threshold touched. Locate the existing scroll unit-test home (likely `web/tests/unit/scroll.test.ts`, modeled on the `bottommostRead` tests referenced in P1-WEB-002) and add the case there. |  |
| P1-WEB-015 | cancelled | WEB | Surface a user-visible error/retry state in the ChatView UI for failed background message hydration. Slice C (P1-AGG-001) added `messages.error` events, but the FE reducer only `console.warn`s them — a failed background fetch shows an indefinite "Loading…" with no operator-visible signal. The daemon self-heals on the next session selection/reconnect, but there is no affordance to see or retry a failed load. Build an explicit error/retry chip when faced with it in practice. | build | 2026-07-11. Cancelled — relocated to .local/coordinator/tasks/ as trigger-gated DEFER candidate defer-chatview-hydration-errorchip (operator-judgment-gated soft trigger — no path_touched). Original note: Deferred follow-up from Slice C: the `messages.error` event reaches the FE reducer as `console.warn` only (`web/src/sync/stream.ts`), while the ChatView empty-state/loading branch (`web/src/components/ChatView.tsx`) renders an indefinite "Loading…" on a failed background fetch — add the error/retry chip there. | P1-AGG-001 |
| P1-WEB-013 | cancelled | WEB | Stuck reasoning timer (`Part.tsx:549` `live()` + `selectors.ts:180`) — cosmetic, self-heals on turn completion. Fix: shared read-side helper `reasoningEnded(part, message) = !part.time?.end && hasSuccessorPart(message, part.id)` mirroring Go's alerts-engine inference (`pkg/alerts/engine.go:255-258`); needs fixture extension (today's fixtures at `pkg/fixtures/opencode.go:75,146` always supply `time.end`). Rejected: upstream per-message-flag (protocol change) and client-side time.end reconciliation (fights part snapshots). |  | 2026-07-11. Cancelled — relocated to .local/coordinator/tasks/ as trigger-gated DEFER candidate defer-reasoning-timer-stuckend (fixture-capability-gated; promoter must confirm a fixture case emitting an in-flight reasoning part WITHOUT time.end exists before re-promoting). Original note: Concept A from the 419ea39 / P1-WEB-011 live-autofollow slice — explicitly out-of-scope there, pre-scoped for deferred pickup. Deferred: do NOT implement until a fixture extension that can emit an in-flight reasoning part WITHOUT `time.end` (so the stuck-timer state is reproducible end-to-end). |  |
| P1-DOCS-001 | cancelled | DOCS | Fix the AGENTS.md restore-placeholder verb so it matches shell-guard (`commit-gate.sh revert`, not `git checkout`). The "Web frontend" section of the generated `AGENTS.md` (sourced from `AGENTS.mission.md`) instructs agents to run `git checkout -- pkg/web/dist/index.html` to restore the placeholder after a local build, but `shell-guard` blocks `git checkout` for non-committer agents and `.opencode/docs/git-execution-routing.md` sanctions `.opencode/scripts/commit-gate.sh revert <paths>` as the in-session restore-to-HEAD path. Correct the mission overlay text so the instruction matches the enforced guard. |  | 2026-07-04 SUPERSEDED by P1-WEB-025 (done 2026-07-03). The current `.vh-agent-harness/AGENTS.mission.md:42-44` already routes agents through `.opencode/scripts/commit-gate.sh revert pkg/web/dist/index.html` (shell-guard-aligned). The old "instructs agents to run `git checkout`" wording this task targeted no longer exists — no edit required. Surfaced while fixing/committing 84251be (P1-WEB-009). `AGENTS.md` is generated, so edit `AGENTS.mission.md` — the line "When a local build dirties `pkg/web/dist/index.html`, `git checkout -- pkg/web/dist/index.html` before committing." — not the rendered file. Verify: grep `AGENTS.md` for the corrected verb and confirm no `git checkout -- pkg/web/dist` instruction remains. |  |
