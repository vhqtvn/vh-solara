# Backlog

Canonical task-status source of truth for the vh-solara repo. One row per
task; status drives where it lives. Active work stays in `Now` / `Next` /
`Later`; completed/cancelled work moves to `Done` / `Cancelled` and is archived
under `docs/planning/archive/` by
`.opencode/scripts/normalize-backlog.js`.

Conventions live in [`AGENTS.md`](../../AGENTS.md) → "Backlog tracking rules".
Run `/backlog-cleanup` (or
`vh-agent-harness exec node .opencode/scripts/normalize-backlog.js`) after any
status change that completes, cancels, or reorders work, so the active sections
stay active-only and history is archived.

Row format: `| ID | Status | Area | Task | Owner | Notes | Links |`.
Statuses: `todo`, `in_progress`, `blocked`, `done`, `cancelled`. Put a
machine-readable completion date (`YYYY-MM-DD`) in `Notes` for `done` /
`cancelled` rows so the normalizer can file them into the right archive quarter.

## Area Legend

| Code | Boundary | Notes |
| --- | --- | --- |
| `WEB` | `web/` | SolidJS SPA (Vite + TS), embedded via `//go:embed`. |
| `API` | `pkg/web/`, `pkg/server/` | The `/vh/*` HTTP API + daemon handlers. |
| `TUNNEL` | `pkg/tunnel/` | Controller ↔ worker multiplexed WebSocket tunnel (yamux). |
| `AGG` | `pkg/aggregator/`, `pkg/state/`, `pkg/opencode/` | OpenCode state aggregation + session store. |
| `AUTH` | `pkg/auth/` | Controller auth / worker enrollment. |
| `QUOTA` | `pkg/quota/` | Leaf provider-neutral LLM-quota reporting package. |
| `PWA` | `web/public/`, manifest, service worker | Installable PWA surface. |
| `REL` | `.github/`, `install.sh`, `Dockerfile*`, `Makefile` | Tag-driven release + packaging. |
| `CI` | `.github/workflows/` | GitHub Actions CI pipelines (workflow YAML). |
| `INFRA` | `Dockerfile*` (dev/test), toolchain + browser image | Containerized test/dev environment across all lanes; environment consistency + isolation by default. |
| `DOCS` | `docs/` | Durable repo guidance. |
| `HARNESS` | `.opencode/`, `.vh-agent-harness/` | Agent harness integration. |

## Archive Index

Older `done` and `cancelled` history lives under [docs/planning/archive/index.md](archive/index.md) and is meant for on-demand reading instead of auto-loading into the active backlog context.

- [2026 Q3](archive/backlog-archive-2026-q3.md) - 95 done / 0 cancelled
- [2026 Q2](archive/backlog-archive-2026-q2.md) - 12 done / 0 cancelled
- [Undated](archive/backlog-archive-undated.md) - 1 done / 0 cancelled

## Now

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P1-DOCS-003 | blocked | DOCS | Create `docs/ai/shell-execution.md` (Slice C) — AGENTS.md cites it 13× but it doesn't exist (load-bearing for shell-guard). Min content: `vh-agent-harness exec` golden rule + Forbidden-patterns table (from `.opencode/repo-configs/forbidden-patterns.core.js` + `shell-guard-core.js`) + repo-relative-paths rationale. Do NOT hand-edit generated `.vh-agent-harness/AGENTS.core.md` or `.opencode/*` refs. |  | Deep-review R2 finding. BLOCKED on maintainer sign-off of forbidden-patterns wording. backlog-now (ready once wording approved). |  |

## Next

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P0-API-001 | todo | API | Tighten CSP `script-src` to `'self'` (drop `unsafe-inline` / `eval`) once stable. |  | From TODO at pkg/web/server.go:601. |  |
| P0-API-002 | todo | API | Map opencode session alias / share slug into `shapeSessions` once the pinned opencode version exposes it in session JSON. |  | From TODO at pkg/web/sessions.go:283. |  |
| P1-API-001 | todo | API | Re-add code-fence info-string attribute support (hl_lines / linenos / linenostart / nohl / hl_style) to the custom render NodeRenderer if it ever becomes user-facing; intentionally dropped today. |  | F2 from commit-review of 28d32c68. pkg/render/renderer.go renderFencedCodeBlock treats the info string as a language name only; the compact chroma-routing renderer replaced stock goldmark-highlighting/v2. The dropped attributes are documentation-style and not authored in OpenCode chat content. |  |

## Later

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| B1 | todo | AGG | server-side hydration ordering (tree frontier truthfulness) |  | scope: seed activity before structure in pkg/aggregator/hydration.go (store.Hydrate ~L64 then concurrent SetActivityFromStatuses ~L114 creates window shipping running nodes as never-seeded activity). rationale: client-side establishment gating (activityEstablished in web/src/sync/treeState.ts) is current mitigation; B1 remains an ordering hardening option (frontier truthfulness) for the <=60s window. triggers: 1) operator reports spinner-less/missing branches immediately after daemon restart; 2) new tree.snapshot consumer introduced; 3) client-side establishment gating proves insufficient in e2e. |  |
| P2-API-006 | todo | API | lastAssistantText pin-test + stale doc fix. Table-driven unit test at `pkg/web/sessions_test.go` for equal-created wrap-safe tiebreak (highest created wins, equal created -> later listing wins, absent -> 0.0); correct handler doc `pkg/web/sessions.go:146-147` from "id DESC" to later-listing-index. |  | source:p2-followup; override:operator; trigger:path_touched(pkg/web/sessions.go); studied:2026-08-16; ref: researches/sources/2026-08-16-ordering-fix-defer-followups.md |  |
| P2-AGG-009 | todo | AGG | ordering-writer pin tests + helper tightening. Pin `setCreatedKey` keyless→keyed reposition and `MergeOlderMessages` created-key cache via `TestPlaceholderPromotionRepositionsOrder` and `TestMergeOlderThenFullHydrateStaysChronological` in `pkg/state/hydration_order_test.go`; tighten `assertOrderChronological` fail-open trailing-suffix. |  | source:p2-followup; override:operator; trigger:path_touched(pkg/state/message_window.go); studied:2026-08-16; ref: researches/sources/2026-08-16-ordering-fix-defer-followups.md |  |
| P2-AGG-010 | todo | AGG | historyExhausted recalibration on full-history warm reconcile. Thread `fullHistory` flag into `reconcileMessagesLocked` (true from `Hydrate`, false from `SetSessionMessages`) to prevent wasted upstream `MessagesBefore` GET on FE "load older" after full hydrate. |  | source:p2-followup; override:operator; trigger:path_touched(pkg/state/hydration.go); studied:2026-08-16; ref: researches/sources/2026-08-16-ordering-fix-defer-followups.md |  |
| P2-AGG-011 | todo | AGG | pkg/opencode/id.go post-wrap doc corrections. Qualify "interleave correctly" and "sorts correctly" to within current 2^48 cycle in `MintMessageID` and header; restate queue requirement; note `ParseMessageIDTime` decoded value is `unixMilli mod 2^36`. |  | source:p2-followup; override:operator; trigger:path_touched(pkg/opencode/id.go); studied:2026-08-16; ref: researches/sources/2026-08-16-ordering-fix-defer-followups.md |  |
| P2-API-007 | todo | API | Fix latent semver issue where `normVer` in `cmd/opencode_update.go` strips build metadata from `cmd.Version` (e.g., `v1.60.0+dev` becomes `1.60.0`), treating local dev builds as equal to base releases. Fix guidance: choose API per actual caller (preserve metadata, ignore for SemVer precedence, or add `IsDevBuild()`). Context: `cmd/version.go:14`, `cmd/update.go:66-67`, commit `6645bb5`. |  | source:review-defer; trigger:behavioral (reopen if future change passes `cmd.Version`/app-version to `normVer`/`compareSemver`, changes `cmd/update.go` from string equality to precedence comparison, or directly compares app-version with OpenCode-version); studied:2026-08-20; ref: tmp/agent-runs/version-stamp-advisories/brief.md |  |
| P2-WEB-050 | todo | WEB | End-to-end test for the archive-failure SSE→transport→signal→banner path. Scope: observe a real `archive-failures.snapshot` (and `.updated`) emitted through live SSE from a real daemon, and assert the mounted `ArchiveFailureBanner` appears, survives reconnect, and clears on retry success — in ONE selected Playwright lane. The lane decision (fixture-server web e2e gains an `archive-failures.snapshot` emission vs a host-web real-embed test that already builds a real `local-server` binary) is part of the slice. |  | Promoted from DEFER card defer-archive-failure-e2e-banner-path (trigger fired: slice 2 `32ee1e3` shipped). Why: lifts behavioral-closure from inconclusive/not-demonstrable to proven for the visibility feature. MUST NOT claim the feature is already behaviorally proven (it is not — that's the point of the row). |  |
| P2-API-008 | todo | API | Egress-seam part-kind coverage gap: e2e-exercise `part.append`/`part.delete` suppression at the Stream-2 egress seam. (Currently only `part.upsert` is e2e-exercised; the other part kinds are unit-tested only for the `partWindowKey` classification). |  | source:review-defer; trigger:always_before(claiming complete Stream-2 part-class window-filter behavioral coverage); studied:2026-08-20 (from 9401881 review). |  |
| P2-WEB-052 | todo | WEB | Reconcile scoped-partial epoch caveat. When triggered: (1) read activity from the Stream-2 scope-selected capture at the stamp seams (~2 lines, snapshots.go:557-564); (2) belt-and-braces: clear epoch on project switch (1 line in actions.ts). Do NOT scope the epoch write to full reconciles only. |  | source:review-defer; trigger:path_touched(web/src/sync/reconcile.ts); studied:2026-08-22. Substance: reconcile sets s.epoch unconditionally on frame, activity merges only in-scope; epoch never cleared. Residual: project-switch window can stamp an in-flight tail stale-idle. |  |
| P2-WEB-053 | todo | WEB | c-F1: positional-settlement lastness vs future filtering/virtualization. When triggered: derive lastness from store order (not rendered position) + add a filtered-case test. |  | source:review-defer; trigger:behavioral(change that filters or virtualizes ChatView message list); studied:2026-08-22. Substance: MessageParts positional settlement keys on RENDERED position (MessageRow.tsx index/count); filtered view could render mid-history orphan as "last" causing forever-live streaming presentation. |  |
| P2-TEST-005 | todo | API | adfb8de fixture/test hygiene advisories: (1) copy-on-strip in handleFixtureOrphan for hygiene; (2) extend O3 reconnect probe to `onmessage` if attachment style changes; (3) tighten `orph` script ID prefix if scratch session gains other scripting. |  | source:review-defer; trigger:path_touched(pkg/fixtures/opencode.go); studied:2026-08-22. Folded advisories from adfb8de review. |  |
| P1-INFRA-001 | todo | INFRA | Dockerize the entire test/dev environment (all lanes, not just `make e2e`): SPA build, Go toolchain, Playwright browsers, fixture server, and the e2e `webServer` lane. |  | Operator-requested future intent (captured only; not started). Reason: environment consistency + isolation by default — today only the e2e lane is containerized via `scripts/fixture-web.sh`, while the Go toolchain, node, and browser binaries are host-dependent, causing cross-machine divergence. |  |
| P1-AGG-008 | todo | AGG | Add a provably-correct causal freshness fence for warm-resync message deletion (upstream-dependent). OpenCode exposes NO causal signal today (no seq/etag/cursor on GET `/session/<id>/message` or on live events; SSE `id:`/`event:` discarded; "no replay"). So the warm-resync absence-inference fix (commit `a36dcddf`, Option A) shipped as a defensive mitigation: absence never deletes; deletions come only from explicit `message.removed`/`message.part.removed`/`session.deleted` events. The accepted tradeoff: a message/part whose explicit `*.removed` event is lost lingers until `session.deleted`. A provably-correct fence needs OpenCode to add a monotonic `seq` to every event envelope AND echo the current event-log `seq` on the GET response (one counter, two surfaces). Then the daemon can fence: `fetched.seq <= highestAppliedEventSeq` ⇒ discard fetched list as stale. Blocked on upstream OpenCode support. |  | See `docs/checkpoints/2026-07-26-warm-resync-absence-deletion-fix.md`. Investigation: 7 read-only dispatches (repo-explorer + 3 researcher + 2 solution-brief + protocol-fact lookup). |  |

## Done

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P1-WEB-054 | done | WEB | O2 Slice 2 — calm happy path (compact merged status line, static tap-ack replacing infinite pulse, severity stacking cap +N more, create-link candidate grouping, full-text payload expansion). | send-state UX lane (coordination session) | 2026-09-25. DONE (Commit `0ed1be4`). Surviving conditional obligations (defers with triggers): (a) multi-record "N send actions in progress…" merge copy + blocked/rejected notices-cap expansion are unit-proven only — not production-reachable under current single-flight/mint-supersede invariants; add production-seam coverage when those seams change; (b) on-device observation checklist remains open as the input for any copy tuning. | researches/decisions/2026-09-25-send-state-ux-design-and-defers.md |
| P2-API-011 | done | API | Send-state §8 open items: session-create/upload true idempotency (proxy create-receipts in /oc, modeled on pkg/web queue admission receipts), upstream dispatch custody, replayable claims, browser-reload outbox for unconfirmed attempts, remaining non-guard fetch bounds, hung-socket e2e fixture knob, upload filename/overwrite safety. | docs-steward | 2026-09-25. DONE (Commits 8b50feb, 872ea0d, 8171438, 4db7300). §8/create-certainty program complete. PARKED (1): upload verbatim dedupe + orphan GC (trigger: disk-pressure incident). PARKED (2): browser-reload outbox for unconfirmed attempts (trigger: a real lost-uncertain-after-reload incident). | researches/decisions/2026-09-25-send-state-ux-design-and-defers.md |
| P1-API-008 | done | API | Queue dispatches mid-restart leave item fenced but unpersisted prompt causes 404 (indistinguishable generic failure) | build | Incident 2026-09-18 (+07), session ses_f4ee263afffe6JK1yaiNy0nvs2 ("Fix multi-project yamux tunnel slowness"): queue item enqueued 15:57:20; "Restart opencode" clicked 15:57:37, killing opencode pid 572684 mid-dispatch (prompt_async had returned 204; opencode never persisted the message); the reconciler correctly ran 3 real 404s and set ReconcileTerminal=true with the GENERIC persistent-404 text — indistinguishable from an unrelated 404. Detection correct; the gap was explanation-only: no restart-time fence existed for queue dispatches (the P1-API-007 turn arm is structurally blind to this case — a never-persisted prompt_async leaves no assistant message for the orphan sweep). Fix landed in commit **9b3cf2e** (`fix(web): fence in-flight queue dispatches at opencode restart`, parent 067120c, tree 8c6ff11e): durable `QueueItem.RestartFenceAt` stamped on every `dispatching` item at BOTH production restart entries (pkg/web/opencode.go handleOpenCodeRestart, pkg/web/archive.go handleRestartOpenCode — the only two restartOC sites; handleRestartServer is a daemon restart, out of scope); restart-specific detail selected under the store mutex at mutation time; detector unchanged (3×404 budget/eligibility/fail-closed intact; fenced exact-match still heals to sent; unfenced 404s keep identical generic text). Verification receipts: e2e crux TestQueueRestartFence_MidDispatchRestartExplained (real POST /vh/restart-opencode mid-dispatch → fenced → 3 real 404s → restart-specific terminal, generic text absent, zero resends) + TestQueueRestartFence_IdleRestartUnfenced control; 6 new unit tests (pkg/web/restart_fence_test.go); full lanes green; behavioral-closure proven/proven. Review: commit-reviewer cascade unanimous approve (high confidence, zero blocking), fresh gate re-review 4/4 approve. 2026-09-18. DONE. | P1-API-007 |
| P1-API-007 | done | API | UI-triggered OpenCode restart kills in-flight turns and leaves them permanently unmarked (silent truncation) |  | Operator runtime receipt (2026-09-05 +07, session Next ses_076bdc20affeEpSJmhAOwbFPXq, workspace /home/vhnvn/repo/vh-solara): 02:54:16 assistant msg_06dfc72ab001cqHfGRwTMh03iR created (step-start; reasoning 02:55:47→02:56:14; tool `compress` completed "Compressed 19 messages"; NO step-finish, NO text part); 02:56:09 user msg_06dfe2735001L3EPwu1QxneU7M ("delete it") queued during the run, never consumed; 02:56:24 journal "Restarting opencode serve on port 44629 (requested via UI)" → "detached restart: stopping recorded instance pid=1501189 port=44629"; after restart the message STILL has time.completed absent + error null while /vh/diag/busy reports idle / runningRoots 0 (topology detached, state ready). Ruled out (do not fold in): orphan/split-brain (one opencode, port stable, live owner lock); 928ed3e (owned-arm stop-wait bounding — different arm); post-restart stale/slow view (48 GB opencode.db cold start — separate problem). Verified code receipts (coordinator, 2026-09-05): detached restart stop phase kills the recorded pid directly with no in-flight check/abort (cmd/opencode_start.go:345-361); killPID is SIGTERM-only (cmd/opencode_detached.go:277-284); the /vh/abort handler already owns the full choreography + conventions (WithIdempotency → Client().Abort → InflightAssistantID → Store().Stop, no synchronous force-idle; pkg/web/verbs.go:331-372); InflightAssistantID has no callers under cmd/ (pkg/state/turn_state.go:479); the restart entry has s.aggs reachable (pkg/web/opencode.go:179-206; server.go:2105-2129); the confirm dialog is warn-only and already promises "The in-flight turn(s) stop; sessions and history are preserved" (web/src/components/RestartOpenCode.tsx:196-206). Mechanism (researcher pass, 2026-09-05): NO path anywhere writes a terminal marker on the assistant message entry — completed/terminalError only ever mirror upstream payloads (pkg/state/store.go:489-502, :818); the sole closure is the FE presentation stamp stampCompletionIfIdle (web/src/sync/reducers.ts:81-92), which yields the silent truncation; the orphan predicate already exists (inflightAssistantIDLocked, turn_state.go:489-501); viable server-side seams: the 60s status reconcile (pkg/aggregator/reconciliation.go:57-77), hydrate-on-reconnect (pkg/aggregator/hydration.go:44-168), and the SetOnHydrate callback (lifecycle.go:132, queue-GC precedent) reachable from both binaries' post-restart SetReady points (client-daemon-vh.go:425-429, local-server.go:220-224). Upstream disposition (grounded web check, 2026-09-05): opencode ~1.18.x does NOT mark interrupted in-flight messages during startup recovery — its orphan sweeps (sweepOrphanAssistants class) are prompt-gated, not boot-gated, and open issues exist for exactly this class (null time.completed after a kill). Our fix must not depend on upstream and must not write to opencode's DB; upstream boot-time recovery is the upstream half (note for the operator to reference/file). Design decisions (settled at routing, to be validated + documented with reasoning by the implementation slice): (1) layered fix — bounded abort-before-stop at the shared pkg/web restart entry reusing the verbs.go choreography, PLUS a server-side orphan sweep that synthesizes the terminal marker in our store, triggered post-hydrate (heals restart-kill, crash, reboot, daemon restart; idempotent re-derivation) and on the status-reconcile clear path; marker must survive warm reconcile and never cancel a session opencode still reports busy (respect s.mu atomicity, stopGen, the TurnStopping/D4 discriminator, #2696 liveIdleObserved). (2) Upstream split as above. (3) Confirm dialog unchanged — the existing copy becomes true. (4) Unconsumed tail user messages: out of scope; residual note only (they render as ordinary user messages; the "delete it" referent-loss hazard is semantic, not bookkeeping). Constraints to record on the card: detached is production (owned must not regress); preserve the fail-closed owner-lock release wait (eb03548); every new wait bounded (928ed3e lesson — a hung abort must never wedge the restart); reuse verbs.go abort-path conventions, no parallel mechanism; tests per the nine-lane localization in AGENTS.md (state unit sweep invariants; web handler abort-before-restart + hung-abort bound; e2e fixture stall + stream-drop → marked interrupted; cmd owned/detached non-regression). DONE 2026-09-05 — commit f631ff7b4aae4bf1a7ba4c58c33c8dde123869d4 (code only; this backlog update commits separately per protocol). Implementation (14 files): layered fix — (a) bounded abort-before-stop at the shared pkg/web restart entry (abortInflightBeforeRestart on *Server, both /vh/opencode/restart and /vh/restart-opencode, before restartOC; walks s.aggs via new Store().RunningSessionIDs(); per-session Abort 2s per-call under 5s shared budget, mirroring the /vh/abort verb exactly — Abort error → log + skip Stop; hang/failure never blocks restart); (b) store orphan sweep pkg/state/interrupted_sweep.go triggered ONLY from the SetActivityFromStatuses clear path (successful statuses fetches: status-reconcile ticker + hydrate fan-out; failed fetch structurally cannot mark) — synthesizes completed + terminalError "VHSolaraInterruptedError" via the normal KindMessageUpsert channel; idempotent + sticky (synthesizedTerminal provenance survives warm reconcile + late message.updated; REAL upstream terminal supersedes/clears it; #2696-class no-resurrect guard extended). isTerminalError deliberately NOT extended (synthetic marker is inference, not opencode-positive). FE untouched (existing .msg-error/settled affordances render the marker); confirm dialog unchanged (its copy became true); owned arm + cmd/ untouched; eb03548 fail-closed wait and 928ed3e bounds preserved. Decisions D1-D4 with reasoning carried in the commit message. Validation at 54e5580 (= parent of f631ff7b, working tree): go build/vet/gofmt clean; go test ./pkg/... ./cmd/... ./tests/integration/ all ok; go test ./tests/e2e/ full ok (incl. TestE2E_RestartOrphanHeal; kill modeled via fixture seams, crux path real: real HTTP restart entries → real store sweep → marker via public snapshot API). Commit-review: APPROVE ×2 (pre-commit cascade + gate-anchored fresh review; unanimous, 0 blocking). Advisory DEFERs with triggers: bc-F1 add s.bumpMsgRev in markInterruptedLocked (stale cold-batch could transiently clobber the marker client-side; self-heals; trigger path_touched(pkg/state/interrupted_sweep.go)); d-F2 guard sweep on me.terminalError!="" + error-block-preserving merge (real error-without-completed rows currently re-stamped until messages re-fetch; gate review leaf D refuted the mislabel hypothesis against upstream v1.18.x; trigger path_touched(same file)); d-F1 TOCTOU fetch→apply gap can transiently false-mark a just-started live turn (self-heals via supersede; trigger same file); a-F1 MessagesLoaded two-tick latency after sweep untested. Residuals (not implemented, by design): unconsumed tail user messages render as ordinary user messages (D4 — the "delete it" referent-loss hazard is semantic, not bookkeeping; any future "sent during interrupted turn" affordance is new scope); upstream half = opencode boot-time recovery of its own DB (prompt-gated orphan sweeps today; operator may reference/file upstream). Watch item: fixture demo session m6 now gets marked on statuses clear (it IS an orphan) — no web e2e asserts its unmarked state, but worth one watch cycle on the next web-e2e CI run. DEFERs DISCHARGED 2026-09-05 — commit 76a30f0627b83283d1c8aa418261280ab367ad8d (fix(state): harden interrupted-turn sweep; 7 files, code only, this update commits separately): bc-F1 markInterruptedLocked now bumps msgRev both arms (cold-batch ABA pinned + mutation-checked); d-F2 real terminal always wins (no relabel; settle arm stamps completed alongside PRESERVED error per the canonical aborted shape, no synthesizedTerminal provenance; merge preserves existing error blocks; both orders tested); d-F1+a-F1 one coherent timing design — two-consecutive-successful-fetch notBusyOnce latch cleared by every busy-class signal incl. the in-flight-assistant escalation (closes the fetch→apply TOCTOU; repro test pins a just-started turn NOT marked), heal latency now second consecutive successful not-busy observation after kill (worst case +60s; e2e updated to two /vh/reload observations), MessagesLoaded two-reconcile gate explicitly pinned; latent double-clearActivity bug fixed in passing (clear loop deduped to the union). Gate review: unanimous approve, 0 blocking. NEW minor defer a-F3 (card-facing): keep the accepted +60s worst-case heal latency stated in the durable closeout receipt if restart_orphan_heal_test.go semantics ever change. Documented residuals: two consecutive lying not-busy observations during genuine streaming can still false-mark (supersede heals); #2696 post-idle escalation semantics unchanged. UPSTREAM THREAD DISPOSITIONED 2026-09-08 (solution-brief pass; strategy settled by operator): (1) CORRECTION of the earlier upstream citation — the symbols "sweepOrphanAssistants"/"sweepOrphanToolParts" (from a fast-tier web check) do NOT exist in opencode v1.18.28 or dev; the re-grounded mechanism is SessionProcessor.cleanup (packages/opencode/src/session/processor.ts), a live-processor Effect finalizer that can only unwind in-memory context, with no recovery invocation verified at serve startup, session init, passive reads, or any maintenance route. The card's load-bearing claim stands unchanged: opencode ~1.18.x performs NO boot-time recovery of interrupted in-flight messages. (2) Strategy: issue-only at most (draft 8-line issue text exists in the 2026-09-08 brief), NO upstream PR investment — operator judges opencode PRs too costly; VHSolaraInterruptedError is treated as the permanent answer, not a bridge (it is inference-labeled, never overwrites real terminals, and any future upstream fix auto-supersedes it via the existing supersede path). The two-stage retire criteria recorded above remain the formal bar, but Stage-2 removal is not expected to trigger and that is an accepted end state. No vh-solara changes pending upstream. Do NOT re-litigate this strategy without new operator direction or an upstream recovery contract actually shipping. |  |
| P1-API-006 | done | API | Owned initial-boot correctness — child-attributed boot readiness, fresh-port retry, fail-closed exhaustion; synchronized publication boundary. |  | Defect family (last real one in the theme): owned INITIAL spawn in client-daemon (setupVHMode) and local-server uses dial-only waitForPort (cmd/client-daemon.go) → squatter on the boot port yields false SetReady proxying foreign content; local-server boot arm also installs no reapOwnedOpenCode observer (post-readiness boot-child crash leaves ocLife stuck ready — P1-API-005 DEFER 3). Settled boot design (researched: NO operator-fixed owned boot port exists — both binaries select it internally): try the initial internally-selected port; ONE fresh auto-selected-port retry; exhaustion → SetFailed + keep serving (p1-oc-001); FINALIZE the effective URL BEFORE aggregator/web-server construction (no live retarget during boot — no subscribers exist yet). Do NOT add port-provenance infrastructure; if a fixed owned port is ever introduced later it must carry explicit fixed-vs-auto provenance and must not silently fall back. Slice A1 (medium, ~180–350 LOC, 5 files + 1 conditional): shared SYNCHRONIZED PUBLICATION BOUNDARY (production repair — the brief explicitly rejects another-oracle-poll/stress-test/documented-bound as insufficient for the child-exit-between-final-oracle-check-and-SetReady window) + deterministic barrier test; client-daemon boot adopts child-attributed wait (waitForChildOwnedPort or equivalent attribution) — satisfies the standing BLOCK trigger (P1-API-005 DEFER 2, review C-F1+D-F4) and discharges DEFER 1 (publish-boundary interleaving) + the client-daemon half of DEFER 2. Exact synchronization primitive / whether a natural oracle abstraction avoids a test-only hook = A1 review checkpoint. Slice A2 (medium, ~100–220 prod + 150–300 test LOC): local-server boot parity + boot-child observer (discharges DEFER 3) + minimal cobra-test containment (snapshot/restore + package-level serial guard; no t.Parallel; subprocess isolation only if leakage demonstrated — discharges DEFER 4) + FOLD/SUPERSEDE P2-API-005's readiness-gate keep-serving crux ONLY AFTER the real initial failed-but-serving outcome is proven through the running local-server. Behavioral cruxes: squatter on initial candidate port never credited/served as ready content; readiness attributed only to the replacement child after at most one fresh retry; exhaustion leaves vh-solara serving with lifecycle failed; failure at the exact final publication boundary cannot be overwritten by SetReady; post-readiness local-server boot-child crash flips ocLife to failed. Non-goals: detached-boot TOCTOU adjacency (shares the dial-only symptom but different no-reap/owner-lock lifecycle contract — stays a noted residual, NOT folded); restart arms (done); GOOS=windows tests/integration vet surgery (unowned baseline). 2026-08-31. DONE in slices A1 (a688776 fix(cmd): synchronize owned publication boundary + child-attributed owned boot) + A2 (aa8eb8d fix(cmd): child-attributed owned boot + observer + test containment for local-server) + gate follow-up 85590c0 (machine-asserted 200). A1: per-child ownedExitGate (RecordExit record-then-close / PublishIfAlive refuse-if-closed, both under one mutex — the exit-vs-readiness overwrite is structurally impossible; deterministic barrier tests pin both linearizations, no test hooks); client-daemon owned boot child-attributed via shared core (stable candidate → ONE fresh retry → SetFailed+keep-serving), URL finalized before consumers (no re-ordering needed). A2: local-server boot parity via startLocalOwnedOpenCode, boot-child reapOwnedOpenCode observer (post-ready crash → failed), cobra containment (complete snapshot incl. localOpenCodeUpdate + serial guard, no t.Parallel, no subprocess — no leakage). DEFERs DISCHARGED: P1-API-005 DEFER 1 (A1, fully — barrier-tested), DEFER 2 both halves (A1 client-daemon + A2 local-server; standing C-F1/D-F4 BLOCK trigger satisfied), DEFER 3 (A2 observer, outcome-proven), DEFER 4 (A2 containment); a688776 review d-F1 (A2). Full go test ./... green all slices; -race clean; tri-OS vet clean incl. tests/integration on windows (baseline fixed by a1e1932's linux tag). REMAINING advisories (path_touched, non-gating): c-F2 exhaustion SetFailed overwrites reaper's exit-code detail (shared core, next core touch); A-F2 restart-after-exhausted-boot untested (trigger restartOpencodeLocked); a-F1 barrier converse test's 50ms negative wait timing-based (mutex is the guarantee); d-F2 worst-case boot wall ~2× ownedReadyWait (bounded by design); local-server lacks lifecycle-ring fan-in parity (intentional asymmetry). 2026-08-31 residual-closure adjudication + fixes: 928ed3e resolves R11 (bounded owned stop-wait — shared stopOwnedOpenCodeChild: SIGTERM → 10s grace → SIGKILL → 5s bounded wait → refusal = truthful SetFailed, fail-closed keep-serving; proven both arms vs SIGTERM-ignoring fixture through real restart surfaces), R1 (exhaustion no longer overwrites reaper's exit-code detail when both children die — errors.Is(errOwnedChildExited) skips the generic SetFailed), R5 (local-server fans child output into lifecycle ring at boot/restart/owned spawns — /vh/opencode/logs proven non-empty through the running server), R2 (restart-after-exhausted-boot recovery test-pinned end-to-end), riders R9/R12 comment accuracy + R18 invariant docs (with CORRECTED premise: aggs[""] IS seeded in NewServer — the default's retarget coverage flows through that seeded entry; access is special-cased outside the map; removing the seed silently drops default coverage). 82828d1 resolves gate-F3 (detached restart arm now passes ring writer — fourth and final fan-in path; both children's banners asserted via real trigger). RETIRED with reasons: R3 (50ms barrier negative-wait cannot false-fail — recorder queues on the held mutex; fatal is true-positive-only), R4 (2× ownedReadyWait boot wall = accepted bounded-retry design), d-F2 dup. NEW ADVISORIES (drop-grade, from gate reviews): synthetic-oracle goroutine parks forever on a truly unkillable D-state child in the defensive done==nil branch (contract-violation-only, documented); wedged-crux test ceilings ~15s generous vs ~2.75s injected bound (CI headroom; unit test pins precise bounds); late-reaper SetFailed may supersede refusal summary (intentional, R1-consistent); F3-test precondition banner check point-in-time (reliable today); bootLocalServerDetachedForTest near-duplicates boot variant (parameterize on a third). | 3a6d109, 1d4efc4, P1-API-005, P2-API-005, tmp/agent-runs/owned-boot-defer-closure-brief/brief.md |
| P2-API-010 | done | API | Sweep evidence — deterministic negative-case tests for the defensive no-signal branch. |  | discharges P2-API-009's standing DEFER (trigger grammar: always_before(claiming behavioral completeness) \| path_touched(ocSweepIsFakeOC\|ocSweepIsGrandchild\|ocProcCmdlineArgs\|integFakeOCIdentity)). Slice B1 (small, ~50–120 test LOC, 2 test files; possibly +1 file ~20–60 LOC for a narrow recordable signal seam — not runtime-proven in the brief): test a missing pid, a live innocent non-matching process, and a matching positive control; the LOAD-BEARING assertion is ZERO signal attempts against rejected candidates (not merely survival). Keep separate from boot behavior; P2-API-009 remains incomplete until B1 lands. Verification adds `go test ./tests/integration/`. 2026-08-31. DONE as a1e1932 test(cmd): deterministic negative-case sweep evidence — recording-forwarder seam (ocSweepSignal/integSweepSignal, default-identical, test-scaffolding only) around the REAL sweep loop; load-bearing assertion = zero SIGKILL attempts against rejected candidates (sorted slices.Equal vs exactly the positive-control pids); candidates = missing pid + live innocent foreign-cmdline sleep (recorded under both suffixes) + matching positive controls from real starter runs (integration through exported cmd.EnsureDetachedOpenCode). DISCHARGES P2-API-009's standing completeness DEFER per its trigger grammar. Also landed (disclosed foreign hunk, reviewer-unanimous): the concurrent session's verified //go:build linux tag on tests/integration/opencode_spawn_lock_test.go — FIXES the recorded GOOS=windows go vet ./tests/integration/ baseline. STANDING DEFER F2: seam-var race-safety contingent on no t.Parallel in these packages (trigger: path_touched on either sweep file). Advisory nit: tag comment overstates syscall.Kill as the darwin exclusion (true exclusion is /proc dependence; tag correct on both platforms). Test-only; full suite green; -count=2 stable. [2026-09-02: windows is not a current target (operator). Windows-related follow-ups are parked, to be revisited only if windows becomes a target. Parked items: (1) optional CI hardening GOOS=windows go vet ./... step; (2) tag-comment wording advisory on tests/integration/opencode_spawn_lock_test.go:5 (correct wording: "syscall.Kill does not exist on windows, and the /proc inspection has no darwin counterpart").] UPDATE 2026-08-31: standing DEFER F2 (seam-var serial discipline) DISCHARGED as documented — serial-discipline comments landed at ocSweepSignal/integSweepSignal in 928ed3e (fires-and-satisfies its own path_touched trigger). Tag-comment advisory (R8) fixed in same commit (true darwin exclusion = /proc + fd-3 owner-lock machinery). | 3aed4b1, P2-API-009, tmp/agent-runs/owned-boot-defer-closure-brief/brief.md |
| P1-API-005 | done | API | Owned OpenCode restart must not false-ready on a foreign port; retry fresh and retarget. |  | Confirmed P1 defect (M): owned-topology restart respawns on the fixed boot port with no portFree guard; dial-only waitForPort accepts a squatter after the replacement child bind-fails → false SetReady + /oc/* proxying foreign content (anchors at brief time: client-daemon-vh.go owned restart arm ~:373-383, local-server.go ~:208-218, waitForPort cmd/client-daemon.go:205-217 — verify at implementation time by symbol). Design (settled by brief): shared child-aware owned-restart operation in BOTH binaries; prefer stable port but readiness must be attributed to the replacement child, never to "something accepting connections"; occupied/lost-bind-race → ONE bounded fresh-port attempt wired through the existing retarget seam (applyFreshPortRetarget / RetargetOpenCode / SetOpenCodeURL) BEFORE SetReady; exhausted → SetFailed + keep serving (p1-oc-001). Client-daemon: preserve sole-reaper ownership; prevent a later readiness write from overwriting a child-failure state. Local-server: direct wait ownership retained, readiness attributed to replacement child. Do NOT make child stderr wording (e.g. EADDRINUSE) part of the contract. Folds in: R2 (one child-aware fresh-port retry — the TOCTOU advisory from P1-API-002, no listener-fd redesign) and R4 (portable retarget/order coverage; non-Linux cannot prove flock//proc semantics — no standalone card). Slices: Slice 1 = shared core + client-daemon behavioral closure (file list in brief §Card1/Slice1); Slice 2 = local-server parity via the REAL accepted restart trigger + portable evidence, and fold-or-supersede the restart-related portion of P2-API-005 (annotate P2-API-005's Notes accordingly — do not delete it). Behavioral crux bar: foreign marker on old port NEVER served as restarted content; proxy+lifecycle follow the fresh child before readiness; exhaustion → failed-but-serving; local-server real-trigger test — if the seam proves infeasible, record INCONCLUSIVE rather than claiming helper coverage as behavioral proof. Load-bearing assumption (medium-high, operator-invited to object): owned port is private mutable runtime topology, not an externally-guaranteed fixed-port contract. 2026-08-31. DONE in two slices. Slice 1 = 3a6d109 fix(cmd): attribute owned-restart readiness to the replacement child (shared restartOwnedOpenCode core: stable-port attempt only when portFree; waitForChildOwnedPort exit-oracle attribution with 750ms stabilization — no stderr contract; exactly ONE fresh-port retry on pre-readiness child exit, timeout NOT retryable; applyFreshPortRetarget before SetReady; exhaustion SetFailed + keep serving; write-order guard; sole reaper preserved; client-daemon arm behavioral crux: foreign marker proven served pre-restart then NEVER served, fresh child's ok served, exhausted → failed-but-serving 502). Slice 2 = 1d4efc4 fix(cmd): wire local-server owned restart through the shared core — real-trigger trio drives POST /vh/restart-opencode through a running localServerCmd.Run (outcome-level); supersedes restart half of P2-API-005. Full go test ./... green both slices; -race clean; darwin/windows vet clean (cmd). STANDING DEFERs (path_touched triggers, NOT discharged): (1) publish-boundary interleaving test — child exit between final oracle check and SetReady can transiently overwrite; BLOCK trigger on any claim that owned restart cannot publish ready after a child failure; (2) BOOT arms (client-daemon AND local-server initial spawn) still dial-only waitForPort — same false-ready class, pre-existing; BLOCK trigger if client-daemon-vh.go boot wait modified without child-attributed wait; (3) local-server boot arm installs no reapOwnedOpenCode observer — post-readiness boot-child crash leaves ocLife stuck at ready; natural boot-parity follow-up candidate; BLOCK trigger on boot-arm behavioral-closure claims; (4) local-server_test.go mutates package-global cobra flags with process-lifetime servers — safe only while strictly serial (no t.Parallel). Advisory drops: stale doc comment on restartOpencode ("same port" — now can land fresh); duplicate SetStarting calls; unbounded stop-wait under opencodeMu (harden in shared core if ever). UPDATE 2026-08-31: all four standing DEFERs DISCHARGED by P1-API-006 (DEFER 1 in A1; DEFER 2 both halves in A1+A2; DEFER 3 + DEFER 4 in A2). UPDATE 2026-08-31 adjudication: R10 (duplicate SetStarting) RETIRED — premise wrong: the caller's write marks starting BEFORE the stop (truthful during slow reap), the core's marks each ATTEMPT (so the fresh retry isn't shown failed while running); both load-bearing, idempotent transition. R9 stale "same port" doc comments fixed in 928ed3e. Unbounded stop-wait advisory RESOLVED by 928ed3e's stopOwnedOpenCodeChild (both arms). | 29c9440, P1-API-003, tmp/agent-runs/oc-owned-arm-residuals-brief/brief.md |
| P2-API-009 | done | API | Lifecycle test-scaffold hygiene (sweep revalidation, dead branch, skip visibility). |  | Three items from the brief (R5): (1) cleanup sweeps must revalidate fake-process identity via /proc/<pid>/cmdline immediately before SIGKILL (innocent-pid hazard on pid recycling within a test run); (2) remove the dead .gchild suffix branch in the integration sweep; (3) keep pickLowPort's honest t.Skipf on port starvation, optionally clearer skip messaging for CI visibility. Slice 3 file list in brief. Independent of Card 1 but simplest to execute after its slices land. 2026-08-31. DONE as 3aed4b1 test(cmd): harden lifecycle test-scaffold sweeps — /proc cmdline revalidation immediately before every sweep SIGKILL (fail-closed skip on unreadable/foreign); dead integration .gchild branch verified dead and removed; pickLowPort skip names test + lost discriminator coverage. Rider dispositions: VH_STATE_DIR isolation already satisfied at HEAD (t.Setenv in newOCLockScenario since ef8e084 — verified, no redundant code); wrapper-dedupe premise stale (not byte-identical). Test-only slice; full go test ./... green; -count=2 stable. STANDING DEFER (do NOT claim behavioral completeness without it): the defensive skip branch (innocent-pid protection) has no deterministic negative-case test — positive path exercised every green run, skip path only by construction; trigger: always_before(claiming P2-API-009 behavioral completeness) OR path_touched(ocSweepIsFakeOC\|ocSweepIsGrandchild\|ocProcCmdlineArgs\|integFakeOCIdentity). Advisory: ocProcCmdlineArgs comment drift (production ocCmdlineMatches fails OPEN on read error, sweep fails CLOSED — tighten comment); sleep-3000 grandchild marker theoretical recycled-pid false positive (narrower than baseline); in-test-body killPid9 sites still signal without revalidation (slice scoped to sweeps). Known pre-existing baseline: GOOS=windows go vet ./tests/integration/ fails at HEAD (syscall.Kill undefined in pidAlive) — needs build-tag surgery, unowned. UPDATE 2026-08-31: standing completeness DEFER DISCHARGED by P2-API-010 (a1e1932 zero-attempt negative-case evidence); GOOS=windows go vet ./tests/integration/ baseline FIXED (linux tag landed in a1e1932). UPDATE 2026-08-31 adjudication: R12 comment-drift fixed + R13 (sleep-3000 marker false-positive: needs pid recycling AND identical cmdline in-window; blast = killing a sleep) and R14 (in-body killPid9 ms-window, ~1000× narrower than the hardened sweep surface) RETIRED as below-bar-theoretical; reuse the sweep identity check if those files are touched again. | tmp/agent-runs/oc-owned-arm-residuals-brief/brief.md, P1-API-002 |
| P1-API-003 | done | API | Propagate effective restart port to the running daemon (re-target proxy after fresh-port restart) |  | Filed 2026-08-30 from P1-API-002 advisory material (operator-approved; NOT a path_touched DEFER — a standalone defect). Double-corroborated: read-only advisory study + commit-review TB1 on 64cec63. Defect: after a fresh-port swap in restartDetachedOpenCode (port-parity guard, cmd/opencode_start.go:380-396), the restart publishes truthful state but the RUNNING daemon's proxy keeps targeting the old port/URL until daemon reboot — rt.opencodeURL/aggregator target are fixed at boot (cmd/client-daemon-vh.go:110-135, :350; cmd/local-server.go:192; pkg/aggregator/aggregator.go:240-242 — no SetURL seam exists anywhere). Suggested shape (M-sized): restartDetachedOpenCode returns the effective restart port (or a structured restart result); both callers (client-daemon-vh.go, local-server.go) atomically re-target their runtime port/URL BEFORE ocLife.SetReady; add a runtime restart/proxy regression test. Related advisory left on P1-API-002 (NOT this card's scope): residual TOCTOU ms-window portFree→child bind (hardening option: pass listener fd or retry-once on EADDRINUSE); minor scaffold nits. 2026-08-30. DONE. Landed as commit 29c9440 fix(web,cmd): re-target running daemon to fresh restart port (P1-API-003) (11 files: mutator seam — opencode.Client guarded BaseURL()/SetBaseURL() (old exported field had zero external users), aggregator.Retarget, web.Server.RetargetOpenCode (aggMu-guarded s.opencodeURL write + atomic config-preserving whole-proxy swap + walk of all s.aggs incl. default); restartDetachedOpenCode returns (cmd, effectivePort, err); shared applyFreshPortRetarget helper wires BOTH binaries' restart arms before ocLife.SetReady(); rider: archive.go handleReloadProject s.opencodeURL read moved under aggMu). Behavioral crux TestRestartRetargetsRunningServerToFreshPort: running web server's /oc/* proxy serves the NEW upstream's marker after a foreign-squatter fresh-port restart via the production rt.restartOpencode path — outcome-level, race-clean, full go test ./... green, darwin/windows vet clean. Commit-review 4/4 approve ×2 rounds (one fail-closed divergence absorb of disjoint concurrent commits). Gate advisories from 29c9440 review (non-blocking): (1) local-server restart-arm retarget verified structurally, not behaviorally (linux crux drives client-daemon arm only) — add when cmd/local-server.go next touched; (2) linux-only crux — non-Linux restart→retarget e2e unexercised; (3) latent ordering hazard: SetOpenCodeURL flips before RetargetOpenCode parse-refusal could reject (unreachable today, Sprintf URLs parse by construction) — add error-return if a non-Sprintf caller appears; (4) minor comment polish (s.aggs[""] invariant note, loopback assumption). UPDATE 2026-08-31 adjudication: adv-1 (local-server retarget structural-only) RETIRED — stale premise, superseded by 1d4efc4's real-trigger trio; adv-2 (linux-only crux) RETIRED as accepted-by-design (non-Linux cannot prove flock//proc; portable core semantics covered by untagged tests); adv-3 (SetOpenCodeURL-before-RetargetOpenCode parse-refusal ordering) KEEP TRIGGER — unreachable today (Sprintf URLs parse by construction), arm only if a non-Sprintf caller appears; adv-4 comment polish LANDED in 928ed3e (R18, with the corrected aggs[""] invariant). | 64cec63, P1-API-002 |
| P1-API-002 | done | API | Cross-process mutual exclusion for detached-OpenCode spawn (F13). Classify→spawn→writeOCState is unserialized across daemon starts; concurrent starters can split-brain against the project sqlite DB before state-write lands. Requires defensible design (e.g., per-project advisory lock or lease spanning the sequence). |  | 2026-08-29. DONE. Landed as commit `ef8e084` (code, 11 files: two-role flock via ExtraFiles + close-without-unlock, Contended/OrphanedOwner verdicts, shared EnsureDetachedOpenCode operation, serialized restart with pid/cmdline re-verify — both path_touched DEFERs on the card are thereby discharged) and `530d8a0` (durable decision records in researches/decisions/2026-08-28-oc-spawn-serialization-brief.md + 2026-08-28-oc-spawn-lock-experiment.md). Notes (source:gate-advisory): (1) integration test reimplements sha1(cwd) project-key derivation — drift risk if ocProjectKey changes; export/derive via shared helper when next touched; (2) missing //go:build linux tags on Linux-only lock test files; (3) minor: restart doc-comment overstates boot-path parity; stray fake-opencode processes survive local test runs; "2–4 ms" loser framing omits the ~7.5s classify-probe path. Standing path_touched DEFERs (live elsewhere, not discharged here): D2 port≤0 restart guard; A1 restart signaled-flag/owner-holder wait nuance. Discharge D2 + A1 (the two standing path_touched DEFERs marked "live elsewhere"): 2026-08-29, landed as commit a0f3312 (fix(cmd): re-derive port and wait out owner holders in detached restart). D2: port≤0 restart now re-derives port (valid state → st.Port, else freePort()) inside restartDetachedOpenCode under the starter lock. A1: owner-release wait unconditioned on the removed signaled flag (covers curPID=0 boot states; literal card shape was already fixed in ef8e084 — premise correction, reviewer-confirmed). Both discharged. Riders also landed in a0f3312: //go:build linux tags on lock test files (portable matrix test moved to new cmd/opencode_start_apply_test.go); restart doc-comment parity fix; OCProjectKey exported + integration test sha1 reimplementation removed. New gate advisories from a0f3312 review (all non-blocking, disposition=drop): (a) D2 port-parity hardening — re-derived st.Port trusted without the boot path's portFree() guard; recorded-port re-bound by unrelated listener → child can't bind but dial-only waitForPort succeeds and publishes state pointing at foreign listener (bounded, owner-covered, not the old wedge; optional fix: prefer st.Port only when portFree(st.Port) else freePort()); (b) doc nit: restart "retains and reaps" overstates (callers never Wait() the handle); (c) test flake risk: TestRestartDetachedWaitsOutUnsignaledHolder closes owner-lock fd twice (suggest sync.Once); (d) pickLowPort test helper assumes default Linux ip_local_port_range, t.Fatal on 20-port exhaustion. Advisory cleanup 2026-08-29, landed as 64cec63 (code: cmd/opencode_start.go, cmd/opencode_start_test.go, cmd/opencode_lock_test.go, tests/integration/opencode_spawn_lock_test.go) + 1391a35 (docs: dated Amendment to researches/decisions/2026-08-28-oc-spawn-lock-experiment.md). All six open advisories discharged: (a) port-parity guard in restartDetachedOpenCode (portFree re-check on final spawn port after owner-release wait, before startChildWithOwner, covering both D2-re-derived and caller-supplied provenances; foreign-listener-on-recorded-port no longer yields poisoned ready state — fresh-port spawn + truthful published state, pinned by TestRestartDetachedForeignListenerOnRecordedPort/OnSuppliedPort); (b) "retains and reaps" comment corrected (zombie-until-exit deliberate, no reaper added); (c) owner-fd double-close fixed via sync.Once + dead channel removed; (d) pickLowPort reads ip_local_port_range, 2048-window below floor, skip-with-reason; (e) t.Cleanup sweeps kill leftover .fake/.gchild pids in both test scaffolds (post-assertion, LIFO); (f) experiment record amendment: 2–4ms figure harness-accurate only; landed contended loser pays classify probe ~1s/~7.5s/~13s worst before Occupied, never winner's budget. NEW ADVISORY MATERIAL (pending operator decision — NOT a card yet): port propagation to running daemon (fresh-port restart publishes truthful state but rt.opencodeURL/aggregator target stays on old port until daemon reboot; no SetURL seam exists — pkg/aggregator/aggregator.go:240-242; found independently by the advisory study AND commit-review 64cec63 TB1; suggested shape: return effective restart port and atomically re-target callers before SetReady); residual TOCTOU ms-window between portFree probe and child bind (safety net = readiness-failure path; hardening option: pass listener fd or retry-once on EADDRINUSE); minor scaffold notes: sweep lacks /proc cmdline revalidation before SIGKILL, dead .gchild suffix in integration sweep, pickLowPort skip could hide crux skips on port-starved hosts. UPDATE 2026-08-30: the port-propagation advisory was filed as P1-API-003 and is now DONE (commit 29c9440). UPDATE 2026-08-31 detached-boot TOCTOU adjudication: residual = ms-scale portFree→child-bind window requiring a FOREIGN LISTENER to bind the recorded port in that window; detached readiness remains dial-only so a squatter in the window would be served until the next restart (portFree re-check → fresh port) or daemon reboot (classify → fresh port) — both heal paths verified. Exposure judged LOW (bind-race vs the owned arm's active port churn). Do NOT rephrase as "safety net = readiness-failure path" — that phrasing on this card is INCORRECT for the squatter case (the dial succeeds; the failure path never fires). Real attribution would need waitid(WNOWAIT)/pidfd or port-provenance machinery — the latter explicitly rejected by settled P1-API-006 design; documented bound, no card. |  |
| ORPHAN-TAIL-SETTLE | done | WEB | Orphan-tail settle work (real-runtime deferred cluster). |  | 2026-08-22. DONE. Landed via 7b0c31d (stamp seams, Part.tsx guard), 4faad3f (warm-refresh, MessageParts.tsx positional settlement), adfb8de (real-runtime e2e closure /fixture/orphan + F4 timing hardening). Cluster CLOSED by adfb8de evidence. |  |
| P2-WEB-051 | done | WEB | Conditional full-lane re-run + cross-session flake attribution. `scroll-follow.spec.ts` fails independently of the compaction-burst spec (`:608` "composer auto-grow while Live keeps following at the tail", `:665` "deliberate scroll-up reader is not yanked"). Attributed to concurrent session's in-flight dirty files (`web/src/components/chat/MessageParts.tsx`, `web/src/sync/refresh.ts`). Actions: (a) lane-verify `settled()` change against `scroll-follow.spec.ts`; (b) re-run full web e2e lane when those files land or revert. | terminal-docked-default-and-touch-selection | 2026-08-20. DONE. Trigger fired: concurrent session landed as 4faad3f. Full verification at 4faad3f: web unit 2396 passed / 0 failed / 1 skipped (incl. crossStreamCompletion 17/17, orphanTailSettle 11/11); typecheck clean; full web e2e lane 238 passed / 0 failed / 1 pre-existing deliberate skip (scroll-follow.spec.ts 20✓/1 deliberate-skip). Receipt: 2026-08-20T20-34-45-p2web051-verification-complete. Original notes: source:review-defer; trigger:after_landing(terminal-docked-default-and-touch-selection); studied:2026-08-20 (from 6b09082 review, DEFER D4). ref: docs/checkpoints/2026-08-20-terminal-docked-default-and-touch-selection.md |  |

## Cancelled

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P1-WEB-010 | cancelled | WEB | Pin `bottommostReadFromDom`'s scroll-origin fallback in vitest. The mid-history-anchor regression (P1-WEB-009 / 84251be) is currently covered by exactly one serial e2e path (`unread-dot.spec.ts:173`). No isolated unit test pins `bottommostReadFromDom`'s `rows[0]?.id` fallback at the scroll origin (first row sits at relative top=+16 due to `.chat-scroll` `padding:16px`). A future refactor of `bottommostRead`'s threshold (`web/src/lib/scroll.ts`) could regress silently until the serial e2e suite reruns. Add a vitest unit test asserting `bottommostReadFromDom` returns `rows[0].id` when the first row sits at top=+padding (scrollTop=0) and `nearBottom` is false. | web | 2026-07-11. Cancelled — relocated to .local/coordinator/tasks/ as trigger-gated DEFER candidate defer-bottommostreadfromdom-vitest (trigger: path_touched(web/src/lib/scroll.ts), not yet met). Original note: D-F1 from commit-review of 84251be (advisory, dropped on P1-WEB-009). Deferred. Trigger to un-defer: `web/src/lib/scroll.ts` `bottommostRead` threshold touched. Locate the existing scroll unit-test home (likely `web/tests/unit/scroll.test.ts`, modeled on the `bottommostRead` tests referenced in P1-WEB-002) and add the case there. |  |
| P1-WEB-015 | cancelled | WEB | Surface a user-visible error/retry state in the ChatView UI for failed background message hydration. Slice C (P1-AGG-001) added `messages.error` events, but the FE reducer only `console.warn`s them — a failed background fetch shows an indefinite "Loading…" with no operator-visible signal. The daemon self-heals on the next session selection/reconnect, but there is no affordance to see or retry a failed load. Build an explicit error/retry chip when faced with it in practice. | build | 2026-07-11. Cancelled — relocated to .local/coordinator/tasks/ as trigger-gated DEFER candidate defer-chatview-hydration-errorchip (operator-judgment-gated soft trigger — no path_touched). Original note: Deferred follow-up from Slice C: the `messages.error` event reaches the FE reducer as `console.warn` only (`web/src/sync/stream.ts`), while the ChatView empty-state/loading branch (`web/src/components/ChatView.tsx`) renders an indefinite "Loading…" on a failed background fetch — add the error/retry chip there. | P1-AGG-001 |
| P1-WEB-013 | cancelled | WEB | Stuck reasoning timer (`Part.tsx:549` `live()` + `selectors.ts:180`) — cosmetic, self-heals on turn completion. Fix: shared read-side helper `reasoningEnded(part, message) = !part.time?.end && hasSuccessorPart(message, part.id)` mirroring Go's alerts-engine inference (`pkg/alerts/engine.go:255-258`); needs fixture extension (today's fixtures at `pkg/fixtures/opencode.go:75,146` always supply `time.end`). Rejected: upstream per-message-flag (protocol change) and client-side time.end reconciliation (fights part snapshots). |  | 2026-07-11. Cancelled — relocated to .local/coordinator/tasks/ as trigger-gated DEFER candidate defer-reasoning-timer-stuckend (fixture-capability-gated; promoter must confirm a fixture case emitting an in-flight reasoning part WITHOUT time.end exists before re-promoting). Original note: Concept A from the 419ea39 / P1-WEB-011 live-autofollow slice — explicitly out-of-scope there, pre-scoped for deferred pickup. Deferred: do NOT implement until a fixture extension that can emit an in-flight reasoning part WITHOUT `time.end` (so the stuck-timer state is reproducible end-to-end). |  |
| P1-DOCS-001 | cancelled | DOCS | Fix the AGENTS.md restore-placeholder verb so it matches shell-guard (`commit-gate.sh revert`, not `git checkout`). The "Web frontend" section of the generated `AGENTS.md` (sourced from `AGENTS.mission.md`) instructs agents to run `git checkout -- pkg/web/dist/index.html` to restore the placeholder after a local build, but `shell-guard` blocks `git checkout` for non-committer agents and `.opencode/docs/git-execution-routing.md` sanctions `.opencode/scripts/commit-gate.sh revert <paths>` as the in-session restore-to-HEAD path. Correct the mission overlay text so the instruction matches the enforced guard. |  | 2026-07-04 SUPERSEDED by P1-WEB-025 (done 2026-07-03). The current `.vh-agent-harness/AGENTS.mission.md:42-44` already routes agents through `.opencode/scripts/commit-gate.sh revert pkg/web/dist/index.html` (shell-guard-aligned). The old "instructs agents to run `git checkout`" wording this task targeted no longer exists — no edit required. Surfaced while fixing/committing 84251be (P1-WEB-009). `AGENTS.md` is generated, so edit `AGENTS.mission.md` — the line "When a local build dirties `pkg/web/dist/index.html`, `git checkout -- pkg/web/dist/index.html` before committing." — not the rendered file. Verify: grep `AGENTS.md` for the corrected verb and confirm no `git checkout -- pkg/web/dist` instruction remains. |  |
