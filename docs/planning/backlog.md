# Backlog

Canonical task-status source of truth for the vh-solara repo. One row per
task; status drives where it lives. Active work stays in `Now` / `Next` /
`Later`; completed/cancelled work moves to `Done` / `Cancelled` and is archived
under `docs/planning/archive/` by
`.opencode/scripts/normalize-backlog.js`.

Conventions live in [`AGENTS.md`](../../AGENTS.md) → "Backlog tracking rules".
Run `/backlog-cleanup` (or
`vh-agent-harness exec node .opencode/scripts/normalize-backlog.js`) after any
status change that completes, cancels, or reorders work, so the active sections
stay active-only and history is archived.

Row format: `| ID | Status | Area | Task | Owner | Notes | Links |`.
Statuses: `todo`, `in_progress`, `blocked`, `done`, `cancelled`. Put a
machine-readable completion date (`YYYY-MM-DD`) in `Notes` for `done` /
`cancelled` rows so the normalizer can file them into the right archive quarter.

## Area Legend

| Code | Boundary | Notes |
| --- | --- | --- |
| `WEB` | `web/` | SolidJS SPA (Vite + TS), embedded via `//go:embed`. |
| `API` | `pkg/web/`, `pkg/server/` | The `/vh/*` HTTP API + daemon handlers. |
| `TUNNEL` | `pkg/tunnel/` | Controller ↔ worker multiplexed WebSocket tunnel (yamux). |
| `AGG` | `pkg/aggregator/`, `pkg/state/`, `pkg/opencode/` | OpenCode state aggregation + session store. |
| `AUTH` | `pkg/auth/` | Controller auth / worker enrollment. |
| `PWA` | `web/public/`, manifest, service worker | Installable PWA surface. |
| `REL` | `.github/`, `install.sh`, `Dockerfile*`, `Makefile` | Tag-driven release + packaging. |
| `CI` | `.github/workflows/` | GitHub Actions CI pipelines (workflow YAML). |
| `INFRA` | `Dockerfile*` (dev/test), toolchain + browser image | Containerized test/dev environment across all lanes; environment consistency + isolation by default. |
| `DOCS` | `docs/` | Durable repo guidance. |
| `HARNESS` | `.opencode/`, `.vh-agent-harness/` | Agent harness integration. |

## Archive Index

Older `done` and `cancelled` history lives under [docs/planning/archive/index.md](archive/index.md) and is meant for on-demand reading instead of auto-loading into the active backlog context.

- [2026 Q3](archive/backlog-archive-2026-q3.md) - 58 done / 0 cancelled
- [2026 Q2](archive/backlog-archive-2026-q2.md) - 12 done / 0 cancelled
- [Undated](archive/backlog-archive-undated.md) - 1 done / 0 cancelled

## Now

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P1-DOCS-003 | blocked | DOCS | Create `docs/ai/shell-execution.md` (Slice C) — AGENTS.md cites it 13× but it doesn't exist (load-bearing for shell-guard). Min content: `vh-agent-harness exec` golden rule + Forbidden-patterns table (from `.opencode/repo-configs/forbidden-patterns.core.js` + `shell-guard-core.js`) + repo-relative-paths rationale. Do NOT hand-edit generated `.vh-agent-harness/AGENTS.core.md` or `.opencode/*` refs. |  | Deep-review R2 finding. BLOCKED on maintainer sign-off of forbidden-patterns wording. backlog-now (ready once wording approved). |  |

## Next

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P0-API-001 | todo | API | Tighten CSP `script-src` to `'self'` (drop `unsafe-inline` / `eval`) once stable. |  | From TODO at pkg/web/server.go:601. |  |
| P0-API-002 | todo | API | Map opencode session alias / share slug into `shapeSessions` once the pinned opencode version exposes it in session JSON. |  | From TODO at pkg/web/sessions.go:283. |  |
| P1-API-001 | todo | API | Re-add code-fence info-string attribute support (hl_lines / linenos / linenostart / nohl / hl_style) to the custom render NodeRenderer if it ever becomes user-facing; intentionally dropped today. |  | F2 from commit-review of 28d32c68. pkg/render/renderer.go renderFencedCodeBlock treats the info string as a language name only; the compact chroma-routing renderer replaced stock goldmark-highlighting/v2. The dropped attributes are documentation-style and not authored in OpenCode chat content. |  |

## Later

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P1-INFRA-001 | todo | INFRA | Dockerize the entire test/dev environment (all lanes, not just `make e2e`): SPA build, Go toolchain, Playwright browsers, fixture server, and the e2e `webServer` lane. |  | Operator-requested future intent (captured only; not started). Reason: environment consistency + isolation by default — today only the e2e lane is containerized via `scripts/fixture-web.sh`, while the Go toolchain, node, and browser binaries are host-dependent, causing cross-machine divergence. |  |
| P1-WEB-042 | todo | WEB | No DOM/component-level regression test for the scrollEl ResizeObserver re-engage branch (the 2b "stuck-on-Latest" fix). The scrollEl ResizeObserver `else if (nearBottom())` re-engage branch in `web/src/components/ChatView.tsx` (a pure clientHeight grow fires no scroll event, so `onScrolled` cannot re-engage `following`) is exercised only by manual/Playwright, not by any component/unit test. Add a DOM-level/component regression that simulates a composer-shrink (clientHeight grow on `.chat-scroll`) while not following and at bottom, asserting `following` re-engages without a scroll event. Constraints: reuse the existing contentEl + scrollEl ResizeObservers — do NOT add a third RO on the scroll area; no mask-image/backdrop-filter/per-element content-visibility. |  | Promoted from .local/coordinator/tasks/defer-scroll-ro-reengage-dom-test (DoR met 2026-07-11). source:review-defer. trigger:path_touched(web/src/components/ChatView.tsx) FIRED. studied:2026-07-08. commit-reviewer F5 finding on ca9c5a064aa7a138162af2896ae7c143cc2f6e66 (non-blocking, captured at closeout of the dual-axis scroll reducer slice). Files: web/src/components/ChatView.tsx + new test spec (created when implemented). Validation: new vitest or Playwright spec covering composer-shrink-at-tail re-engage + `cd web && npx vitest run` + `npm run typecheck`. |  |
| P1-WEB-019 | todo | WEB | ReasoningPart stick-to-bottom has no resume re-pin guard — INVESTIGATED, IMMUNE (no fix required). There is no `ReasoningPart.tsx` file; it is a function in web/src/components/Part.tsx:572-641 with its own ResizeObserver on `.reasoning-body`. Unlike ChatView it uses a live `stick` boolean (Part.tsx:603-607) with NO stored `pinnedTop`/`pinnedScrollHeight` snapshot and NO intent latch, so the coalesced RO delivered on tab resume unconditionally re-pins when `stick===true`. Only a narrow theoretical edge (hidden settle-shrink + async server-HTML regrow + scroll-before-RO event ordering) could leave the bounded ≤320px "Thinking" panel slightly off-bottom, and it self-corrects on the next user scroll / part de-tail. Logged only to preserve the investigation so it is not re-opened. |  | Read-only study of follow-up A from commit 90f6ef12. Verdict: not a bug (immune). Do NOT implement unless an operator reports it. |  |

## Done

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| FIX-QUEUE-GC-5 | done | API | Automatic bounded compaction of terminal queue items — bounds disk growth from terminal items (especially `unknown` items that now routinely appear after STUCK-1's recovery). Purges `sent`/`failed`/`unknown` items based on per-status TTL (by `ResolvedAt`, NEVER `CreatedAt`) and per-status count cap. Retention values (code constants): sent=1h/50, failed=7d/100, unknown=30d/200 (retention order preserves ambiguous-recovery evidence longest). NEVER purges `pending` or `dispatching`. Missing/malformed `ResolvedAt` (zero value) is conservative — survives the TTL pass (treated as fresh); only the count cap can remove such items, ordered by `Order` ascending as fallback. Compacted-to-empty queue deletes `queue.json` (matches the lazy-creation pattern). Trigger points: `List()` (after STUCK-1 recovery) and `Resolve()` (after the terminal transition); NO dedicated goroutine/ticker. Atomic persistence via existing `save()`; on save failure rolls back ALL in-memory mutations (covers both recovery and compaction in `List()`; covers terminal mutation + compaction in `Resolve()`). Slice 5 of 5 (closes the GC family: GC-1 cleanup primitive, GC-2 session.delete subscriber, GC-3 orphan reconciliation, GC-4 manual dismissal, GC-5 auto-compaction). | build | 2026-07-20. DONE. Commit `48c4932156` (code only, backlog in this separate commit per W1). Files: `pkg/web/queue.go` (`sort` import; retention constants sentItemTTL/Cap, failedItemTTL/Cap, unknownItemTTL/Cap; test-hook atomic overrides + currentSentTTL/currentFailedTTL/currentUnknownTTL readers + `SetCompactionTTLsForTest(sent,failed,unknown time.Duration)` mirroring STUCK-3's `SetStaleDispatchThresholdForTest` pattern — per-arg restorable, ns storage; `compactTerminalItemsLocked(now time.Time) (changed bool)` under s.mu — partitions by status into sent/failed/unknown, TWO passes: TTL pass marks items where `nowMs-ResolvedAt > ttlMs` STRICT (age==TTL survives; ResolvedAt<=0 conservative survives; future ResolvedAt survives), count-cap pass sorts survivors by (effective-key asc, Order asc) where effective-key=ResolvedAt if >0 else nowMs (conservative: missing treated as fresh so cap removes LAST), rebuilds s.items skipping marked; `persistAfterCompaction(compactChanged bool) error` — if compactChanged && len==0, os.Remove(s.path) (IsNotExist=nil idempotent), else falls through to save(), with defense-in-depth `s.archived` guard on the remove branch returning errQueueArchived mirroring save()'s last-resort guard; List() wiring — preMutation snapshot BEFORE recovery+compaction, recoverStaleDispatchingLocked then compactTerminalItemsLocked, persistAfterCompaction if either changed, on error `s.items = preMutation` rolls back BOTH; List() `s.archived` tombstone guard added at entry (b-F1 fix — List() is now a mutating op and needs the same guard as Enqueue/Remove/Claim/Resolve: without it a retained archived-store pointer whose compaction empties its stale items would os.Remove a queue.json that a fresh post-archive store at the same path had written — different mutexes, not serialized, silent data loss); Resolve() wiring — preSnapshot snapshot BEFORE terminal mutation, apply State/Detail/ResolvedAt, capture resolved BEFORE compaction may shrink slice, compactTerminalItemsLocked, persistAfterCompaction, on error `s.items = preSnapshot` rolls back terminal mutation + compaction, return captured resolved), `pkg/web/queue_compact_test.go` (NEW — 12 cases: TTLBoundary strict `>` boundary, CountCapUnderBurst cap+10 sent trimmed to cap, CombinedTTLAndCap both rules applied, DeterministicOldestFirst cap+1 unknown oldest trimmed, StatusSpecificLimits sent+unknown at 2h sent purged unknown survives, UsesResolvedAtNotCreatedAt old CreatedAt+recent ResolvedAt survives, MissingResolvedAtConservative 2 sub-tests survivesTTLPass + removedByCountCapByOrder, NeverTouchesPendingOrDispatching, UnknownSurvivesLongestAndHighestVolume 2 sub-tests TTL_ordering_age_2h + cap_ordering_high_volume 250 each, RollsBackOnSaveFailure parent-dir-blocked List fails 3 items still in memory, EmptyQueueDeletesFile all expired queue.json deleted fresh store loads empty, ListArchivedGuardNoFreshFileDeletion b-F1 regression — archive + fresh store Enqueue + retained-pointer List returns errQueueArchived + fresh queue.json intact; PROVEN the test catches b-F1 by disabling the guard → test fails). Verified: `go build ./pkg/web/` OK; `go vet ./pkg/web/` clean; `gofmt -l` clean; `go test ./pkg/web/ -run TestQueueCompact -v -count=1` 12/12 PASS (0.695s); `go test ./pkg/web/ -run TestQueue -count=1` OK 35.5s (no regressions in 8 prior slices incl. archive/rollback paths); `go test ./pkg/web/ ./pkg/aggregator/ ./pkg/state/ -count=1` ALL OK; `go test ./pkg/web/ -race -run TestQueue -count=1` OK 38.7s (race-free under mutex); `go test ./tests/e2e/ -count=1` OK 0.94s (STUCK-3 recovery + compaction coexist). Safety grep: `git diff pkg/web/queue.go \| grep QueuePending \| QueueDispatching` in compaction code → only ONE match in EXISTING recoverStaleDispatchingLocked recovery guard (unchanged STUCK-1 code); zero matches in compactTerminalItemsLocked (configs map has ONLY terminal-state keys; both passes guard `it.State == status` where status iterates only over terminal configs — NO code path can mark pending/dispatching for removal). commit-reviewer: first pass BLOCKED on b-F1 (List() missing archived guard — data_integrity); fix applied (List() entry guard + persistAfterCompaction remove-branch defense-in-depth + regression test proven to catch the bug); re-review APPROVED 4/4 tier-1 leaves high-confidence low-risk zero BLOCK/DEFER; 2 DROP advisories (doc-drift 11→12 cases header; symmetry nil→errQueueArchived) BOTH addressed. Non-goals (deferred): operator-configurable retention via `.vh-solara/project.jsonc` (values are code constants for now); GC-3 reconciliation calling compaction on active queues; attachment GC. |  |
| FIX-QUEUE-GC-4 | done | API | Operator-initiated dismissal of terminal queue items (`sent`/`failed`/`unknown`) that today accumulate forever. The lifecycle docstring promise (queue.go:19-21) that terminal items "persist until explicit operator dismissal" is now fulfilled — `Remove()` accepts terminal states; `dispatching` stays non-removable (active-dispatch safety guard — the state machine must own its terminal transition first). No auto-dismissal (GC-5 deferred). Slice 4 of 5. | build | 2026-07-20. DONE. Commit `5636a4d` (code only, backlog in this separate commit per W1). Files: `pkg/web/queue.go` (Remove() guard flipped from `it.State != QueuePending` to `it.State == QueueDispatching` — pending (cancel) + terminal sent/failed/unknown (dismiss) now removable; error message updated from "only pending items are removable" to "dispatching items cannot be removed while in flight"; Remove() docstring rewritten. Atomic survivor persistence (temp-file + fsync + rename via save()) + save-failure rollback untouched — only the guard condition changed), `pkg/web/queue_test.go` (renamed `TestQueueRemoveOnlyPending` → `TestQueueRemoveRejectsDispatchingAcceptsPending` to reflect the still-true invariant; NEW `TestQueueRemoveAcceptsTerminalStates` table-driven across sent/failed/unknown with fresh-store reload verification of atomic survivor persistence; NEW `TestQueueRemoveIsIdempotentAfterRemoval` — second Remove returns errQueueNotFound, not silent OK; HTTP round-trip test updated — old test DELETEd the failed item and asserted 409 which broke under the new contract, now DELETEs claim2 (dispatching → 409 safety guard) and the resolved-to-failed claim (→ 200 terminal dismissal)), `pkg/web/queue_http.go` (doc-sync only — route table comment + handleQueueRemove docstring updated from "remove (pending only)" to "remove (pending + terminal; not dispatching)"; zero code changes, csrfGuard + Remove() delegation unchanged), `web/src/components/QueueChip.tsx` (dismiss (x) button `Show` guard extended from `state === "pending"` to also include `"failed"` and `"unknown"`; same button + aria-label for all dismissable states — operator intent "clear this chip" is uniform across states, no cancel/dismiss label distinction; never shown for dispatching; component docstring updated), `web/tests/unit/QueueChip.test.tsx` (rewrote the "no resend/retry button for terminal items" describe block — it asserted terminal items have NO remove button, directly contradicting the new behavior — into "dismiss button visibility + click handler": visibility test (pending/failed/unknown HAVE button, dispatching does NOT), 3 click-handler tests (pending/failed/unknown each call onRemove with correct id); preserved the "no resend/retry text" assertion; file header updated to mention GC-4 coverage), `web/src/queue.ts` (doc-sync only — `removeQueued` function comment + 409-branch comment updated from "deletes a PENDING item / non-pending removal" to the new contract; zero code/type changes, 409 branch behavior unchanged). UX labeling choice: SAME button + label ("Remove queued message") for pending and terminal — no cancel/dismiss distinction (matched existing convention, surfaced in closeout). Verified: `go test ./pkg/web/ -count=1` ok 90s (no regressions incl. FIX-SNAPSHOT-LEAK / FIX-QUEUE-STUCK-1 / FIX-QUEUE-GC-1/2); `gofmt -l` clean on all 3 Go files; `npm run test:unit` 70 files 776 tests PASS (was 774; net +2 from QueueChip test rewrite: −2 old + 4 new); `npm run typecheck` clean. commit-reviewer APPROVE (high/low) — all 4 tier-1 leaves, zero BLOCK, zero DEFER, all 6 findings DROP/advisory (F3 out-of-slice ChatView.tsx:2407-2410 comment drift captured as conditional candidate `source:review-defer,trigger:path_touched(web/src/components/ChatView.tsx)` in `.local/coordinator/tasks/`). Slice 5 = automatic compaction / TTL / count-cap (GC-5, deferred). |  |
| FIX-QUEUE-GC-3 | done | API | Authoritative orphan queue-file reconciliation as the durable backstop for GC-2's best-effort `session.delete` event subscriber. GC-2 fires `CleanupSession` inline as `session.delete` events stream in, but `store.emit()`'s fan-out is nonblocking and drops events on a full subscriber buffer — so an event can be lost, leaving an orphan `queue.json` on disk whose session ID is no longer in the authoritative active set. GC-3 scans the FILESYSTEM (NOT the loaded `queueRegistry.stores` map — orphans by definition aren't loaded) after every successful hydrate and removes every `queue.json` whose session ID is NOT in `store.SessionIDs()`. **FAIL-CLOSED contract** (the single most important rule): a nil active set (hydrate failed / store unavailable / any uncertainty) OR a not-yet-hydrated aggregator deletes NOTHING. An empty NON-NIL active set (hydrate succeeded with zero sessions) is the OPPOSITE case — every on-disk queue is correctly treated as an orphan and deleted. Distinguishing these two is the entire reason the aggregator now exposes a `HydratedOnce` signal. Slice 3 of 5. | build | 2026-07-20. DONE. Commit `5893c4a` (code only, backlog in this separate commit per W1). Files: `pkg/aggregator/aggregator.go` (+`sync/atomic` import; +2 struct fields after `seedDone` — `onHydrate func()` seedMu-guarded fired at end of every successful hydrate, `hydratedOnce atomic.Bool` sticky set true at end of first successful hydrate NEVER reset — records "this aggregator has produced at least one authoritative session set"; +2 methods `SetOnHydrate(fn)` seedMu-guarded setter + `HydratedOnce() bool` lock-free atomic load, safe on the request path; +fire-site at end of `hydrate()` before `return nil` — `hydratedOnce.Store(true)` OUTSIDE seedMu (callback dispatch must not hold the lock) then reads `cb := a.onHydrate` under seedMu, releases seedMu, invokes `if cb != nil { cb() }` outside any lock. ALL error paths in hydrate return BEFORE this fire-site, so a failed/partial hydrate leaves hydratedOnce=false and fires nothing — fail-closed), `pkg/web/queue.go` (+`reconcileOrphanQueues(root, activeSessions map[string]bool) error` method on `*queueRegistry` after `CleanupSession`. FAIL-CLOSED: `if activeSessions == nil { return nil }` deletes nothing; empty non-nil map falls through and deletes all. Scans `os.ReadDir(<root>/.vh-solara/sessions)`; IsNotExist on the dir → return nil; for each dir entry: skip non-dirs, skip if `activeSessions[sid]`, stat `queuePath(root, sid)` — IsNotExist skips attachment-only dirs, other stat errors logged via `vhlog.Warn` and skipped (non-fatal so one bad entry doesn't block the rest), otherwise call `qr.CleanupSession(root, sid)`. Idempotent + race-safe via qr.mu serialization in deleteStore. NEVER `os.RemoveAll` — empty-only rmdir via CleanupSession), `pkg/web/server.go` (+`reconcileQueuesForAgg(dir, a)` method — the per-aggregator driver. FAIL-CLOSED gate: `if !a.HydratedOnce() { return }` before any work. Then `projectRoot(dir)` (err → `vhlog.Error` + return — effectively never fails), `a.Store().SessionIDs()` → build `active := make(map[string]bool, len(ids))` → `s.queues.reconcileOrphanQueues(root, active)` (err → `vhlog.Error`). Wired inside `installQueueGCCleanup` right after `s.queueGCOn[dir]=true`+unlock — `a.SetOnHydrate(func() { go s.reconcileQueuesForAgg(dir, a) })` shares the queueGCOn guard lifecycle (installed once per (dir,aggregator), reset on reload since `handleReloadProject` resets `queueGCOn[dir]`); +immediate-run branch `if a.HydratedOnce() { go s.reconcileQueuesForAgg(dir, a) }` covers the default aggregator which is started+hydrated by the daemon BEFORE first HTTP request reaches `aggFor("")` — without it the default dir's orphans would only be cleaned after the NEXT hydrate. NO separate guard map needed), `pkg/web/queue_reconcile_test.go` (NEW — 8 `TestQueueReconcile_*` cases: (1) orphan removed + live survives with active={live}; (2) live-session queue items preserved — Enqueue then read back via fresh `store().List()`; (3) **nil inventory removes nothing** — THE fail-closed test; (4a) empty NON-nil inventory deletes all — the empty-vs-failed distinguisher; (4b) not-yet-hydrated aggregator via `reconcileQueuesForAgg("",agg)` deletes nothing — `queueLifecycleServer` does NOT start Run so HydratedOnce=false, pins the boot-race gate; (5) filesystem-only orphans discovered via `os.ReadDir` (asserts `len(srv.queues.stores)==0` before reconcile); (6) attachment-bearing orphan loses ONLY queue.json — `attachments/resume.pdf` survives, session dir survives non-empty rmdir; (7) concurrent Claim/Resolve/Enqueue loop vs 50 reconciliation passes — race-safe, run under `-race`; (8) 8 concurrent reconcilers racing to remove the same files — disappearing-files tolerated, no error. Reuses `seedQueueFile`/`queueFileExists`/`queueLifecycleServer`/`ev` helpers). Verified: `go build ./...` clean; `go test ./pkg/web/ -run TestQueueReconcile -v -count=1` all 8 PASS; `go test ./pkg/web/ ./pkg/aggregator/ ./pkg/state/ -count=1` all PASS (no regressions in the 7 prior slices); `go test ./pkg/web/ -race -run "TestQueueReconcile \| TestQueueGC" -count=1 -v` all 16 PASS, race-clean for GC-3 code; `gofmt -l` clean on all 4 files; `go vet ./pkg/web/ ./pkg/aggregator/` clean; safety invariant `git diff \| grep os.RemoveAll` = ZERO actual calls. commit-reviewer APPROVE (4/4 tier-1 unanimous, high confidence, low risk, zero BLOCK, zero DEFER, 3 advisory DROP — F1 hydratedOnce field-doc precision reword applied pre-commit ["atomic because hydrate writes it OUTSIDE seedMu…"], F2 SetOnHydrate+immediate-run double-fire harmless idempotent, F3 test-7 mutator stop-timing advisory). **Phase-1 trigger decision (locked)**: Option 2 (narrow aggregator callback) chosen over Options 1/3/4 — Options 1 (no existing post-hydrate hook in aggregator.go), 3 (duplicates hydrate work / changes ownership), 4 (pure periodic sweep — can't distinguish empty-vs-unhydrated without the very signal being avoided) cannot satisfy fail-closed correctness. Aggregator change ~15 lines. Pre-existing -race failures (TestReloadProject_NonDefault on aggregator.go:225 Stop vs :240 RunManaged a.cancel field; TestCloseoutProjectIsolation/TestSessionMessagesNoColdFetchRegression/TestEnsureMessagesRejectsForeignSession on integration_test.go:117 fakeOpenCode.handler map) confirmed OUT OF SCOPE — none touch GC-3 symbols; this slice does not modify Stop/RunManaged/cancel or the fakeOpenCode fixture. F8 review finding (Store.SessionIDs doc/impl drift — doc claims archived-inclusive, impl is live-only which GC-3 depends on) captured as trigger-gated DEFER candidate `defer-store-sessionids-doc-impl-drift` in `.local/coordinator/tasks/`. Slice 4 = terminal-item dismissal (GC-4, done); Slice 5 = automatic compaction/TTL/count caps (GC-5, deferred). |  |
| FIX-QUEUE-GC-2 | done | API | Web-layer subscriber for normalized `session.delete`; route through GC-1 cleanup primitive; retain direct `/vh/archive` cleanup; idempotent direct+event; no state-layer coupling. Slice 2 of 5. | build | 2026-07-20. DONE. Commit `77e44feb` (code only, backlog in this separate commit per W1). Files: `pkg/web/queue.go` (`CleanupSession` public web-owned wrapper around `deleteStore` — the single cleanup helper both `/vh/archive` and the subscriber call), `pkg/web/server.go` (`queueGCMu`+`queueGCOn` Server fields; `queueGCSubscribeBuffer=128` const; `installQueueGCCleanup(dir, a)` method installed from `aggFor` default+per-dir branches — subscribes via `store.SubscribeWith(buf, state.Interest{MessageSessions: map[string]bool{}})` so ALL message-class events are dropped at fanout, only structural/session.* / activity / status / permission / question / notice / unread reach the channel; goroutine filters `KindSessionDelete`, extracts `{"id":<sid>}`, sanitizes via `safeID`, calls `CleanupSession`; idempotent per-dir via `queueGCOn` map mirroring `ensurePermissionWatcher`; `handleReloadProject` resets `queueGCOn[dir]` on non-default aggregator teardown so next `aggFor(dir)` rebuilds the subscriber on the fresh store), `pkg/web/archive.go` (routed `/vh/archive` direct call through `CleanupSession` for naming consistency; DIRECT CALL RETAINED — archive correctness must NOT depend on best-effort subscriber delivery; the two calls compose idempotently), `pkg/web/verbs_test.go` (`fakeOC` extended with `archiveStatus` field + PATCH `/session/:id` handler case for failed-archive simulation), `pkg/web/queue_lifecycle_test.go` (NEW — 7 `TestQueueGC_*` cases: raw session.delete, RemoveSessions archive-equivalent, direct `/vh/archive` synchronous, direct+event idempotency, failed-archive no-premature-delete, subscriber-does-not-block-store, hydrate prune). Verified: `go test ./pkg/web/... ./pkg/state/... ./pkg/aggregator/...` all PASS (pkg/web ~45s, no regressions in FIX-SNAPSHOT-LEAK / FIX-QUEUE-STUCK-1 / FIX-QUEUE-GC-1); `gofmt -l` clean on all 5 files; `go vet ./pkg/web/...` clean. NO `pkg/state` changes — grep confirms zero new `pkg/state → pkg/web` imports (the state layer stays generic; the web layer subscribes). Subscription model is channel-based + ASYNC (`emit()` nonblocking fan-out under `s.mu`; consumer goroutine does `os.Remove` OUTSIDE the store lock — no buffered-channel/worker indirection needed; `Interest{MessageSessions: map[string]bool{}}` drops all message-class events at fanout to minimize buffer pressure). Lifecycle: goroutine exits naturally when `aggregator.Stop()` → `store.Close()` closes its channel; no leak, no explicit unsubscribe. commit-reviewer APPROVE (high/low) — all 4 tier-1 leaves, zero BLOCK, zero DEFER, all findings DROP/advisory (F1 per-dir reload-rearm test gap mirrors the same gap in `ensurePermissionWatcher` — repo precedent; F2 `queueGCOn[dir]=true` set before `projectRoot` validation is documented intentional best-effort; F3 `time.Sleep(50ms)` in failed-archive test was upgraded to an inverted polling loop before commit). Advisory follow-ups captured for a future coverage slice: `TestQueueGC_ReloadProjectReinstallsSubscriber`. Slice 3 = orphan reconciliation / periodic sweep (GC-3); Slice 4 = terminal-item dismissal (GC-4); Slice 5 = automatic compaction/TTL/count caps (GC-5). |  |
| FIX-QUEUE-GC-1 | done | API | Queue cleanup primitive: generalize deleteStore into idempotent web-owned helper; empty-only rmdir after queue.json removal; never RemoveAll. Foundation for GC-2/3/4/5. Co-located queue_test.go. Slice 1 of 5. | build | 2026-07-20. DONE. Commit `e5781232` (code only, backlog in this separate commit per W1). Files: `pkg/web/queue.go` (deleteStore now computes `dir := filepath.Dir(path)` and after `os.Remove(path)` swallows `_ = os.Remove(dir)` for empty-only parent-dir cleanup in BOTH the no-store and store-present branches; doc comments updated to reflect the new cleanup contract — idempotent, empty-only rmdir, NEVER os.RemoveAll, filesystem cleanup runs without a registered store), `pkg/web/queue_test.go` (10 new TestQueueCleanup* cases pinning the full contract: tombstone+registry-sync, queue-file removal, idempotent-no-file, empty-parent-dir removal, attachments/ preservation, atomic-write temp preservation, idempotent-no-dir, retained-pointer errQueueArchived via save() under st.mu matching seedDispatchingItem L1032 precedent, repeated-calls idempotency, no-store branch removes on-disk orphan + empty dir). Verified: `go test ./pkg/web/... -count=1` ok 46s; `go test ./pkg/web/ -run TestQueue -v -count=1` all 42 pass (32 prior + 10 new); `go test ./...` all packages ok (no regressions incl. FIX-SNAPSHOT-LEAK + FIX-QUEUE-STUCK-1); `gofmt -l` clean; `go vet ./pkg/web/...` clean. Safety invariant: `git diff \| grep os.RemoveAll(` = ZERO actual calls (4 comment-only mentions asserting NEVER). commit-reviewer APPROVE (high/low) — all 4 tier-1 leaves, zero BLOCK findings, all findings DROP/advisory; F3 lock-hygiene (test calling save() without st.mu) fixed before commit. Tombstoning (`st.archived = true` before `delete(qr.stores, key)`) + registry sync preserved; qr.mu held across both os.Remove calls; BLK-1 retained-pointer resurrection class definitively closed. Slice 2 = archive.go call-site + session.delete event subscription; Slice 3 = orphan reconciliation sweep; Slice 4 = terminal-item dismissal; Slice 5 = automatic compaction/TTL/count caps. |  |
| FIX-QUEUE-STUCK-1 | done | API | State-machine repair: queue items stuck forever in `dispatching` after dispatch interrupted by network failure, browser crash, or vh-solara restart (operator's original bug: `q-489f7d3475b28f70` "any defer" stuck in session `ses_092f23ff5ffea035yWYfAVKBWc`). Fix: add `DispatchStartedAt` field to `QueueItem` (omitempty for legacy on-disk compat); `Claim()` stamps it atomically with the pending→dispatching transition (rollback restores both on save failure); `recoverStaleDispatchingLocked(now)` recovers stale (DispatchStartedAt older than 30s threshold, which exceeds the 12s frontend dispatch timeout) AND legacy (DispatchStartedAt==0, the restart-recovery case) dispatching items to terminal `unknown` on every `List()` load with operator-facing diagnostic Detail; `List()` persists recovery atomically and rolls back ALL in-memory mutations on save failure. Recovery NEVER produces `pending`, NEVER re-dispatches, NEVER re-issues the prompt POST (ambiguous-receipt safety: after transport failure it is impossible to know whether OpenCode received the POST, so resend may duplicate work). Slice 1 of 3. | build | 2026-07-20. DONE. Files: `pkg/web/queue.go` (DispatchStartedAt field + Claim atomic stamp/rollback + staleDispatchThreshold + staleDispatchRecoveryDetail + recoverStaleDispatchingLocked + List recovery chokepoint w/ preRecovery snapshot rollback), `pkg/web/queue_test.go` (11 new recovery cases + seedDispatchingItem helper + time import). Verified: `go test ./pkg/web/ -count=1` ok 44s (no regressions incl. FIX-SNAPSHOT-LEAK tests); `go test ./pkg/web/ -run TestQueue -v` all 32 pass (21 existing + 11 new); `gofmt -l pkg/web/queue.go pkg/web/queue_test.go` clean; `go vet ./pkg/web/` clean. Safety-rule confirmed by code inspection: recoverStaleDispatchingLocked only assigns QueueUnknown (queue.go:266), never QueuePending; no HTTP/Claim/dispatch in helper or List. Slice 2 = queue_http.go + ChatView.tsx (NOT touched); Slice 3 = fixture/e2e (NOT touched). |  |
| FIX-QUEUE-STUCK-2 | done | WEB | Surface the backend recovery `Detail` (set by STUCK-1 `recoverStaleDispatchingLocked`) visibly on recovered terminal `unknown` queue items in the SolidJS SPA. The backend already recovers abandoned `dispatching` items to terminal `unknown` on `List()` load with `Detail` = `staleDispatchRecoveryDetail` (the duplicate-risk warning); the SPA previously buried it in a `data-tip` tooltip only. Slice 2 of 3. | build | 2026-07-20. DONE. Commit `a20b75d` (code only, backlog in this separate commit per W1). Files: `web/src/components/QueueChip.tsx` (NEW — extracted the inline queue-chip rendering from ChatView into a small component so the terminal-state detail surfacing is unit-testable in isolation; renders the backend Detail visibly as a full-width warn-colored `.queue-detail-note` below the chip pill for `unknown` items carrying detail; NO resend/retry button for any terminal item — recovery = operator composes a NEW message, never reviving the recovered one), `web/src/components/ChatView.tsx` (replaced the inline `<For>` queue-chip body with a `<QueueChip>` component call; behavior for pending/dispatching/failed identical — same tip/label logic, remove button on pending only), `web/src/styles/legacy/70-composer-diff-git.css` (`.queue-detail-note` rule extending the existing `.queue-chip` block — flex-basis:100%, `var(--warn)`, 11.5px; `--warn` defined on :root and inherited/overridden by every shipped theme), `web/tests/unit/QueueChip.test.tsx` (NEW — 6 tests: unknown+detail renders visibly, unknown-without-detail graceful, failed/dispatching show NO recovery note, remove button exists ONLY for pending — never terminal — and no "resend"/"retry" text anywhere, pending remove calls onRemove with item id). Verified: `npm run typecheck` PASS (clean); `npm run test:unit` PASS (70 files, 774 tests incl. 6 new). NO backend changes (`Detail` already exposed via `json:"detail,omitempty"`; `web/src/queue.ts` already had `detail?: string`; `queueDrain.ts` untouched). commit-reviewer APPROVE (high/low) — all 4 tier-1 leaves, zero BLOCK, three advisory DROP findings (unused sessionId prop kept for semantic clarity; misleading test comment fixed pre-commit). Slice 3 = fixture/e2e (Playwright) separate. |  |
| FIX-QUEUE-STUCK-3 | done | API | In-process e2e validation of the STUCK-1 recovery contract: prove end-to-end through the real HTTP stack (tests/e2e.StartCluster — real controller daemon + real worker over yamux + fake OpenCode, no docker/LLM) that when a queue dispatch is committed by OpenCode but the HTTP response is lost, the item recovers to terminal `unknown` on the next List() after the stale threshold, with NO redispatch (exactly one committed user message). Closes the 3-slice STUCK family (1=state machine, 2=SPA detail, 3=e2e proof). Test-only; the only production-code touch is making the 30s stale threshold overridable for tests. Slice 3 of 3. | build | 2026-07-20. DONE. Commit `0184afa` (code only, backlog in this separate commit per W1). Files: `pkg/fixtures/opencode.go` (NEW `PromptAsyncMode` type + 3 constants Normal/CommitThenDropResponse/RejectBeforeCommit; struct field + methods `SetPromptAsyncMode`/`PromptAsyncModeNow`/`UserMessageCount`/`commitUserMessage`/`dropResponse`, all mutex-guarded; mode-aware `prompt_async` handler — CommitThenDropResponse persists the user message via `commitUserMessage` THEN hijacks+closes the conn so the worker reverse proxy surfaces a backend EOF/not-204, modeling the ambiguous-receipt window; default case is the existing faithful `go simulatePrompt`+204), `pkg/web/queue.go` (TEST-ONLY threshold hook: added `sync/atomic` import; kept `const staleDispatchThreshold = 30s` UNCHANGED; added `var staleDispatchThresholdOverride atomic.Int64` (ms, zero=use const) + `currentStaleThreshold()` + exported `SetStaleDispatchThresholdForTest(d)` (d<=0 restores default); single read site in `recoverStaleDispatchingLocked` switched to `currentStaleThreshold()` — production semantics identical when override==0; atomic chosen over plain var+setter for race-safety under -race, over env var to avoid production footgun), `tests/e2e/queue_recovery_test.go` (NEW — `TestQueueDispatchCommittedThenResponseLostRecoversUnknown` PRIMARY: asserts recovery to `unknown` NOT pending/sent/dispatching, recovery detail text present, AND `UserMessageCount==1` BOTH right after dispatch (proves commit-before-drop) AND after recovery (proves no redispatch); `TestQueueDispatchNormalModeCommitsAndKeepsDispatching` guard: Normal mode returns 204 and item stays dispatching before threshold). CSRF note: csrfGuard (server.go:938-948) guards `/oc/*` POSTs too, so the test sets `X-VH-CSRF:1` on ALL POSTs including the dispatch (spec assumption #5 said /oc/* was CSRF-exempt — WRONG; code authoritative). Verified: `go test ./tests/e2e/ -run TestQueue -v -count=1` PASS (both tests); `go test ./tests/e2e/ -count=1` PASS (no e2e regressions); `go test ./pkg/web/ -count=1` PASS; `go test ./tests/e2e/ -run TestQueue -race -count=1` PASS (atomic hook race-free); `gofmt -l` clean on all 3 files. commit-reviewer APPROVE (high/low) twice (build pre-review + committer gate review), zero BLOCK/DEFER, all findings advisory DROP. |  |
| FIX-SNAPSHOT-LEAK | done | API/AGG | Cross-project message leak via 3 handlers (`handleSnapshot`, `handleStream`, `handleSessionsCloseout`) — user-supplied session IDs passed to `EnsureMessages`/`Messages` without project-store membership check; OpenCode `/session/<id>/message` is project-blind so a request from project B with a session ID owned by project A fetches+caches project A's messages into project B's store. All pins verified at HEAD c3c07df. Fix (three layers): (1) `Store.HasSession` predicate (O(1) RLock+map lookup) + `Server.projectScopedFilter` helper at the HTTP boundary for `handleSnapshot`/`handleStream` (silent-drop policy: HTTP 200, foreign ID absent from response, no 400); (2) inline `ShouldServeSession` guard in `handleSessionsCloseout`; (3) defense-in-depth backstop in `EnsureMessages`/`EnsureMessagesAsync` gated on a new `armed` flag. `armed` is set synchronously via new `Aggregator.Arm()` called inside `aggFor` BEFORE the aggregator is published to `s.aggs[dir]` (closes first-request TOCTOU window surfaced by commit-review tier1_b-F1) and redundantly inside `Run()`; bare `New()`/`NewForDirectory` without aggFor/Run stay unarmed to preserve the bare-test contract (`aggregator_test.go:393,446,557` call EnsureMessages/EnsureMessagesAsync on unseeded sessions). Tests: new `pkg/web/snapshot_isolation_test.go` (6 tests: snapshot/stream/closeout isolation + session-messages no-cold-fetch regression + EnsureMessages foreign-session rejection + TestCloseoutFirstRequestNoRace first-request-race regression). | build | 2026-07-20. DONE. Commit `927a4c8671bdbcda1eb6c84bd1487faa381a597a` (code only, backlog in this separate commit per W1). Files: `pkg/state/store.go` (HasSession), `pkg/web/server.go` (projectScopedFilter + wiring in handleSnapshot/handleStream + aggFor's Arm call), `pkg/web/sessions.go` (ShouldServeSession guard), `pkg/aggregator/aggregator.go` (armed field + Arm + ShouldServeSession methods + backstops in EnsureMessages/EnsureMessagesAsync), `pkg/web/snapshot_isolation_test.go` (NEW, 6 tests). Commit-review: APPROVE (unanimous 4/4 tier-1 leaves, high confidence, moderate risk; prior BLOCK tier1_b-F1 resolved via synchronous Arm() in aggFor). Verified: `go build ./...` clean, `go test ./pkg/web/ ./pkg/aggregator/ ./pkg/state/` PASS, 6 isolation tests PASS, `gofmt -l` clean, `go vet` clean. |  |
| P1-WEB-046 | done | WEB | Fix paste button keyboard-activation parity (migrate to classifyHold). ACTIVE bug (not latent): keyboard Enter/Space on the focused paste button fired `onClick` with no preceding `pointerdown`, so `pasteDownAt` stayed at its initial `0` and the raw `Date.now() - 0 >= 450` check always passed → always took the 'insert at cursor' (hold) branch, never the documented 'replaces all' (tap) default (`aria-label="Paste (hold to insert at cursor)"` / `data-tip="Paste — replaces all · hold to insert at cursor"` at `ChatView.tsx:2376-2377`). Identical shape to the pre-`c8266e7` Copy-button bug. Fix: migrate the paste click path's raw inline `Date.now() - pasteDownAt >= 450` comparison (`ChatView.tsx:1203`) to `classifyHold(pasteDownAt, Date.now()) === "hold"`, which has a `downAt===0 → "tap"` sentinel — keyboard activation now correctly yields 'replace' (the documented default), and pointer-driven hold/tap behavior is unchanged (same threshold, same direction: hold>=450ms→insert, tap<450ms→replace). Also resolves AG-F5 threshold-drift advisory: paste stops hardcoding `450` and shares `HOLD_THRESHOLD_MS` via `classifyHold`, same as the Copy button. | build | 2026-07-18. DONE. Files: `web/src/components/ChatView.tsx` (paste `onPasteClick` migrated to `classifyHold`; in-file comment `:1189-1202` updated to note shared helper + keyboard sentinel; original load-independence rationale preserved). No new test — existing `classifyHold` tests at `web/tests/unit/copyHold.test.ts:35-54` already cover the `downAt===0 → "tap"` sentinel (4 cases); pure-helper semantics unchanged, so a wiring/jsdom test is out of scope (separate `defer-copyhold-wiring-test` DEFER). Provenance: source:review-defer; promoted from defer-paste-keyboard-edge; trigger path_touched(web/src/components/ChatView.tsx) met; review:commit c8266e7 CF4. Verified: `npm --prefix web run test:unit` all pass (no count change — no new tests); `npm --prefix web run typecheck` clean (exit 0). |  |
| P1-API-004 | done | API | OpenCode changelog display in the update dialog. Best-effort fetch of `https://opencode.ai/changelog.json` (short in-memory cache, graceful degrade) + new `GET /vh/opencode-changelog?from=&to=` (auth-gated `/vh/*`, GET CSRF-exempt, server-side semver filter `installed < tag <= latest` reusing `normVer`+`semverRe` from `cmd/opencode_update.go`). Three highlight axes: (A) section priority — Core/SDK/Extensions first, Desktop/TUI de-emphasized; (B) forward-compat `highlights[]` rendered at top of release if non-empty; (C) best-effort "⚠ may affect you" heuristic (tokens: breaking/migrat/removed/deprecat/no longer/replaced/renamed/must now/required) on Core/SDK/Extensions items only. Collapsible "What's new (since {installed})" panel in `OpenCodeUpdateDialog.tsx`, lazy-loaded via its own resource (never blocks version fetch/update button). No mask-image/contain/backdrop-filter on scroll containers. | build/oc-changelog-dialog | 2026-07-17. DONE. Files: `cmd/opencode_update.go` (fetch/cache+compareSemver+filter+heuristic+OpencodeChangelog), `pkg/web/opencode_changelog.go` (DTOs+handler+setter), `pkg/web/server.go` (route+ocChangelogFn field), `cmd/client-daemon.go`+`cmd/local-server.go` (wire), `tools/fixtureserver/main.go` (fixture), `web/src/components/OpenCodeUpdateDialog.tsx`+new `.module.css`, Go tests (`cmd/opencode_update_test.go`+`pkg/web/opencode_changelog_test.go`) + web test. Verified: `go build ./...` clean, `go test ./...` all ok, `npm run typecheck` clean, `npm run test:unit` 607 passed, `gofmt -l` clean. |  |
| P1-AUTH-001 | done | AUTH | `randToken` ignores the `crypto/rand` error → predictable OAuth state on entropy failure. `randToken` (`pkg/auth/handlers.go:194-198`) drops the error (`_, _ = rand.Read(b)`) and is consumed as the OAuth `state` CSRF token (`:54`). Fix: return `(string, error)`; callers render 500 on failure. Low risk. | deep-review-sweep | 2026-07-16. DONE. Commit aa69e51. returns (string,error); caller renders 500; TestRandToken green. Deep-review R2 finding. backlog-now (ready). Verified: line numbers confirmed 2026-07-15. |  |

## Cancelled

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P1-WEB-010 | cancelled | WEB | Pin `bottommostReadFromDom`'s scroll-origin fallback in vitest. The mid-history-anchor regression (P1-WEB-009 / 84251be) is currently covered by exactly one serial e2e path (`unread-dot.spec.ts:173`). No isolated unit test pins `bottommostReadFromDom`'s `rows[0]?.id` fallback at the scroll origin (first row sits at relative top=+16 due to `.chat-scroll` `padding:16px`). A future refactor of `bottommostRead`'s threshold (`web/src/lib/scroll.ts`) could regress silently until the serial e2e suite reruns. Add a vitest unit test asserting `bottommostReadFromDom` returns `rows[0].id` when the first row sits at top=+padding (scrollTop=0) and `nearBottom` is false. | web | 2026-07-11. Cancelled — relocated to .local/coordinator/tasks/ as trigger-gated DEFER candidate defer-bottommostreadfromdom-vitest (trigger: path_touched(web/src/lib/scroll.ts), not yet met). Original note: D-F1 from commit-review of 84251be (advisory, dropped on P1-WEB-009). Deferred. Trigger to un-defer: `web/src/lib/scroll.ts` `bottommostRead` threshold touched. Locate the existing scroll unit-test home (likely `web/tests/unit/scroll.test.ts`, modeled on the `bottommostRead` tests referenced in P1-WEB-002) and add the case there. |  |
| P1-WEB-015 | cancelled | WEB | Surface a user-visible error/retry state in the ChatView UI for failed background message hydration. Slice C (P1-AGG-001) added `messages.error` events, but the FE reducer only `console.warn`s them — a failed background fetch shows an indefinite "Loading…" with no operator-visible signal. The daemon self-heals on the next session selection/reconnect, but there is no affordance to see or retry a failed load. Build an explicit error/retry chip when faced with it in practice. | build | 2026-07-11. Cancelled — relocated to .local/coordinator/tasks/ as trigger-gated DEFER candidate defer-chatview-hydration-errorchip (operator-judgment-gated soft trigger — no path_touched). Original note: Deferred follow-up from Slice C: the `messages.error` event reaches the FE reducer as `console.warn` only (`web/src/sync/stream.ts`), while the ChatView empty-state/loading branch (`web/src/components/ChatView.tsx`) renders an indefinite "Loading…" on a failed background fetch — add the error/retry chip there. | P1-AGG-001 |
| P1-WEB-013 | cancelled | WEB | Stuck reasoning timer (`Part.tsx:549` `live()` + `selectors.ts:180`) — cosmetic, self-heals on turn completion. Fix: shared read-side helper `reasoningEnded(part, message) = !part.time?.end && hasSuccessorPart(message, part.id)` mirroring Go's alerts-engine inference (`pkg/alerts/engine.go:255-258`); needs fixture extension (today's fixtures at `pkg/fixtures/opencode.go:75,146` always supply `time.end`). Rejected: upstream per-message-flag (protocol change) and client-side time.end reconciliation (fights part snapshots). |  | 2026-07-11. Cancelled — relocated to .local/coordinator/tasks/ as trigger-gated DEFER candidate defer-reasoning-timer-stuckend (fixture-capability-gated; promoter must confirm a fixture case emitting an in-flight reasoning part WITHOUT time.end exists before re-promoting). Original note: Concept A from the 419ea39 / P1-WEB-011 live-autofollow slice — explicitly out-of-scope there, pre-scoped for deferred pickup. Deferred: do NOT implement until a fixture extension that can emit an in-flight reasoning part WITHOUT `time.end` (so the stuck-timer state is reproducible end-to-end). |  |
| P1-DOCS-001 | cancelled | DOCS | Fix the AGENTS.md restore-placeholder verb so it matches shell-guard (`commit-gate.sh revert`, not `git checkout`). The "Web frontend" section of the generated `AGENTS.md` (sourced from `AGENTS.mission.md`) instructs agents to run `git checkout -- pkg/web/dist/index.html` to restore the placeholder after a local build, but `shell-guard` blocks `git checkout` for non-committer agents and `.opencode/docs/git-execution-routing.md` sanctions `.opencode/scripts/commit-gate.sh revert <paths>` as the in-session restore-to-HEAD path. Correct the mission overlay text so the instruction matches the enforced guard. |  | 2026-07-04 SUPERSEDED by P1-WEB-025 (done 2026-07-03). The current `.vh-agent-harness/AGENTS.mission.md:42-44` already routes agents through `.opencode/scripts/commit-gate.sh revert pkg/web/dist/index.html` (shell-guard-aligned). The old "instructs agents to run `git checkout`" wording this task targeted no longer exists — no edit required. Surfaced while fixing/committing 84251be (P1-WEB-009). `AGENTS.md` is generated, so edit `AGENTS.mission.md` — the line "When a local build dirties `pkg/web/dist/index.html`, `git checkout -- pkg/web/dist/index.html` before committing." — not the rendered file. Verify: grep `AGENTS.md` for the corrected verb and confirm no `git checkout -- pkg/web/dist` instruction remains. |  |
