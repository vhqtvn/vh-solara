# Backlog Archive: 2026 Q3

This file stores older `done` and `cancelled` rows moved out of `docs/planning/backlog.md` by `.opencode/scripts/normalize-backlog.js` so the main backlog can stay focused on active work.

## Done

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
| P1-AUTH-002 | done | AUTH | OIDC `email_verified` parsed but never enforced. `pkg/auth/handlers.go:74-89` reads the claim; the allow-list (`pkg/auth/auth.go:209-228`, `emailAllowed`) is email-only. Fix: opt-in `RequireVerifiedEmail` config gate OR hard-enforce; add a table test (no existing test pins the lax behavior → test-safe). Low risk. | deep-review-sweep | 2026-07-16. DONE. Commit aa69e51. opt-in --auth-require-verified-email gate; 8-subcase TestGrantOIDC green. Deep-review R2 finding. backlog-now (ready). Verified: only `emailAllowed(claims.Email)` is checked, `EmailVerified` unused 2026-07-15. |  |
| P2-AGG-001 | done | AGG | `ringBuffer.push` reallocates O(cap) every push once full (~160KB copy/event). `pkg/state/ring.go:17-24` (cap 4096; producer `store.go:468`). Fix: fixed array + modular head index. **Add a unit test pinning `since()` semantics FIRST.** Med risk. | deep-review-sweep | 2026-07-16. DONE. Commit d407fee. fixed-array modular head; test-first 6 ring tests; push O(1). Deep-review R2 finding. backlog-now (ready). |  |
| P2-AUTH-001 | done | AUTH | Controller mutating endpoints (`/api/workers/*`) rely solely on SameSite=Lax with no CSRF token (worker side already uses `X-VH-CSRF`). `pkg/auth/auth.go:93,136` + controller handlers + SPA. Fix: mirror worker-side `X-VH-CSRF:1` on controller mutating handlers + SPA `installCsrf`. Defense-in-depth. Low-Med risk. | deep-review-sweep | 2026-07-16. DONE. Commit 944ff0b. csrfGuard scoped to /api/ inside auth; dashboard JS sends X-VH-CSRF:1; 4 tests green. Deep-review R2 finding. backlog-now (ready). |  |
| P2-AGG-002 | done | AGG | Swallowed `gzip.Close` + `json.Marshal` errors (`_,_ =`). `pkg/state/store.go:2241`, `pkg/web/server.go:901,1062,1162`. Fix: propagate/log instead of discarding. Low risk (structurally can't fail today). | deep-review-sweep | 2026-07-16. DONE. Commit e975ffd. log-on-error at all sites; no signature ripple. Deep-review R2 finding. backlog-now (ready). |  |
| P2-API-001 | done | API | `handleStatic` double-opens embed files per request. `pkg/web/server.go:1278-1305`. Fix: cache the existence check or precompute served paths. Low risk (embed.FS is memory-backed). | deep-review-sweep | 2026-07-16. DONE. Commit dd60c6d. staticPaths map + knownStatic; bare-dir→SPA fallback (matches documented intent). Deep-review R2 finding. backlog-now (ready). |  |
| P2-API-002 | done | API | `git.go` doesn't run file args through `safeJoin` (inconsistent with `code.go:49-67`). `pkg/web/git.go`. Fix: mirror `safeJoin` (reject `..` + symlink escape). Low risk. No shell-injection today (argv-only, `--` separator). | deep-review-sweep | 2026-07-16. DONE. Commit dd60c6d. confineGitFiles; traversal/symlink-escape rejected with 400. Deep-review R2 finding. backlog-now (ready). |  |
| P2-AGG-003 | done | AGG | `SetActivityFromStatuses` shadows the Go 1.25 builtin `clear`. `pkg/state/store.go:681`. Fix: rename the closure. Trivial. | deep-review-sweep | 2026-07-16. DONE. Commit dd60c6d. clear→clearActivity. Deep-review R2 finding. backlog-now (ready). |  |
| P1-WEB-043 | done | WEB | `stream.ts` SSE `JSON.parse` undefended (7 sites: `:706,739,753,987,1101` listeners + `:927,945` helpers); `notice` `:760` is the defended pattern to mirror. `web/src/sync/stream.ts`. Impact: silent per-frame drop + tree-cursor desync (NOT listener kill — DOM spec keeps the listener registered). Fix: wrap each in try/catch mirroring `:759-763`; for cursor paths advance before parse or account in catch; helpers return a safe empty envelope. Test: extract `applyTreeFrame(kind, rawStr)` injectable (precedent `applySessionSnapshot` `:816`). Low risk. | deep-review-sweep | 2026-07-16. DONE. Commit e126f24. 7 sites wrapped + applyTreeFrame extracted; 11 new tests; npm test:unit 521 green. Deep-review R2 finding. backlog-now (ready). |  |
| P1-WEB-044 | done | WEB | Custom `Select` not keyboard-navigable (6 consumers: TabBar, ChatView, CodeView, PreferencesView, NotificationsSettings, SettingsDialog). `web/src/components/Select.tsx:40`. Fix: focus-move model (options are real focusable `<button>` `:93`); trigger ArrowDown/Up/Enter/Space→open+focus; listbox Arrow/Home/End/type-ahead→`.focus()`+`scrollIntoView({block:nearest})`; Enter/Space pick; Escape/outside-click→return focus to trigger. Med risk. Test: jsdom keydown driving focus + onChange. | deep-review-sweep | 2026-07-16. DONE. Commit 59c7285. focus-move model; 9 jsdom tests green. Deep-review R2 finding. backlog-now (ready). |  |
| P1-WEB-045 | done | WEB | Nested `role=button` inside `<button>` + no Enter/Space activation. `web/src/components/Part.tsx:509,524-535,561-573`. Invalid HTML + WCAG 2.1.1. Only 2 instances (both Part.tsx). Minimal fix (Low): add `onKeyDown` Enter+Space (Space preventDefault). Full fix (Med): restructure so actions are siblings. Recommend minimal first. | deep-review-sweep | 2026-07-16. DONE. Commit 7f892ab. onActionKey helper + 14 tests green. Deep-review R2 finding. backlog-now (ready). |  |
| P2-WEB-001 | done | WEB | GitView icon buttons missing `aria-label` (4 buttons). `web/src/components/GitView.tsx:96,98,100,163`. Note: `Tooltip.tsx:30` renders `data-tip` as a separate bubble and never sets an accessible name → data-tip-only buttons are SR-unnamed. Fix: add `aria-label` matching `data-tip`. Trivial. | deep-review-sweep | 2026-07-16. DONE. Commit d705953. 4 icon buttons aria-labeled. Deep-review R2 finding. backlog-now (ready). |  |
| P2-WEB-002 | done | WEB | Native dialog migration: `CodeView.tsx:225` `prompt("Go to line")` and `GitView.tsx:101` `confirm(...)`. Replace with in-app patterns (`TextPromptDialog.tsx:5` built for this; `PermissionCard`/`QuestionCard` confirm pattern). Low risk. | deep-review-sweep | 2026-07-16. DONE. Commit 5659876. CodeView go-to-line→TextPromptDialog; GitView discard→in-app confirm card; 6 tests green. Deep-review R2 finding. backlog-now (ready). |  |
| P2-WEB-003 | done | WEB | ChatView (2322 lines) has zero unit coverage. Extract pure logic + tests: arm-time stash state machine (`ChatView.tsx:635-815`, best first extraction, pure given a clock); scroll-geometry/self-heal predicates. NOTE: queue drainer ALREADY extracted (`queueDrain.ts` + test) — do NOT re-flag. Low risk (pure extraction). | deep-review-sweep | 2026-07-16. DONE. Commit f1a52fb. read-cursor stash → pure readCursorStash.ts; 26 tests; byte-for-byte preserved. Deep-review R2 finding. backlog-now (ready). |  |
| P3-WEB-001 | done | WEB | `CodeView.tsx:412,415` `rel="noreferrer"` → add `noopener`. Nit (modern browsers imply it). Trivial. | deep-review-sweep | 2026-07-16. DONE. Commit d705953. 2 links rel=noopener noreferrer. Deep-review R2 finding. backlog-now (ready). |  |
| P1-REL-001 | done | REL | `install.sh` has no checksum verify (while `update` mandates SHA256) + unguarded `jq`/`sudo`/null-tag. `install.sh:15,16,42-49`. Fix: add SHA256SUMS fetch+verify mirroring `cmd/update.go:103-110`; guard `jq`; reject literal `null`; elevate only when needed (`install -m 0755` + sudo fallback). Low-Med risk. Verify: positive real release + negative forced mismatch. | deep-review-sweep | 2026-07-16. DONE. Commit c7d57c4. SHA256SUMS verify + atomic sibling-staging install + jq/null/sudo guards; offline 4-case + live v1.50.0 pass. Deep-review R2 finding. backlog-now (ready). Security. |  |
| P2-INFRA-001 | done | INFRA | Node version alignment: `.nvmrc=lts/*` (floating), `Dockerfile:6`/`Dockerfile.e2e:6`=node:20, CI/README=24, no `engines`. Fix: `.nvmrc`→`24`; Dockerfiles→`node:24-alpine`; `web/package.json` add `"engines":{"node":">=24"}`. Low risk. | deep-review-sweep | 2026-07-16. DONE. Commit 65fa90f. .nvmrc→24; Dockerfile*→node:24-alpine; web/package.json engines ≥24. Deep-review R2 finding. backlog-now (ready). |  |
| P2-CI-001 | done | CI | Make/CI alignment bundle: `make test`=`./pkg/...` vs CI `./...` (5 test files skipped locally) → make test `./...` + `test-unit` target; `release.yml:51` add `CGO_ENABLED=0`; `release.yml:26-28` add `setup-node cache:npm`+`cache-dependency-path`; `Makefile:9` `npm install`→`npm ci`; add `make fmt`/`fmt-check`/`vet`/`typecheck` wrappers. Low risk. | deep-review-sweep | 2026-07-16. DONE. Commit 63025ef. make test ./..., test-unit, fmt/fmt-check/vet/typecheck wrappers, npm ci, release CGO_ENABLED=0, setup-node cache. Deep-review R2 finding. backlog-now (ready). |  |
| P2-CI-002 | done | CI | CI hardening: no `concurrency:` (push+PR double-fire) → add `concurrency:{group:${{github.workflow}}-${{github.ref}}, cancel-in-progress:${{github.event_name=='pull_request'}}}` to ci.yml; 12 actions floating-major → SHA-pin (resolve current SHAs) + add `.github/dependabot.yml` (github-actions, weekly). Med risk (pins need maintaining). | deep-review-sweep | 2026-07-16. DONE. Commit 0e87a2a. 12 actions SHA-pinned; ci.yml concurrency cancel; dependabot.yml added. Deep-review R2 finding. backlog-now (ready). |  |
| P2-INFRA-002 | done | INFRA | HEALTHCHECK in `docker-compose.yml` for `server` (:8080) and `ui-demo` (:8099): `wget -qO- http://127.0.0.1:<port>/vh/healthz` (endpoint exists, auth-exempt `pkg/web/server.go:525-527`/`pkg/auth/auth.go:164`; confirm fixtureserver mounts it). Low-Med risk. | deep-review-sweep | 2026-07-16. DONE. Commit ae9edef. both services probe /vh/healthz; controller serves it + auth-exempt made mode-independent. Deep-review R2 finding. backlog-now (ready). |  |
| P1-DOCS-002 | done | DOCS | README truth-pass (Slice A, README + one arch doc, one commit): delete nonexistent `--bin` flag (`README.md:259`); relabel `netns` stub + delete "namespace isolation, ip" clause (`README.md:110,313`) AND fix broken `docs/architecture/02-implementation-decisions.md:15-18` which falsely claims netns implemented; relabel `health` stub (`README.md:274,315`); drop `-<uuid>` from worker URL (`README.md:282`); add `--api-token`/`VH_API_TOKEN` to server example + mcp CLI note (`README.md:134-136,314`); correct `<project>`→`sha1(cwd)` + add "State & files" section documenting `VH_STATE_DIR` (`README.md:195` + new subsection); relabel `version` row (`README.md:316`); note `kit`/`skill` subcommands (`README.md:310-311`). All trivial except the state-layout section (Med). | deep-review-sweep | 2026-07-16. DONE. Commit 15877b9. --bin, netns/health relabel, worker-url uuid, state-layout section, api-token, version, kit/skill; fixed 02-implementation-decisions.md netns claim. Deep-review R2 finding. backlog-now (ready). Relabel-only for netns/health (NOT implementing them — product decision not made). |  |
| P2-DOCS-001 | done | DOCS | `06-auth.md` config block + stale comment (Slice B): add `--auth-oidc-redirect-url` and `--auth-cookie-secure` to `docs/architecture/06-auth.md:119-127`; drop "client-daemon" from stale comment `cmd/auth_flags.go:13-14` (only server.go:48 + local-server.go:261 call registerAuthFlags). Trivial. | deep-review-sweep | 2026-07-16. DONE. Commit 98c64c7. all 12 flags documented 1:1; comment dropped client-daemon. Deep-review R2 finding. backlog-now (ready). |  |
| P0-API-003 | done | API | Stored XSS in controller dashboard — worker name/id unescaped in `handleUIPage`. `pkg/server/daemon.go` (+ `pkg/server/daemon_xss_test.go`). FIXED by the parallel build/xss slice: DOM-API construction (textContent/createElement/addEventListener/property assignment) replaced innerHTML+onclick string-concat; 3 regression tests added. | build/xss-slice | 2026-07-15. DONE. Files: `pkg/server/daemon.go`, `pkg/server/daemon_xss_test.go`. Verification (all green): `go test ./pkg/server/` (10/10 incl 3 new), `go build ./...`, `gofmt -l pkg/server` (clean), `go vet ./pkg/server/`. |  |
| P1-WEB-032 | done | WEB | Pre-existing DETERMINISTIC hard-bug in `web/tests/e2e/scroll-follow.spec.ts` test (6) "scrolling back to the bottom flips Latest → Live" (`:305` post-P1-WEB-029; was `:267` pre-fix). Surfaced during the P1-WEB-029 probe arc. Fails in ISOLATION on unmodified HEAD (3/3 at `:267` after reverting both source files via `commit-gate.sh revert`), so it is NOT serial contamination and NOT a regression from P1-WEB-029's `beforeEach`/de-dup. Mechanism: the test captures `bottomClamp` by scrolling to bottom, scrolls up, scrolls back to the EXACT clamp, and asserts `following()` flips true (bottom geometry via `expectFollowingTail` `:71` `.toBe(1)` + button.jump count 0); the viewport is NOT at the bottom after the back-scroll. Determinism bar (same as the rest of the spec): `--repeat-each=50 --retries=0` green in isolation. Diagnosis-first: confirm the geometry at runtime before fixing. | build | 2026-07-14. CORRECTED DONE — prior 2026-07-07 closeout was PREMATURE (misattributed). The prior closeout attributed test 6's fix to the fixture-reset hardening (commit `25f332f`, shared with P1-WEB-032/033) targeting SERIAL `button.jump` COUNT pollution (tests 5/7/14 from a leaked `[[ask]]` blocker pill). That serial fix was VALID for tests 5/7/14 but IRRELEVANT to test 6: test 6 fails 20/20 RED in ISOLATION (no serial contamination possible). ACTUAL ROOT CAUSE (diagnosed 2026-07-14 via the debugging-loop skill + instrumentation): a STARTUP TIMING RACE. `onScrolled` (ChatView.tsx:993) early-returns on `if (!ready()) return` while `maybeRestore` (a ~178ms startup window between the session-switch effect's `setReady(false)` and `maybeRestore`'s `setReady(true)`) hasn't completed. The test's `setScrollTop(0)` fires DURING that window → onScrolled drops the event → `following()` stays true → `button.jump` never renders → RED at `:324` (toBeVisible → element not found). The serial fix's `button.jump` count-pollution mechanism never enters the picture — the button never renders at all. Environment-sensitive: a slower test path (e.g. a `waitForTimeout` before scrolling) lets `maybeRestore` complete first and the test passes, explaining how the prior `--repeat-each=50` claim may have held in a different timing envelope. SAME RACE also broke test 2 ("scrolling up shows the Latest button", `:179`), also isolation RED. FIX (TEST-ONLY — no production code changed): added `waitForReady(page)` helper to `scroll-follow.spec.ts` that waits for `.chat-content.ready` (the app's own `revealed()` = `ready() && delivered()` reactive signal that startup restore completed) before issuing scroll commands. Inserted in `openDemo` (covers tests 1/2/3/4/7/9/11/12/13…) and inline in tests 5 and 6 (both bypass `openDemo`). Determinism bar MET: test 6 `--repeat-each=50 --retries=0` = 50/50 green; test 2 ×50 = 50/50 green; full `scroll-follow.spec.ts` ×1 = 18 passed / 0 failed / 1 skipped (pre-existing "needs fixture hook"); test 11 ×10 = 10/10 green (P1-WEB-029 fix NOT regressed). Changed file: `web/tests/e2e/scroll-follow.spec.ts` only. | P1-WEB-029 |
| P1-WEB-041 | done | WEB | Fix attach-chip never rendering after file pick. `addFiles()` in `web/src/components/ChatView.tsx` cleared `fileInputRef.value` before `Array.from(files)`, emptying the live `FileList` tied to the `<input type=file>`, so the upload loop never ran and no `/vh/attach` request fired. Fix: snapshot files before clearing the input (input clear preserved so re-picking the same filename re-fires onChange). Regression from `3f5a652`; only the file-pick path (setInputFiles) was affected, not the paste path. Also bumped `web/tests/e2e/scroll-follow.spec.ts:688` working-text turn-start wait 5s→8s (serial-suite `[[stall]]` absorption race). | build | 2026-07-11. DONE. Commit `77ae4c8`. Files: `web/src/components/ChatView.tsx`, `web/tests/e2e/scroll-follow.spec.ts`. Verification: features2 attach test passes 2/2; scroll-follow test passes 3/3; `npm run typecheck` clean; `npm run test:unit` 342/342. Commit-reviewer APPROVE unanimous (0 blocks). |  |
| P1-WEB-040 | done | WEB | Style thin, app-themed custom scrollbars on ALL scrollable surfaces so Chromium-based browsers (Chrome/Edge on Windows + Linux) and Firefox stop showing the thick unstyled OS-default scrollbar. CSS-only slice. Approach: one GLOBAL foundation rule (`web/src/styles/foundation/scrollbars.css`, `*` selector) styling every surface at once — cleaner than editing ~15 component CSS files. Covers both vendor paths: Firefox via `scrollbar-width: thin` + `scrollbar-color`, Chromium/Safari via the `::-webkit-scrollbar*` family (width/height/thumb/track/corner). GPU-safe per AGENTS.md Firefox/WebRender guardrails: NO mask-image, NO backdrop-filter, NO contain: paint, NO content-visibility — only plain scrollbar properties. Colors reuse the `--fg-dim` theme token (translucent via color-mix) so the thumb adapts to all 27 shipped themes; thumb inset via transparent border + background-clip:padding-box. Existing opt-outs preserved via specificity: `.seg` (hidden: `scrollbar-width:none` + `::-webkit-scrollbar{display:none}`) and `.term-tabs`/`.code-tabs` (thin) unchanged. No JS/TS scroll logic touched. | build | 2026-07-11. DONE. Commits `4b53151` (the CSS: new `scrollbars.css` + `@import` wired into `main.css`) + `0930d65` (docs-sync: `main.css` header comment + `docs/ai/web-css-architecture.md` §1 tree now enumerate `scrollbars.css`). Reviewer APPROVE unanimous 4/4 on both, zero blocks. Verified: `cd web && npm run typecheck` clean; `cd web && npm run test:unit` 311/311 pass (33 files); `git diff --exit-code -- pkg/web/dist/index.html` clean (no make web/build; placeholder untouched). Forbidden-pattern grep on scrollbars.css: only matches are in the explanatory comment (documenting the avoided patterns), no CSS usage. Token check: `--fg-dim` resolves in every theme block of `tokens.css` (dark, light, dim, midnight, hc, shire-dark/light, tokyonight, dracula, nord, catppuccin(+latte), gruvbox, rose-pine(+dawn), one-dark, everforest, ayu, solarized-dark/light, monokai, kanagawa, material, paper, paper-color, eink, eink-color). Excluded: none — global `*` covers every scrollable surface including horizontal overflow (`overflow-x:auto` on `.vh-diff`, katex tables, `.code-tree`). | docs/ai/web-css-architecture.md |
| P1-WEB-039 | done | WEB | `onScrolled` missing `ready()` guard destroys a freshly-entered session's read anchor on empty-content clamp (silent mid-history read-position loss). Mechanism: switching within one ChatView mount to an uncached session → `openSession` pre-reserves empty `{order:[],byId:{}}` (`actions.ts:76-79`) → switch effect resets `pinnedGeom`+`setReady(false)` but NOT `following()` → empty transcript clamps `scrollTop`→0 → `onScrolled` (no `ready()` guard; self-pin bail `following()`-gated) → `classifyScrollDelta` `atBottom=true` → `clearReadAnchor` → `maybeRestore` defers on empty order → reads cleared anchor → pins to bottom. Fix: `if (!ready()) return;` at top of `onScrolled` (matching existing `ready()` gates at `:441/:470/:865/:958/:1207`). | build | 2026-07-09. DONE. Fixed in commit `9fa61bf`: added `if (!ready()) return;` at top of `onScrolled` (`ChatView.tsx:975`, after the `scrollEl` guard) — the lone scroll/position effect missing the established `ready()` guard (5 siblings at `:441/:470/:865/:958/:1207`). New Playwright regression in `unread-dot.spec.ts` (test 6): two-synthetic-scroll design — seeds the target's anchor + delays its EventSource stream to hold messages empty (`ready()`=false), then dispatches scroll 1 (top, flips `following()`=false via user-scroll-up) + scroll 2 (bottom, trips reached-bottom → `clearReadAnchor`). RED-without-fix (anchor destroyed), GREEN-with-fix `--repeat-each=5 --retries=0` (5/5). DEVIATION: the mission's natural-repro premise (`following()` stays stale-false through the switch) is incomplete — the composer session-switch effect `ChatView.tsx:1231-1253` runs `setFollowing(true)` on every switch, so the natural empty-content collapse is caught by the following()-gated self-pin bail and does NOT reach `clearReadAnchor` by itself. The vuln CLASS is still real (`following()`=false during `!ready()` is reachable by a user scroll-up then back-to-bottom before the snapshot loads) and `onScrolled` was the lone unguarded effect; the test forces the exact path deterministically. Verification: typecheck clean, vitest 219/219 (baseline unchanged), new test RED-without-fix/GREEN-with-fix, full serial e2e 122 passed (1 failed `header.spec.ts` + 1 flaky `interactive.spec.ts` are a CONCURRENT session's uncommitted `.status`→`.status-dot-head` CSS rename in `styles.css`/`Sidebar.tsx`, unrelated to this change). commit-reviewer APPROVED (4/4 unanimous, high confidence, low risk). Surfaced during P1-WEB-004 build; re-verified read-only at HEAD `b35fe8f` against the HEAD-`5321250` persistence study (the `ca9c5a0` dual-axis refactor moved `clearReadAnchor` into the reducer's reached-bottom branch but did NOT mask the vuln). NOT fixed by `840f2f7` (transport-only) or `ca9c5a0`. | P1-WEB-004 |
| P1-WEB-038 | done | WEB | Warm-open tunnel-perf fix + sync-ui `server`→`snap` relabel + per-turn tok/s / TTFT stats — released in v1.38.0. Two-slice fix: (1) warm-snapshot `.dot.refreshing` indicator (logic + tests + CSS) signals when a warm session paints cached data while its fresh snapshot is in flight; (2) bounded `refreshOpenSessions` fan-out (`REFRESH_CONCURRENCY=3`, tunable) to protect the yamux-over-WebSocket tunnel from head-of-line / bandwidth contention (the ~5s was tunnel transport, not server compute). Also relabeled the FE `server` latency metric to `snap` (honest end-to-end labeling), added `GET /vh/snapshot` `dur_ms` diagnostic logging + `make build-debug` target, and per-turn tok/s + TTFT (hover info icon). | build | 2026-07-09. DONE. Released in tag `v1.38.0` (commit `a95c6a8`), minor bump `v1.37.1`→`v1.38.0` (5 feat, 3 fix, 0 breaking). Verification on committed state: GREEN — gofmt clean, `go build ./...`, `go test ./...`, SPA typecheck, vitest 219/219, `make web` leaves placeholder untouched. Full perf detail + shipped-commit table + deferred follow-ups (slice (b) payload shrink, etc.) in `docs/checkpoints/2026-07-08-warm-open-tunnel-perf-investigation.md`. | docs/checkpoints/2026-07-09-v1.38.0-release.md |
| P1-WEB-018 | done | WEB | Mid-turn auto-follow latch has no in-turn recovery (hardening). If the contentEl ResizeObserver re-pin guard (web/src/components/ChatView.tsx:779-780) arms `userScrolledUp` while `working()` stays true — the residual while-VISIBLE same-frame coalesced shrink+regrow (a raw `md-stream`→`MarkdownHtml` settle shrink and a token regrow collapsing into one RO delivery: net-grow with clamped `scrollTop<pinnedTop`) — Live stays dead until manual "↓ Latest"/scroll-back. The self-heal effect (ChatView.tsx:390-400) needs a busy edge and the resume re-engage (ChatView.tsx:419-426, added by 90f6ef12) is gated on `!userScrolledUp()`, and the latch deliberately survives the turn boundary — so the next turn does NOT recover it either (permanent until user action, not transient). Low probability (the settle swap is normally multi-frame), high impact. Likely approach: a bounded "re-arm following if still near bottom after a settle swap" guard, or clear the latch on a confirmed content-shrink clamp. Must NOT add a per-frame layout sweep (Firefox/WebRender heat). | build | 2026-07-08. DONE. Implemented the settled solution-brief (dual-axis geometry reducer option A_plus + logical anchor correction) which SUBSUMES this row's single-axis shrank-guard mis-classification. The reducer (web/src/lib/scroll.ts::classifyScrollDelta) decomposes contentDelta + viewportDelta, predicts the browser's post-geometry clamp scrollTop, and treats genuine user scroll-intent as the RESIDUAL; ChatView's content RO, scrollEl RO, and onScrolled feed previous/current geometry through it (sentinel → pinnedGeom). Read mode preserves the logical data-mid anchor via measured anchorDelta (overflow-anchor as assist only). Fixes BOTH this row's same-frame coalesced shrink+regrow mid-turn latch AND the composer grow/shrink autoscroll deadlock (composer grow → flex-shrink clamp + scroll event flipped following(false) before the scrollEl RO could re-pin; a conservative scrollEl-RO `else if (nearBottom())` re-engage covers the shrink-back no-event case). Commits ca9c5a0 (reducer+ChatView wiring+29 vitest tests, commit-reviewer APPROVE) + b7afccc (stale comment cleanup, commit-reviewer APPROVE). Verified: `npm run typecheck` clean; `npx vitest run` 199 passed (was 170; scroll.test.ts 20→49). NO mask-image/backdrop-filter/content-visibility; no third ResizeObserver; epsilon-guarded scrollTop writes. OPERATOR-VERIFICATION PENDING (not run by this agent): manual/Playwright repro on a real large session + Firefox WebRender heat check; commit-reviewer flagged a potential 2-24px Live-drop regression from the reducer atBottom (1px) vs nearBottom() (24px) threshold disagreement — captured as defer-scroll-epsilon-threshold-disagreement in .local/coordinator/tasks/. Original read-only study of follow-up B from commit 90f6ef12: Verdict: partial/conditional — reachable but low-probability; if armed, permanent until user action. Affected sites: web/src/components/ChatView.tsx:390-400 (self-heal), :419-426 (resume re-engage), :779-780 (RO guard arming site). The primary trigger (hidden/resume coalesced RO) was closed by 90f6ef12; this is the remaining surface. No current repro — needs a fixture hook to force a same-frame settle+token, modeled on the existing scroll-follow e2e. |  |
| P1-WEB-034 | done | WEB | Fix e2e flake — `web/tests/e2e/sync.spec.ts:30` "switching sessions re-targets the message stream" (Group B of the e2e-flake-fix arc). CI fails `:43` `getByText('Done. Updated').first().toHaveCount(0)` → got 1 after switching demo→other (both CI attempts). Diagnosis-first; confirm at runtime, then fix. Preferred order: test-side → fixture-side → committed sync code; HARD STOP if a fix would require editing the concurrent session's dirty files (store.ts/stream.ts/ServersPanel.tsx/styles.css/store.go/aggregator.go/server.go). Determinism bar = isolation `--repeat-each=20 --retries=0` green + cross-file (sync+interactive) green + full sync.spec.ts green. Refs P1-WEB-033 (Group A, done). | build | 2026-07-07. DONE. Root cause = CI-load DOM-teardown timing, NOT a code isolation bug (contradicts task's "isolation not timing" settled assumption — surfaced). Proven: SPA binding correct+reactive (single rebound ChatView keyed off selectedId; `.msg` emitted only at ChatView.tsx:1522 inside `<For each={messages()}>`; sessionID-scoped message routing in stream.ts; scoped Snapshot in store.go; no global message-text renderer). CI failure NOT reproducible locally — full e2e suite 117 passed/1 skipped on BOTH the dirty tree AND a detached worktree at exact CI HEAD (3c814d8). Fix 85870d6: hardened sync:30 to pollution-independent `.msg` count 0 (old `getByText('Done. Updated')` marker only verified re-targeting under cross-spec pollution, passed trivially otherwise = coverage hole). Timing-neutral (Solid `<For>` batch teardown → old marker and new selector reach count 0 same instant). Verified: isolation --repeat-each=20 20/20, cross-file sync+interactive 14/14, full sync.spec 3/3, typecheck clean, vitest 170/170, go build clean, gofmt clean. UNRESOLVED teardown-timing flake DEFER'd to `.local/coordinator/tasks/` (commit-reviewer F2). Local green ≠ CI green (CI load is the confound). HARD STOP respected — no concurrent-session dirty files touched. | P1-WEB-033 |
| P1-WEB-037 | done | WEB | Fix e2e `web/tests/e2e/sync.spec.ts:47` message mis-attribution (`.msg` toHaveCount(0) got 6 after demo→other switch) — the #2 issue P1-WEB-036 stopped on. NO REVERT of 840f2f7. Root cause confirmed by reproduction/instrumentation BEFORE editing (prior static code-reasoning was CI-refuted — do not repeat). If production code (aggregator/store) is correct, fix is test/fixture isolation; justify with reproduced mechanism. Determinism bar: Go tests + e2e sync x20 isolation + serial + typecheck + vitest + gofmt. | build | 2026-07-07. DONE. Root cause CONFIRMED by reproduction + instrumentation (NOT a production bug; mission's CI-trace "u19-u21 are demo's messages split off" hypothesis REFUTED). The 6 msgs in `other` (mids u19-u21+a19-a21) are other's OWN messages, seeded by `read-position.spec.ts:241-248` (3 turns via promptSession — sorts BEFORE sync alphabetically, shares the fixture backend in serial order). Shared counter (pkg/fixtures/opencode.go `f.counter`) assigns mids across ALL sessions, so u19-u21 sitting between demo's u17,u18,u22 is just creation order, NOT shared content. Production CORRECT (verified empirically via debug specs): aggregator `EnsureMessagesAsync` (aggregator.go:134) threads sid as a stable goroutine param; store `reconcileMessagesLocked` is sid-scoped; client stream.ts `messages.batch` keyed by payload.sessionID. `/fixture/reset` does NOT clear the aggregator cache (proven: demo 6→10→reset+reload still 10), so a reset-based fix would not work. P1-WEB-034's "CI-load DOM-teardown timing" diagnosis CORRECTED — it's deterministic cross-spec pollution. FIX (TEST-ONLY, commit 182f2c67, +11/-6 sync.spec.ts): replaced count-based `.msg` toHaveCount(0) with content-based re-targeting check — assert demo's seeded m1 marker ("Refactor the parser and explain the change.", pkg/fixtures/opencode.go:73, demo-only static seed) is ABSENT after switching to other. Robust to accumulation in either session + aggregator cache state. Commit-reviewer: approve (high conf/low risk, 0 blocking). Verified: serial read-position+sync 5/5 (was failing); sync x20 isolation 60/60; full serial suite 117 passed/1 skipped; vitest 170/170; typecheck clean; gofmt clean; Go pkg/state+aggregator pass (pkg/web TestPlaceholderSelfContained pass after placeholder restore). pkg/web/dist/index.html clean at commit. Reviewer F3 (pre-switch m1 visibility assertion for louder failure) captured to .local/coordinator/tasks/. | P1-WEB-036, P1-WEB-034 |
| P1-CI-001 | done | CI | Add `actions/upload-artifact@v4` step to the `e2e` job in `.github/workflows/ci.yml` (immediately after the `e2e` step) so Playwright failure artifacts (screenshot, video, `error-context.md`, HTML report, traces) are uploaded and downloadable from the run page. Previously the `e2e` job ran `npm run test:e2e` but uploaded nothing — every e2e failure lost its evidence with the runner. | build | 2026-07-07. DONE. One step added (ci.yml:90-97): `if: failure()`, `uses: actions/upload-artifact@v4`, `name: playwright-e2e`, `path: tmp/agent-runs/playwright` (repo-root-relative; grabs both `output/` per-test screenshot/video/error-context.md AND `report/` HTML report + traces — Playwright config `artifactRoot=repoRoot/tmp/agent-runs/playwright`), `retention-days: 14`, `if-no-files-found: ignore`. No `working-directory` on the step (composite action; path is relative to `GITHUB_WORKSPACE`=repo root, which is what we want). Validated: `python3 tmp/validate-ci-yaml.py` — `yaml.safe_load` + asserts on every required field (`if`/`path`/`if-no-files-found`/`retention-days`/`name`) + last-step placement on `e2e` job; passed. Scope respected: no production code, SPA, e2e test, or other workflow touched; `pkg/web/dist/index.html` clean at commit. |  |
| P1-WEB-036 | done | WEB | Diagnose + FIX-IN-PLACE (NO REVERT) two CI failures linked to concurrent commit 840f2f7 ("fix(sync): collapse session-switch cold-load stall from ~14s to ~1s"). #1 Go test `pkg/web/integration_test.go:460` TestStreamAsyncHydration (DETERMINISTIC, reproducible). #2 e2e `web/tests/e2e/sync.spec.ts:47` `.msg` toHaveCount(0) got 6 after demo→other switch (prior P1-WEB-034 mis-diagnosed as teardown-timing flake; CI REFUTED). Mission: verify root cause with reproduction/instrumentation evidence BEFORE editing; STOP + propose CI instrumentation if #2 root cause stays uncertain. Determinism bar: go test ./pkg/web/ green; e2e sync:30 --repeat-each=20 --retries=0 green isolated + serial; typecheck; vitest; gofmt. | build | 2026-07-07. PARTIAL DONE (mission-sanctioned STOP on #2). #1: TEST-ONLY fix. Root cause CONFIRMED by reproduction + server-contract analysis: 840f2f7 changed `pkg/state/store.go::reconcileMessagesLocked` so a cold-load (`!msgLoaded[sid]`) emits ONE `messages.batch` (KindMessagesBatch) instead of per-message `message.upsert`. The async-held fetch in TestStreamAsyncHydration is exactly a cold-load → emits `messages.batch` only → the test's `message.upsert` assertion (:459-461) timed out. This is the INTENDED contract, pinned by `pkg/state/store_test.go::TestColdLoadEmitsSingleMessagesBatch` (cold-load sequence == [KindMessagesBatch, KindMessagesLoaded]; warm reconcile keeps individual upserts). Stream forwarding of `messages.batch` CONFIRMED (server.go sendable() matches `messages.` prefix; batch payload carries plain-text sessionID). FIX: updated integration_test.go :447-469 (doc comment + assertion block) to expect `messages.batch` (the new cold-load reconcile signal) + `messages.loaded`; test intent (reconciled content reaches the same connection after held fetch completes) preserved. NOT a production regression. Verified: `go test ./pkg/web/ -run TestStreamAsyncHydration -count=1` PASS; full `go test ./pkg/web/ -count=1` green (after restoring dist placeholder); `go build ./...` clean; `gofmt -l` clean. #2: STOP — root cause NOT confirmed, NO fix applied (mission-sanctioned: do NOT faith-fix). Reproduction EXHAUSTIVE and ALL GREEN locally: isolation `sync.spec.ts --grep 'switching sessions' --repeat-each=20 --retries=0` 20/20; full serial suite `npx playwright test` 117 passed/1 skipped (sync:30 = test #92 pass); custom throttle spec with CDP Emulation.setCPUThrottlingRate [1,4,8,12,20] + Network.emulateNetworkConditions (80ms RTT, 2MB/s) → at ALL rates incl 20x, `.msg` drops to 0 within 3-28ms of the switch and STAYS 0 for 6s; solid-js 1.9.13 in both lock + local node_modules (NO dependency drift). Root-cause analysis (code paths all robust — none can produce `.msg`=6): ChatView renders `.msg` directly from `messages()` memo (`<For each={messages()}>` at ChatView.tsx:1522, NOT inside a reveal `<Show>`); `messages()` is reactive on `props.sessionId` + `state.messages[id]`; server cold-load for empty 'other' emits `messages.batch{messages:[]}` then `messages.loaded`; promise-gate (stream.ts pendingBatch, module-level, self-deleting via try/finally) awaits batch decode before flipping messagesLoaded; all race orderings yield 0 `.msg` (state.messages['other'] undefined → messages()=[]), never 6. RECONCILE with P1-WEB-034: prior "isolation provably correct" CONFIRMED (20/20 + 117/117 + 20x throttle all green); prior "teardown-timing flake" NOT confirmed (20x throttle far harsher than CI load → instant 0). Neither explains CI. CI failure remains UNEXPLAINED by local reproduction; 840f2f7 prime suspect by recency but no path through its new code yields 6. Status: CI-environment-specific failure, unreproducible locally. Follow-up: CI instrumentation proposed (temp branch adding console.log probes in ChatView messages() memo + stream.ts batch/loaded listeners, captured via page.on('console') dumped on toHaveCount failure → read CI artifact stdout → revert) — captured to `.local/coordinator/tasks/` NOT committed. Files (code): `pkg/web/integration_test.go` (net +9 lines, test-only). pkg/web/dist/index.html restored to committed placeholder (materialized during reproduction). NO REVERT of 840f2f7. | P1-WEB-034 |
| P1-AGG-007 | done | AGG | Two-slice web session-load perf fix (root cause: the store WRITE lock). Slice 1 (`b92ca02`): add `Store.PendingPermissions()` RLock accessor so the per-project-dir 2s `reconcileFailFastPerms` ticker stops calling `store.Snapshot(nil)` — which built ~48MB under the WRITE lock — just to read permissions; consumer `pkg/web/server.go` switched to the new accessor, reject logic byte-identical. Slice 2 (`0e3c9dc`): scope `Store.Snapshot(messagesFor)` per-session structural maps (Sessions/Gate/Questions/Activity/LastAgents/CurrentVerbs/Permissions/Todos/Statuses/Unread) + delta flush to selected sessions when `messagesFor` is non-nil and non-empty; `nil`=firehose and `{}`=tree-owner semantics unchanged. Delta-flush scoping proven safe (`flushPartDeltasLocked` keeps accumulators; convergence test non-vacuous). | build | 2026-07-07. DONE. Files (3, net across both commits): `pkg/state/store.go`, `pkg/state/store_test.go`, `pkg/web/server.go`. Verified: `go build ./...` + `go test ./...` all `ok`; `gofmt -l pkg cmd main.go` clean. Direct continuation of the P1-AGG-001..006 session-load latency series (Slice 2 extends the P1-AGG-004 delta-coalesce flush scoping). | P1-AGG-004 |
| P1-WEB-033 | done | WEB | Re-green two HARD-FAILING Playwright e2e on CI HEAD a07bf6c: `scroll-follow.spec.ts:387` "a content shrink while Live keeps following" and `:864` "tab resume re-engages Live after a hidden content reshuffle". Both fail `expectFollowingTail`'s `button.jump` toHaveCount(0) → got 1. Diagnosis-first; confirm at runtime, then fix. Drain-at-source in `pkg/fixtures/opencode.go` `handleFixtureReset` and/or `PendingInput.tsx`/`ChatView.tsx` blocker gate. Determinism bar = `--repeat-each=50 --retries=0` green in isolation AND in the full serial `scroll-follow.spec.ts` run. Refs P1-WEB-032 (prior fix this regressed/did-not-hold). | build | 2026-07-07. DONE. Root cause REFINED beyond P1-WEB-032: the fixture reset cleared only the FIXTURE maps (pendingQ/pendingQReq/pendingP) but the AGGREGATOR store (pkg/state/store.go — the SPA's snapshot source) held the question until a resolve event or re-hydrate; the serial suite never re-hydrates between spec files, and the reset emitted only `session.idle`. So `interactive.spec.ts:43` (leaks an unanswered `[[ask]] need a decision` for demo, never replies; runs first alphabetically) armed the aggregator store → every later demo open mounted the PendingInput blocker card (a 2nd `button.jump` via PendingInput.tsx:64 when the card scrolled out of view), polluting count assertions during tests 7/`:864`'s content reshuffle. FIX: in `handleFixtureReset`, collect cleared IDs under f.mu, then after Unlock emit `question.replied`/`permission.replied` {sessionID,requestID} for each cleared item (before session.idle) so the aggregator applies KindQuestionClear/KindPermissionClear (store.go:754,721) + forwards question.delete/permission.delete to SPA → snapshot goes clean on next demo open. NEEDED: a disable-emit diagnostic reproduced the original :387/:864/:936 failures (the sibling :936 comment-(14) is also a victim, fixed for free). VERIFICATION (local): repro (interactive:43 + :387 + :864) 3/3 PASS (was 1+2 fail pre-fix); isolation `--repeat-each=50` 100/100 PASS; serial `scroll-follow.spec.ts --repeat-each=3` 42 pass/3 skip; cross-file (interactive+scroll-follow) 25 pass/1 skip; typecheck clean; vitest 163/163; `go build ./...` + `gofmt -l` clean. CI-LOAD CAVEAT (honest): local ×50 green does NOT guarantee CI green — CI load is the recurring confound. UNRELATED pre-existing flakes observed in full-suite runs (:510 viewport-shrink, :658 Live-pill-hides) are mechanically independent of this fix (passed with emit disabled; flaked in some emit-enabled runs = pure load-flake). Group B side-quest: `sync.spec.ts:30` does NOT share the contamination (no button.jump assertion, no [[ask]]/[[perm]]; passes with contamination precedent). Files: `pkg/fixtures/opencode.go`. | P1-WEB-032 |
| P1-WEB-035 | done | WEB | Fix flaky/hard-failing Playwright e2e `terminal: full-screen TUI (vim) stays live — xterm DECRQM stall regression` (`web/tests/e2e/terminal.spec.ts:89`, Group C of the e2e-flake-fix arc). TEST-ONLY fix — production DECRQM handler must NOT be touched. | build | 2026-07-07. DONE. Root cause (confirmed by local repro, refutes the task's "poll matched a non-shell `>`" hypothesis): TWO non-robust signals. (1) The `~`-rendered poll matched STALE scrollback — the shared PTY persists across SPA reconnects and replays its buffer on every `page.goto`, so the poll returned before the freshly-launched vim rendered; the insert-mode marker then landed as vim normal-mode commands and never rendered → marker-poll timeout (the local flake). (2) The tty-reclaim poll waited for a bare `>` as the last non-empty row — environment-specific (matches zsh+powerlevel10k locally but NEVER CI's bash prompt `runner@host:...$`), so it timed out at the same spot in CI (the CI hard fail). b60ff25 (Ctrl+Shift+C/V copy/paste handler) investigated, NOT a contributor (early-returns unless Ctrl+Shift+c/v; also failed pre-b60ff25). FIX (single file `web/tests/e2e/terminal.spec.ts`): added `termKill` helper (POST `/vh/term/kill {dir,id}` + `X-VH-CSRF:1`); kill the leftover shared PTY at test start so the Terminal click spawns a FRESH shell with empty scrollback; unique per-run marker `DECRQM_LIVE_${Date.now()}`; replaced the env-dependent `>` poll with a sentinel-echo + bounded-retry (`echo VH_READY_<ts>` + poll-for-output, 5×/4s, flush+resend on timeout) — environment-independent positive proof the shell reclaimed the tty; preserved `echo VIM_EXITED_OK` witness output-poll as a 2nd independent guard. commit-review: APPROVE 4/4 unanimous (test-only + CSRF + retry contracts verified, no block/split/DEFER). Verified: typecheck clean; vitest 164/164; isolation `--repeat-each=20 --retries=0` 20/20 green (~5.1s, no outliers); full terminal.spec.ts 3/3; cross-file terminal+interactive 14/14. Local green ≠ CI green (vim/tty timing is CI-load-sensitive). Production DECRQM handler (TerminalPane.tsx) NOT touched; pkg/web/dist/index.html restored to committed placeholder. |  |
| P1-HARNESS-001 | done | HARNESS | Harden the commit-gate message-builder so per-line echo can't silently drop body lines. While committing 84251be, the committer agent's per-line `echo >>` scratch construction for the commit-message file silently dropped two body lines on the first pass (file came out 9 lines instead of 11) and one echo hit a UUID-typo'd filename. Both were caught by a read-back verification BEFORE acquire, so the committed artifact is clean, but the per-line-echo path is fragile. Switch the message-builder to a single heredoc / Write-tool path instead of per-line echo appends, eliminating the silent-line-drop class. |  | 2026-07-05. DONE. Implemented as the `stage-message` subcommand (INFRA-GATE-004a) in .opencode/scripts/commit-gate.sh:1345-1414 — switched the message-builder off per-line `echo >> msg-${UUID}` appends to an atomic STDIN/heredoc single-shot capture (cat > sibling-temp + POSIX rename), eliminating the silent-line-drop class flagged on commit 84251be. The committer now stages full multi-line bodies in one stage-message heredoc call; every multi-line commit in the pending-input arc (450e73e1, 835fb44a, 480ca2a3) rendered byte-for-byte via read-back. No code change in this task — bookkeeping closure only. Flagged by the committer agent on commit 84251be. Likely fix site: `.opencode/scripts/commit-gate.sh` and/or the committer agent prompt under `.vh-agent-harness/overlays/` (the harness source, since `.opencode/` is generated). Verify: construct a multi-line commit message via the builder and assert the line count matches the input. |  |
| P1-WEB-031 | done | WEB | Decide pending-input popup dismissal semantics — DECISION ITEM, not yet a code task. Today, card action buttons (QuestionCard Reply; PermissionCard Allow once / Always / Reject) do NOT self-dismiss the popup — the popup closes reactively because the store mutation unmounts the card (DRY, consistent across Q+perm). Decide whether to keep this or switch to modal-style immediate dismissal by wrapping each action in `popup.hide()`. If switching, apply to BOTH cards for consistency. Options: (a) keep reactive-close — current, no change; (b) add `popup.hide()` on action for modal-style dismissal (small change to both `QuestionCard.tsx` + `PermissionCard.tsx`). |  | 2026-07-05. RESOLVED (decision) — keep reactive-close (current behavior); NO code change. Card action buttons (QuestionCard Reply; PermissionCard Allow once/Always/Reject) do NOT self-dismiss the popup; the popup closes reactively because the store mutation unmounts the card (DRY, consistent across both cards). Option (b) modal-style popup.hide() on action rejected — would add code to both cards for negligible UX gain (popup already closes near-instantly once the store mutation propagates). Decision owner: operator. |  |
| P1-WEB-030 | done | WEB | Polish `cardPopup` primitive + popup tests. (F4) Reword the test title at `web/tests/unit/PermissionCard.test.tsx:82` (currently says "…and closes") to match what the body asserts — the popup *stays* mounted in the mocked context (it closes in production via store mutation); cosmetic wording, assertions already correct. (F5) Clarify the `FOCUSABLE` selector comment in `web/src/components/cardPopup.ts:25-29` re: `tabindex="-1"` — the selector excludes ALL `tabindex="-1"` elements, not just the dialog container. (F7) `export const FOCUSABLE` from `cardPopup.ts` and reuse it in `web/tests/unit/QuestionCard.test.tsx` (test currently uses a hand-maintained subset selector that omits `a[href]`/`select`) to prevent drift. Three trivial edits, one micro-commit. |  | 2026-07-05. DONE. Commit 480ca2a3. Three `drop`-advisory follow-ups from the 835fb44a commit-review (P1-WEB-030). F4: reworded the PermissionCard test title to match its body (popup stays mounted in the mocked context; closes in production via store unmount). F5: clarified the FOCUSABLE selector comment in cardPopup.ts — [tabindex]:not([tabindex="-1"]) excludes ALL tabindex="-1" elements, not just the dialog container. F7: exported FOCUSABLE from cardPopup.ts (value unchanged) and reused it in QuestionCard.test.tsx, replacing a hand-maintained local subset that omitted a[href]/select; focusable count unchanged (no a[href]/select in the popup DOM). No behavior, selector-value, or assertion-logic change. Validation: typecheck clean; vitest 155/155; pkg/web/dist/index.html clean at HEAD. |  |
| P1-WEB-002 | done | WEB | Extract isCursorAhead (the monotonic read-cursor invariant) into a pure orderAhead(cand, stored, order) helper in web/src/lib/scroll.ts and add unit-test cases (stored missing → true, cand===stored → false, cand newer, cand older, both absent). Currently a private closure in ChatView.tsx:505-510, untested. | build | 2026-07-05. DONE. Commit c2deb17. Behavior-preserving extraction. Cited line :505-510 (from commit-review of acdef8e) had DRIFTED to :617 on current HEAD (P1-WEB-022 maybeRestore + P1-WEB-026 send paths touched the file); located by content grep for `isCursorAhead`, not the stale line number. Changed: web/src/lib/scroll.ts (+exported pure `orderAhead(cand, stored, order)` — the closure's `sm()?.order` capture became an explicit third param; comparison semantics verbatim: same short-circuit order `!stored → true`, `cand===stored → false`, else `order.indexOf(cand) > order.indexOf(stored)`), web/src/components/ChatView.tsx (call site `flushReadCursor` now threads `sm()?.order ?? []`; removed the closure; updated the prose comment reference `isCursorAhead`→`orderAhead` at the session-switch effect), web/tests/unit/scroll.test.ts (+5 unit tests modeled on the bottommostRead block: stored missing→true, cand===stored→false, cand newer→true, cand older→false, both absent→true). SEMANTIC NOTE (both-absent case): the closure's first short-circuit `if (!stored) return true;` resolves it to `true` before cand is examined — this never fires at runtime (the call site guards `if (!cand) return;` so cand is always a non-empty string when orderAhead is reached), but the test pins the faithful behavior (empty-string cand, undefined stored → true). Verified: `npm run typecheck` clean; `npx vitest run` 160/160 (scroll.test.ts 15→20, exactly +5; total pre-slice was 155 — the task's "prior 142" baseline was stale from the 2026-07-04 P1-WEB-026 closeout); `git diff --exit-code -- pkg/web/dist/index.html` CLEAN (no `make build`/`make web` run; no GPU-expensive CSS). commit-reviewer: APPROVE (4 leaves unanimous, high confidence, low risk; 3 info-DROP findings F1/F2/F3 — none gating). Non-goals respected: did NOT touch bottommostRead/threshold (P1-WEB-010), did NOT touch protected scroll invariants. |  |
| P1-WEB-004 | done | WEB | Perf-safe synchronous read-cursor snapshot on session switch — close the <400ms debounce-window loss for the from-bottom scroll-up case (the gap P1-WEB-001's old comment hid). Must avoid a per-frame layout sweep (Firefox/WebRender heat). | build | 2026-07-05. DONE. Commit a653f67. Closed the <400ms debounce-window read-cursor loss: scrolling UP from the bottom and switching sessions fast (before the 400ms flushReadCursor debounce fired) lost the read position — the switch effect cancelled the pending flush, and by the time the effect ran the DOM had flipped to the entering session so bottommostReadFromDom would have measured the wrong geometry. Fix (ONE logic file, ChatView.tsx, ~15 lines): (1) leading-edge throttled arm-time capture in scheduleReadCursor — bottommostReadFromDom runs at ≤5/sec (200ms gate) on the FIRST arm of each scroll-away burst, stashing {sid, cand} where sid=props.sessionId (the OUTGOING session — layout is settled + correctly attributed at arm time). Perf-safe: reads-only loop, ONE layout flush, categorically different from the GPU re-raster heat saga (no mask-image/backdrop-filter/contain:paint/content-visibility). (2) Switch effect flushes the stash for the OUTGOING session using state.messages[prevId]?.order (NOT sm()?.order — that is the entering session at switch time) with orderAhead monotonic guard. (3) TWO mandatory stash invalidations at the clearReadAnchor sites (flushReadCursor nearBottom + onScrolled atBottom) so a return-to-bottom after a scroll-up does NOT re-apply a stale mid-history anchor on the next switch. REFINEMENTS (both mandatory, study-validated): R1 = the invalidations (without them the stash goes stale); R2 = outgoing-session order (sm()?.order is wrong at switch time). Protected invariants UNCHANGED: a4a7f7c/750562c/4fa8255/P1-WEB-022/P1-WEB-026/P1-WEB-002 — study confirmed no contradictions. LINE DRIFT: NONE — HEAD-3991a18 study line refs matched exactly on current HEAD. TEST DEVIATION: the mission spec's suggested `page.goto` switch mechanism was WRONG (page.goto unmounts ChatView, losing the in-memory armedCand stash and bypassing the fix); tests use in-app popstate for the switch-TO-demo (exercises the stash flush) + page.goto for the reopen (avoids an openSession-clears-messages → onScrolled-clears-anchor timing vulnerability in serial runs) + a DIRECT localStorage readAnchor verification between switch and reopen. Study memo: researches/sources/read-cursor-session-switch-study.md (mechanism, approach validation, perf-safety audit, contradiction audit). Verified: `npm run typecheck` clean; `npx vitest run` 160/160 (baseline unchanged); `unread-dot.spec.ts --repeat-each=5 --retries=0` 25/25 PASS (both new tests 5/5 stable); full e2e serial 112 passed / 3 failed (pre-existing scroll-follow serial flakes :214/:351/:833, pass in isolation) / 1 skipped — both new P1-WEB-004 tests pass in serial. commit-reviewer: APPROVE (4 tier-1 leaves unanimous, high confidence, moderate risk). |  |
| P1-WEB-003 | done | WEB | Add Playwright e2e covering the browser-smoke gap for the scroll read-position feature: (a) stream into a following viewport, scroll up mid-stream, assert the .jump "↓ Latest" button appears; (b) reload with a stored read anchor and assert the viewport lands on the anchored [data-mid] row. | build | 2026-07-05. DONE. Commit c3677ab. Coverage-only: added `web/tests/e2e/read-position.spec.ts` (NEW, 338L, 2 tests) pinning two browser-smoke gaps filed by C-F1 / D-F3 from the commit-review of acdef8e (the P1-WEB-001 scroll read-position feature). GAP (a) = real content stream + mid-stream scroll-up + `.jump` button surviving stream completion — `scroll-follow` test (4) uses `[[stall]]` (5s busy, NO content streaming), so the contentEl ResizeObserver `pin()` loop (ChatView.tsx :820-852) never competes and there's no stream-completion edge; new test (a) fires a REAL prompt (4 chunks ~720ms), scrolls up mid-stream, asserts `button.jump` visible mid-stream AND post-completion (intent latch held through the busy→idle transition). GAP (b) = reload landing on the SPECIFIC anchored `[data-mid]` row via the real debounced `flushReadCursor` path — `unread-dot` (3)/(4) + `scroll-follow` (12) assert only geometry ("not at bottom") or seed the anchor synthetically via `addInitScript`; new test (b) builds an overflowing transcript, scrolls a mid-history row to top (real debounced flush), reads the DYNAMIC anchor id back from localStorage `vh.scroll.v2`, reloads, and asserts ` \| rowTopRel \| <=8px` (exact `maybeRestore` restore target) + notAtBottom + notAtOrigin (triple discriminator). NEW focused spec — the read-position machinery spans both `unread-dot` (anchors + jump button) and `scroll-follow` (streaming + reload) concerns, and both existing specs are already large (422L / 934L); the file sorts alphabetically BEFORE them so the fixture is pristine. NO app-source changes. Verification: `npm run typecheck` clean; `npx vitest run` 160/160; new e2e `--repeat-each=5 --retries=0` → 10/10 pass; full serial e2e 114 passed / 3 failed (the 3 are the known pre-existing `scroll-follow` serial flakes `:214` focus gate / `:351` content shrink / `:833` tab resume — pass in isolation, fail in serial) / 1 skipped — no new failures, no regression. commit-reviewer APPROVED (unanimous 4 leaves, advisory DROPs only). `pkg/web/dist/index.html` clean (restored via `commit-gate.sh revert` after the Playwright webServer materialized it). GOTCHA #2 (the `openSession`-clears-messages → `onScrolled`-clears-anchor timing vuln) NOT hit — test (b)'s reload starts from a clean mount so the self-pin bail protects the anchor through the empty-content window, as predicted. No deviation from spec; no new defect found. |  |
| P1-WEB-029 | done | WEB | Fix residual flakiness in `web/tests/e2e/scroll-follow.spec.ts` test (11) "the Live pill hides when the turn finishes" (`:628-660`). Split from P1-WEB-026 (whose (7)/(8)/(10b) targets are fixed). Diagnosis-first: confirm root cause at runtime BEFORE committing. Test-hardening only; no app-side scroll/RO code changes without runtime evidence; no GPU-expensive CSS (no mask/backdrop/contain/content-visibility). Determinism bar = `--repeat-each=50 --retries=0` green. | build | 2026-07-05. DONE — **Tier 2 (fixture reset)** after Tier 1 proved insufficient. Test (11) **50/50 GREEN** at the serial ×50 bar. Re-diagnosis (beyond the original researcher memo) found TWO cross-test contaminants in the shared serial fixture backend (`pkg/fixtures/opencode.go`): (1) the leaked `[[stall]]` busy goroutine (the original memo's mechanism) AND (2) **transcript message accumulation** — `simulatePrompt` persists user (+assistant) messages to the store, so ~50 serial iterations × 5 sending tests (4/9/10b/11/12) accumulated hundreds of uneven-height messages; the long uneven transcript made test (11)'s focus-toggle reflow shift the viewport just off-bottom → `following()` flipped false → the focus-gate `.chat-live` re-show assertion (`:668`) failed (NOT the disappearance assertions contaminant 1 predicts). PROBE RESULTS: Probe 1 (isolation ×50, pre-fix) 50/50 GREEN (serial-only confirmed); Probe 2 (full serial ×50, `--retries=0`) pre-fix = **66 failed** (test 11 + test 6, `:628`/`:267`); post Tier 1 (test-only `beforeEach` wait-for-idle + de-dup) = **50 failed** (4× test 11 + 45× test 6 + 1× test 8 — Tier 1 absorbed contaminant 1 but NOT contaminant 2); post Tier 2 = **51 failed** with **test 11 ZERO failures** — the 51 are 50× test 6 (`:305`, pre-existing deterministic hard-bug, fails in isolation on clean HEAD — filed as P1-WEB-032) + 1× test 12 (`:744`, stochastic stall-busy-emit vs reload race at ~2%). Full e2e suite (`npx playwright test`) with the fix: **114 passed / 1 skipped / 3 failed** = scroll-follow `:254`/`:387`/`:864` (= the known pre-existing `:214`/`:351`/`:833` flakes, shifted +40 by the `beforeEach` insertion) — **ZERO new failures** (verified by 3-way comparison vs clean HEAD: 113/1/4 with the same known flakes + a stochastic sync:30). FIX (Tier 2, DEVIATION from the mission's gen-counter proposal — justified because the gen-counter alone would NOT fix contaminant 2): `pkg/fixtures/opencode.go` adds a `baseline` snapshot taken at the end of `New()` + a `POST /fixture/reset?session=demo` route (`handleFixtureReset`) that restores messages from baseline + clears `busy` + emits `session.idle`; the spec's `test.beforeEach` calls it via the `request` fixture (NOT `page.request` — that resolves against `about:blank` before any `goto`) with `X-VH-CSRF: 1` + `expect(reset.ok()).toBe(true)`. Tier 1 de-dup retained (demo-based tests omit their own VP/goto/msg.first; tests 8+12 keep own goto). NO gen-counter needed: a stale stall goroutine emits busy ONCE before its sleep (before reset) and its only post-reset action is the deferred `session.idle` (harmless). Files: `pkg/fixtures/opencode.go` (~34 lines, test infra, never shipped), `web/tests/e2e/scroll-follow.spec.ts` (`beforeEach` + de-dup), `researches/sources/scroll-follow-test11-diagnosis.md` (diagnosis memo). Verified: `npm run typecheck` clean; `npx vitest run` 160/160; `go build ./...` OK; `go vet` pkg/fixtures + tools/fixtureserver clean; `git diff --exit-code -- pkg/web/dist/index.html` CLEAN (restored after every e2e run via `commit-gate.sh revert`). Protected invariants UNTOUCHED (a4a7f7c/750562c/4fa8255/P1-WEB-022/P1-WEB-026/P1-WEB-002/P1-WEB-004). | P1-WEB-026 |
| P1-WEB-026 | done | WEB | Fix residual flakiness in `web/tests/e2e/scroll-follow.spec.ts` (follow-up to P1-WEB-022/023/024). Three targets: test (11) "the Live pill hides when the turn finishes" (`:628-660`), test (7) "a content shrink while Live" (`:351-442`), test (8) "a viewport shrink while Live" (`:476-510`). Diagnosis-first: confirm root cause at runtime BEFORE committing. Test-hardening only; no app-side scroll/RO code changes for (7)/(8) without runtime evidence; no GPU-expensive CSS. Determinism bar = `--repeat-each=50 --retries=0` green. | build | 2026-07-04 (10b) FIXED this slice (the "fix (10b) app bug first" option from the prior BLOCKER below). Mechanism CONFIRMED via differential probe (H1): `sendParts` (`ChatView.tsx:1193`) + `runShell` (`:1312`) called `jumpToLatest()` UNCONDITIONALLY at send → cleared the `userScrolledUp` intent latch + yanked to tail synchronously, defeating it. The busy-edge self-heal (`:415`) is INNOCENT — Probe V2 (direct `/oc/session/demo/prompt_async` bypassing jumpToLatest): 20/20 NOT yanked; Probe V1 (Enter-send, jumpToLatest runs): 15/15 yanked when the send path was genuinely exercised (5/20 hit the queue-path confound = the in-suite mechanism, not a contradiction). FIX: gate both call sites `if (!userScrolledUp()) jumpToLatest();` + explanatory comments (mirrors the self-heal's own `:415` gate; `userScrolledUp` chosen over `following()` so a transient content-shrink clamp still re-glues). Verified: `npm run typecheck` clean; `vitest` 142/142; (10b) `--repeat-each 20 --retries 0` → 20/20 (was 18/20 fail from clean idle); full `scroll-follow.spec.ts` 13 pass / 1 pre-existing skip / 1 fail = (11) only (NO regression in the send-while-following path); `unread-dot`+`chat-controls-gating` 6/6. The (10b)↔(11) coupling from the prior BLOCKER is now resolved — an (11) drain-at-source fix no longer exposes a latent (10b) failure. REMAINING: only (11) Live-pill-hides (`:628`) still flakes; separate future slice, still needs operator direction on the (11) fix approach. —— PRIOR 2026-07-03. BLOCKED after full runtime diagnosis — NO clean test-side fix exists for any of the three targets; all need operator direction. (7) content-shrink: 0/50 — Hypothesis T DISPROVED (diag: `readyBeforeGlue:true`+`preShrink_delta:0` every run; `.ready`-gate would be a no-op); does not manifest here. (8) viewport-shrink: FIXED 2026-07-04 (option 8a — app-side one-line fix + comment rewrite; HEAD 93dfcd1). Mechanism (was ~6/50 flake): viewport shrink fires a stray ≤1px scroll event (browser sub-pixel/layout rounding) that defeated the STRICT self-pin bail `scrollEl.scrollTop === pinnedTop` (`ChatView.tsx:878`) → onScrolled set following=false + armed the intent latch → the scrollElRO re-pin (`:860`) gated on `following()` was skipped → permanent Live loss for the full turn (reachable in production on every mobile-keyboard open). Contradicted the `:851-860` comment (claimed 'no scroll event fires on a shrink'). FIX: (1) bail now tolerates ≤1px drift — `if (scrollEl && following() && Math.abs(scrollEl.scrollTop - pinnedTop) <= 1) return;`; (2) the false `:851-860` comment rewritten to document the stray event + the tolerance. Safe: a genuine ≤1px user scroll stays inside the 24px nearBottom band → atBottom/latch classification unchanged; the tolerance cannot absorb a real scroll-away. Touches NO protected invariant (a4a7f7c/750562c/4fa8255/Deferred/messagesError untouched); NO GPU-expensive CSS. VERIFIED: `npm run typecheck` clean; `vitest` 142/142; `scroll-follow.spec.ts -g 'viewport shrink' --repeat-each=20 --retries=0` BEFORE 2 fail/18 pass → AFTER 20/20; `unread-dot`+`chat-controls-gating` 6/6. Durable plan/decisions: researches/sources/scroll-follow-app-bugs-study.md. (11) Live-pill-hides: cross-test clobber CONFIRMED but brief's Option A PROVEN INSUFFICIENT at runtime (3/20 unchanged — working already false from (10b)'s idle, guard passes instantly, (9)'s 5s idle still pending). The correct fix (drain-at-source) EXPOSES a PRE-EXISTING latent app bug in (10b): (10b) fails 18/20 from clean idle (repeat 1, body untouched) — `following` re-engages during its own fast turn despite the `userScrolledUp` latch (self-heal `ChatView.tsx:410-419` should suppress but doesn't; exact mechanism needs app instrumentation). (10b) only passes in-suite by INHERITING (9)'s leaked BUSY state. So (11) fix and (10b) are COUPLED. BLOCKER: operator must choose — (a) magic-timeout workaround in (10b) tail [hacky, keeps (10b) vacuous], (b) drain (9) + fix (10b) app bug in separate slice first, (c) ref-count `state.activity` (Option B app-wide), (d) fixture reset/beforeEach (Go change). All test changes reverted (spec at HEAD); diagnostics removed. Earlier note: STARTED on HEAD `93dfcd1` (task brief said `750562c`; an unrelated build commit `93dfcd1` landed on top — does not touch scroll-follow/spec/ChatView/selectors/stream/opencode.go). Backlog ID: brief said P1-WEB-025 but that is already used+done for the hybrid build-config, so using next-free P1-WEB-026. CLOSEOUT 2026-07-04: targets (7)/(8)/(10b) fixed and verified this slice; remaining target (11) "Live pill hides when the turn finishes" split to P1-WEB-029. | P1-WEB-022, P1-WEB-023, P1-WEB-024 |
| P1-WEB-028 | done | WEB | Release-readiness fix slice for dist-build model (ship-review of 93dfcd1). (Fix1) Docker embed path: Dockerfile/Dockerfile.e2e COPY source `/app/pkg/web/dist`→`/app/web/dist-build` to match vite `outDir` (dist-build); raw COPY chosen over in-stage materialize.sh because Docker builds start from a fresh checkout. (Fix2) README.md + web/README.md corrected from the old prebuilt-UI-committed model to the shipped hybrid model (plain `go build` embeds a self-contained placeholder banner, not the real SPA; `make build` materializes). (Fix3) memo cross-ref P1-WEB-026→027 (026 taken by the scroll-follow task at memo-write time). (Fix4) Dockerfile Go base `golang:1.23-alpine`→`golang:1.25-alpine` to match go.mod `1.25.0`. | build | 2026-07-04. DONE. ship-review found the Dockerfiles were the 5th embed-producing surface missed by 93dfcd1's materialize refactor (Makefile fixtures, release.yml, fixture-web.sh were covered; Dockerfiles were not). Changed: Dockerfile (COPY path L19 + Go 1.25 L13 + stale stage-1 comment L4), Dockerfile.e2e (COPY path L19 + Go 1.25 L14), README.md (blockquote L78-84 + inline comment L173), web/README.md (build section L13-19), researches/decisions/dist-build-hybrid-findings-93dfcd1.md (026→027 L91), docs/planning/backlog.md (this row + normalizer). Verified: static Docker trace (COPY src `/app/web/dist-build` == vite outDir `dist-build`; Go image 1.25 ≥ go.mod 1.25.0; stage order `npm run build`→`/app/web/dist-build`→COPY into `./pkg/web/dist`→`go build` embeds — correct); `go build ./...` PASS; `go test ./pkg/web/` PASS (cold placeholder path); docs re-read matches hybrid model. Docker daemon build not run (static-only) — see closeout. |  |
| P1-WEB-027 | done | WEB | dist-build hardening (follow-up to P1-WEB-025 hybrid model). (B1) Harden `pkg/web/placeholder_test.go` to read the COMMITTED `dist/index.html` from DISK (not the embed FS) and remove the early-return on `/assets/` — closes the real CI gap where a `git commit -a` after `make build` (committing the materialized SPA shell) passed CI but shipped a cold-clone that 404s on `/assets/*`. (B5) Extract `web/scripts/materialize.sh` containing the `cp -r` materialize + a clean-first `rm -rf` of stale generated outputs; dedupe ALL 4 former copy sites (`Makefile` `web-materialize`, `Makefile` `fixtures`, `.github/workflows/release.yml`, `web/scripts/fixture-web.sh`) — the study undercounted as 3; the `fixtures` inline copy was the hidden 4th. (B4) The clean-first step IS B4, folded into the shared script (local-dev hygiene + avoids binary bloat from orphaned hashed assets; safe because `web/public/sw.js` deletes old caches on activate). (B2) Document the conscious WCAG 1.4.4 zoom-lock tradeoff as a comment above the viewport meta in `web/index.html` (NO behavior change — `maximum-scale=1.00, user-scalable=no` is an intentional app-like PWA decision; iOS Safari ignores it, Android Chrome honors it). | build | 2026-07-04. DONE. Based on researcher study of commit 93dfcd1 (3 fix-now items + 1 comment). Worked on HEAD c680b94 (two scroll-follow commits past 93dfcd1; hybrid-model files unchanged). ID NOTE: brief suggested `P1-WEB-026` but that ID is taken (blocked scroll-follow task) — used next-free `P1-WEB-027`. Changed: `pkg/web/placeholder_test.go` (read-from-disk + no early-return), `web/scripts/materialize.sh` (NEW), `Makefile` (`web-materialize`→script; `fixtures`→depends on `web-materialize`, dropped inline build+copy), `.github/workflows/release.yml` (materialize step→script), `web/scripts/fixture-web.sh` (inline cp→script), `web/index.html` (viewport comment only), `docs/planning/backlog.md` (this row + normalizer archive). DEV-EXPERIENCE TRADEOFF: after a local `make build`, `go test ./pkg/web/` FAILS until `commit-gate.sh revert pkg/web/dist/index.html` restores the placeholder — INTENTIONAL (enforces "don't commit the materialized shell"). Verified: gofmt clean; `go test ./pkg/web/` PASS; `go test ./...` PASS (all pkgs); `go build ./...` PASS; `make web` leaves `pkg/web/dist/index.html` clean (`git diff --exit-code`=0); `make build` materializes real SPA (embedded index.html has `/assets/`, assets/ dir present); Playwright e2e 113 passed / 1 skipped (test 76 pre-existing skip, needs fixture hook); `git grep "cp -r web/dist-build"` → zero matches in executable code (recipe parameterized in materialize.sh, all 4 sites deduped); `git status` clean (placeholder restored via committer revert). RESIDUAL: (1) post-`make build` test-fail-until-restore is intentional; (2) zoom-lock is a documented conscious WCAG tradeoff, not a defect. | [dist-build-hybrid-findings-93dfcd1.md](../../researches/decisions/dist-build-hybrid-findings-93dfcd1.md) |
| P1-WEB-022 | done | WEB | Fix: a stored mid-history read anchor was lost when reopening a BUSY (mid-hydration) session — the viewport was yanked to the live tail. Root cause in `web/src/components/ChatView.tsx::maybeRestore()`: the empty-order guard (`if (!sm()?.order?.length) return false`) only defers while NO message has arrived, then `restoredFor = props.sessionId` was set BEFORE the `order.includes(anchor)` check. Lazy hydration streams a PARTIAL snapshot (the store's `reconcileMessagesLocked` emits one `message.upsert` per message in a loop) BEFORE `messages.loaded` flips `delivered()`, so an async contentEl ResizeObserver (which calls maybeRestore) could fire when `order=["m1"]` (length truthy → guard passes) but the seeded anchor (e.g. m4) was not yet present → fell into the stale-anchor else-branch (`setFollowing(true); setUserScrolledUp(false); pin()`), AND `restoredFor` was already set → every later RO call bailed at the top → view stayed pinned at the tail. Fix (researcher "Option B", single file, ~3 logic lines in maybeRestore): (1) add an early `return false` when `anchor != null && !order.includes(anchor)` — BUT gated on `!delivered()` so a GENUINELY-stale anchor (post full-delivery, id simply absent from history) still falls through to the existing pin-to-bottom instead of wedging the view; (2) move `restoredFor = props.sessionId` to AFTER that deferral check so a no-op deferral never locks out later retries. Per-message upserts are all processed before `messages.loaded` (FE reducer is sequential), so once `delivered()` is true the anchor is guaranteed in `order` if it exists at all. NO GPU-expensive CSS. No reveal-gate/Deferred/styles/scroll.ts/Go change. | build | 2026-07-03. DONE. Pre-existing bug (downstream of 4fa8255 reveal gate + fc135918 messagesError lifecycle — neither the cause). Changed: `web/src/components/ChatView.tsx` (maybeRestore: hoisted `const order = sm()?.order ?? []` reused for both the deferral + the `el && order.includes(anchor)` restore test, dropping the prior inline recompute; +1 deferral line `if (!order.includes(anchor) && !delivered()) return false;` placed before the `restoredFor` assignment; restoredFor now set only after the deferral check; null-anchor branch + genuinely-stale-anchor else-branch + following/userScrolledUp/ready/ack/self-heal coordination all UNCHANGED), `docs/planning/backlog.md`. Verified: `cd web && npm run typecheck` clean; `npx vitest run` 142/142 (16 files); Playwright 3-spec gate (`scroll-follow` + `unread-dot` + `chat-controls-gating`): TARGET test (12) "reopen of a busy session at a stored anchor is not yanked to the tail" now PASSES (was failing 3/3 pre-fix on clean 4fa8255); 19/21 passed in the full run, the single failure was test (17) "tab resume re-engages Live after a hidden content reshuffle" which passed 2/2 in isolation — a pre-existing timing flake on a DIFFERENT code path (the contentEl RO re-pin guard ~:799-807, not maybeRestore), unrelated to this fix; chat-controls-gating 3/3 + unread-dot 3/3 green. No frontend build needed; `pkg/web/dist/index.html` left clean (e2e webServer dirtied it transiently → restored via `commit-gate.sh revert`). Commit routed through committer (pending). | P1-WEB-020, P1-WEB-021 |
| P1-WEB-025 | done | WEB | Hybrid build-config: SPA builds to a gitignored staging dir (`web/dist-build/`); a self-contained fallback placeholder `pkg/web/dist/index.html` (no `/assets`/`/sw.js`) stays tracked for cold-clone compile safety; embed-producing targets (`make build`/`install`/`fixtures`, release) materialize staged assets → `pkg/web/dist/` before `go build`. No build tags. Fixes the "every `make web` dirties the tracked placeholder" clobber (Vite owned `pkg/web/dist` as both outDir and Go embed source). | build | 2026-07-03. DONE. Design = hybrid staging outDir + explicit materialize + self-contained placeholder (no build tags). `make web` now writes the SPA to gitignored `web/dist-build/` (Vite `outDir` + `sw.js` path), so it never clobbers the tracked `pkg/web/dist/index.html` — rewritten as a fully self-contained fallback banner (no `/assets`/`/sw.js` refs) keeping cold `go build`/`go test` working with NO frontend build. Embed-producing targets (`make build`/`install`/`fixtures`, release.yml) materialize (`cp -r web/dist-build/. pkg/web/dist/`) immediately before `go build`. CI guard (ci.yml web job) fails if `npm run build` dirties `pkg/web/dist/index.html`; unit test `TestPlaceholderSelfContained` locks placeholder self-containment. Supersedes P1-DOCS-001 root cause (the `git checkout` restore verb is now `make web`-clean by design; residual `make build` local-dirty documented with `commit-gate.sh revert` for agents). Changed: `web/vite.config.ts`, `Makefile` (+`web-materialize`), `web/scripts/fixture-web.sh`, `.github/workflows/{ci,release}.yml`, `.gitignore`, `pkg/web/dist/index.html`, `pkg/web/static_test.go`, `pkg/web/placeholder_test.go` (NEW), `.vh-agent-harness/AGENTS.mission.md` + `AGENTS.md` (rendered mirror). Verified: gofmt clean; `go test ./...` PASS (cold-clone, placeholder); `go build ./...` OK; `make web` leaves `pkg/web/dist/index.html` clean (banner, no vite artifacts; `web/dist-build/` gitignored); `make build` embeds real SPA (55 assets, `TestServesEmbeddedSPA` passes via `/assets/` branch); Playwright 113 passed / 1 skipped; `TestPlaceholderSelfContained` PASS; CI guard logic (`git diff --exit-code` post-build) clean. | P1-DOCS-001 |
| P1-WEB-023 | done | WEB | E2E coverage for the chat reveal gate (the zero-coverage blind spot downstream of 4fa8255 reveal gate + fc135918 messagesError lifecycle + a4a7f7c scroll-anchor). Playwright `toBeVisible` cannot detect `opacity:0`, so the gate (`.chat-content { opacity: 0 }` → `.chat-content.ready { opacity: 1 }` via `revealed() = ready() && (delivered() \|\| messageFailed())` in ChatView.tsx) had ZERO e2e coverage, and the e2e fixture (`pkg/fixtures/opencode.go` handleSession GET) returned messages instantly — so the partial-hydration window was unobservable in e2e (the hold existed ONLY in the Go integration-test fake `pkg/web/integration_test.go` msgHold). Slice = coverage-only, NO gate/ChatView/styles/Go-logic change. (A) Add a slow-hydration fixture mode: seed a `slow` ROOT session (4 messages) and add a BOUNDED GET delay in handleSession — `if id == "slow" && r.Method == http.MethodGet { time.Sleep(900 * time.Millisecond) }` (900ms < 1.5s bound), placed OUTSIDE `f.mu.Lock()` so it can't block `/session` list / `/event` emit() / cold-seed fan-out; keyed off session id (NOT prompt text like `[[stall]]`) with a documented rationale, since the partial snapshot is produced by the aggregator stream on session open, not by a prompt. (B) New spec `web/tests/e2e/reveal-gate.spec.ts`: opens `/?session=slow`, asserts `getComputedStyle(.chat-content).opacity === '0'` + `not.toHaveClass(/\bready\b/)` during the ~900ms hydration window, then `toHaveClass(/\bready\b/)` + `opacity === '1'` after reveal — using `expect.poll` (NOT `toBeVisible`, which is blind to opacity:0). | build | 2026-07-03. DONE. Changed: `pkg/fixtures/opencode.go` (seed `slow` session + bounded GET delay), `web/tests/e2e/reveal-gate.spec.ts` (NEW). Verified: `go build ./...` ok; web `npm run typecheck` clean; `npx vitest run` 142/142; `npx playwright test reveal-gate.spec.ts` PASS (observed both hidden→revealed); no cross-contamination — chat-controls-gating + unread-dot FULLY PASS, scroll-follow test 12 (deterministic target) PASS; test 16 is a PRE-EXISTING HEAD `a4a7f7c` failure (proven via swap-to-clean-HEAD repro: fails identically without my change; it is the a4a7f7c regression test, not my regression), test 17 known flake PASS. `slow` is slow-only (no other spec uses `session=slow`) and is a ROOT so doesn't perturb the `1 idle` child-count assertion in smoke.spec.ts. No GPU-expensive CSS. |  |
| P1-WEB-020 | done | WEB | Gate the ChatView visual transcript reveal on full message delivery (`messages.loaded` / `delivered()`) — the O2 GPU-safe fix for the "transcript visibly grows top-down on a large session" symptom. Slice-C async hydration streams a PARTIAL snapshot (`messagesLoaded=false`) → `message.*`/`part.*` deltas → final `messages.loaded`; without a reveal gate the transcript populated progressively while already visible. Add a SEPARATE visual-reveal gate (`revealed() = ready() && (delivered() \|\| messageFailed())`) layered on top of the existing `ready()` positioning semantics (left untouched — still drives scroll-restore/self-heal/ack). Keep layout measurable (opacity:0 preserves geometry). Handle failed hydration (`messages.error`) so the UI cannot hang forever if `messages.loaded` never arrives. NO GPU-expensive CSS (no mask/backdrop/contain/content-visibility). No `Deferred` change. | build | 2026-07-02. DONE. O2 path (O1 rejected: `ready()` entangled with scroll-restore/self-heal/ack). O3 (bounded rAF settle-and-pin) NOT needed — the gate holds reveal until `messages.loaded`, eliminating the progressive top-down growth; residual e2e noise is pre-existing scroll-follow timing flakiness (unrelated to reveal logic). Changed: web/src/sync/store.ts (+`messagesError` field+init), web/src/sync/actions.ts (project-switch reset), web/src/sync/stream.ts (`messages.error`→set, `messages.loaded`/delivered-snapshot/`session.delete`→clear), web/src/components/ChatView.tsx (`revealed()` memo + 3 visual sites + Switch/Match empty-state w/ error hint), web/src/styles.css (`.ready` comment + GPU-cheap `.chat-empty.chat-error`), docs/planning/backlog.md. Verified: `npm run typecheck` clean; `npx vitest run` 139/139 (Deferred 3/3 green); Playwright unread-dot + chat-controls-gating FULLY PASS; scroll-follow has pre-existing timing flakiness (clean-tree 3-spec run had MORE failures (3) than gate-ON (1-2) → no deterministic regression). Partially addresses P1-WEB-015 (operator-visible error signal now exists; interactive retry chip still deferred). commit-reviewer: approve (medium conf, low risk, 0 BLOCK). Follow-ups: stale-`messagesError`-on-retry edge (F3/F4) + missing unit tests for the `messagesError` lifecycle (F5) + stale test comment (F6). |  |
| P1-WEB-021 | done | WEB | Follow-up slice to P1-WEB-020 (the chat-reveal gate): fix the `messagesError` lifecycle gap + add its unit tests + fix a stale comment. (F3/F4) The partial-snapshot branch of `applySessionSnapshot` (the client-side "hydration attempt started" signal — fires for BOTH `openSession`-driven hydration AND Stream-2 transport reconnect retries, which retry WITHOUT calling `openSession`) cleared `messagesLoaded` but NOT `messagesError`, so reopening an errored session OR retrying hydration left a stale `messagesError=true` during the in-flight retry → the reveal gate (`revealed() = ready() && (delivered() \|\| messageFailed())`) hit `messageFailed()` and showed the "select again to retry" hint WHILE a retry was already in flight (misleading but transient). ONE-line fix at the single correct reset point (the partial-snapshot branch) — NOT `openSession` (would miss the reconnect path); no daemon `messages.started` event exists (only `messages.loaded`/`messages.error`), so a proactive client clear is the only mechanism. (F5) Unit tests for the full `messagesError` lifecycle + a `beforeEach` hygiene fix (`messagesError` was leaking across tests). (F6) Stale comment/test-name: post-4fa8255 the reveal gate OPENS on error, so "keeps loading UI up" misdescribed the visual behavior. Sync-layer + tests only — no ChatView/Deferred/styles/Go change. | build | 2026-07-02. DONE. Changed: `web/src/sync/stream.ts` (+`delete s.messagesError[id]` in the `if (loaded===false)` partial-snapshot branch of `applySessionSnapshot`, wrapped in `produce` mirroring the sibling else-branch clear; deliberately NOT added to `openSession`), `web/tests/unit/applySnapshot.test.ts` (beforeEach +`messagesError` reconcile({}) reset; +3 new tests — `messages.loaded clears a prior messagesError`, `a DELIVERED session snapshot clears a prior messagesError`, `a PARTIAL snapshot (gate.messagesLoaded===false) clears a prior messagesError` = F3/F4 regression guard; extended the `messages.error` test with a `messagesError` assert + renamed it + rewrote the stale comment per F6; extended the `session.delete` test with a `messagesError` prune assert). Verified: `npm run typecheck` clean; `npx vitest run` 142/142 (`applySnapshot.test.ts` 18/18, was 15 — +3 new); Playwright `chat-controls-gating` 3/3 + `unread-dot` 3/3 PASS; `scroll-follow` 13 pass + 1 pre-existing skip ("needs fixture hook") + 1 FAIL ("reopen of a busy session at a stored anchor is not yanked to the tail") — CONFIRMED PRE-EXISTING on clean HEAD `4fa8255` (fails 3/3 identically WITH and WITHOUT this slice's changes; fails at the scroll-anchor pre-assertion `scrollHeight-scrollTop-clientHeight<24`, pure `scroll.ts`/localStorage behavior untouched by this sync-layer change — `messagesError[id]` is never set in that normal busy-reopen path so the new `delete` is a no-op there). No frontend build needed; `pkg/web/dist/index.html` left clean (fixture-web build dirtied it transiently → restored via `commit-gate.sh revert`). Commit routed through committer (pending). | P1-WEB-020 |
| P1-AGG-002 | done | AGG | Slice — fan out `hydrate` trailing upstream calls concurrently (memo #2). `pkg/aggregator/aggregator.go`: collapse serial `SessionStatuses`/`ListQuestions`/`ListPermissions` into 3-way `sync.WaitGroup` fan-out; errors swallowed + `log.Printf` (decision (a)). GATE confirmed `opencode.Client` goroutine-safe. | build | 2026-07-01. DONE. Changed: `pkg/aggregator/aggregator.go` (`hydrate`: 3 serial enrichment GETs `SessionStatuses`/`ListQuestions`/`ListPermissions` → 3-way concurrent `sync.WaitGroup` fan-out, ctx-bound, `defer wg.Done` panic-safe; error semantics = swallow+`log.Printf` (decision (a)) preserving prior best-effort behavior — NOT surfaced, so `/vh/reload` semantics unchanged), `pkg/aggregator/aggregator_test.go` (+`TestHydrateFansOutStatusQuestionsPermissionsConcurrently` deterministic concurrency proof; +`TestHydrateSwallowsEnrichmentErrors`). GATE confirmed `opencode.Client` goroutine-safe. Verified: `go build`/`vet`/`gofmt` clean; `go test ./pkg/aggregator/` ok; `go test -race ./pkg/aggregator/` ok. No commit. Decision memo: `researches/decisions/session-load-residual-speedups.md` (#2). | [session-load-residual-speedups.md](../../researches/decisions/session-load-residual-speedups.md) |
| P1-WEB-016 | done | WEB | Slice — parallelize `refreshOpenSessions` reconnect message refresh (memo `researches/decisions/session-load-residual-speedups.md` #1, variant a). Replace the serial `await fetchSessionMessages(id)` loop over `Object.keys(state.messages)` (`web/src/sync/stream.ts`) with `Promise.all(...)` (per-session try/catch) so N open sessions refresh concurrently on tree reconnect instead of N serial `/vh/snapshot` round-trips. FE-only, no server contract change. Tests: `web/tests/unit/refreshOpenSessions.test.ts`. | build | 2026-07-01. DONE. Changed: `web/src/sync/stream.ts` (`refreshOpenSessions`: serial `await`-in-loop over `Object.keys(state.messages)` → `Promise.all` with per-session try/catch preserving error isolation; exported fn for test), `web/tests/unit/refreshOpenSessions.test.ts` (+4 tests: concurrency latch proof, active-session skip, per-session error isolation, no-op empty). FE-only, no server contract change. Verified: `npm run typecheck` clean; `vitest` 139/139 (was 135). No commit. Follow-up: `state.messages` LRU pruning is a separate memory/correctness trade-off (not implemented). Decision memo: `researches/decisions/session-load-residual-speedups.md` (#1, variant a). | [session-load-residual-speedups.md](../../researches/decisions/session-load-residual-speedups.md) |
| P1-AGG-001 | done | AGG | Slice C — lazy async selected-session hydration. Remove the SYNCHRONOUS full upstream `client.Messages` fetch from the session-selection (first-open) path (handleStream → ensureMessages → EnsureMessages) so selecting an unloaded session is fast on first open. Approved design = O2 lazy async hydration with explicit completion events. (1) State: add a `MessagesLoaded` gate field (=`msgLoaded` strictly; keep `Hydrated`), new event kinds `messages.loaded`/`messages.error`, exported store emitters. (2) Aggregator: `EnsureMessagesAsync` — per-session single-flight in-flight map bound to `a.runCtx`; emit completion unconditionally (even on zero/no-diff), emit error + leave unloaded on failure. (3) Server: replace the blocking Stream-2 pre-snapshot call with the async trigger (keep subscribe-before-snapshot; keep `handleSnapshot` sync); make `messages.*` events session-scoped in `sendable`. (4) FE: Stream-2 snapshot sets `messagesLoaded[id]=true` only when the gate says loaded; handle `messages.loaded`/`messages.error`. | build | 2026-07-01. DONE. Changed: pkg/state/store.go (const kinds, GateFacts.MessagesLoaded, snapshot gate, EmitMessagesLoaded/EmitMessagesError), pkg/aggregator/aggregator.go (msgMu+msgInflight, EnsureMessagesAsync, waitMessagesAsync), pkg/web/server.go (triggerMessageLoad, handleStream async branch, sendable messages. prefix), web/src/types.ts (GateFacts.messagesLoaded), web/src/sync/stream.ts (applySessionSnapshot extracted, messages.loaded/error cases, listener kinds, applyMessageEvent exported), pkg/state/store_test.go (+2), pkg/aggregator/aggregator_test.go (+5), pkg/web/integration_test.go (+TestStreamAsyncHydration, hold mechanism), web/tests/unit/applySnapshot.test.ts (+7). Verified: go build/vet/test ./... ok; go test -race ./pkg/aggregator/ ./pkg/state/ ./pkg/web/ ok; gofmt -l clean; web typecheck clean; vitest 135/135 (was 127). No commit. |  |
| P1-AGG-003 | done | AGG | Event-delivery latency fix (Option A — filtered store subscriptions). Push the tree-only vs active-session interest filter UPSTREAM from the SSE egress `sendable()` into `store.SubscribeWith(...Interest{MessageSessions})`, so a structural-only (tree) SSE stream never enqueues irrelevant `message.*`/`part.*`/`messages.*` token floods (re-emitted as `part.upsert`) that it would discard anyway — eliminating the "session appeared late behind a token flood" latency. Backward-compatible: `Subscribe(buffer)` kept as `SubscribeWith(buffer, Interest{})` (firehose), zero call-site changes. NOTE on ID: the task brief said `P1-AGG-002`, but that ID is already `done` (hydrate fan-out concurrency, 2026-07-01, row above) — used next-free `P1-AGG-003` to obey the "no ID reuse" backlog rule (COORDINATOR DECISION — flag if the brief's ID is required for cross-doc linking). HELD follow-ups (measured, NOT this slice): Option C — delta coalescing / reduce the `appendPartDeltaLocked` reducer JSON cost; Option D — move fanout outside `s.mu`. | build | 2026-07-01. DONE. Changed: `pkg/state/store.go` (`subs map[int]*subscriber` + `subscriber{ch,interest}` struct; `Interest{MessageSessions}` type + `wants(kind,sid)` single kind→class map + `isMessageClassKind` (exact Kind constants, no string-prefix) + `payloadSessionID` (one unmarshal/emit); `SubscribeWith(buffer,Interest)` + `Subscribe` kept as backward-compat wrapper; `emit`/`EmitNotice` filter via `sub.interest.wants` BEFORE the nonblocking send, preserving close+remove on full for INCLUDED events, resolving payload sessionID ONCE/emit; `New` inits `map[int]*subscriber{}`), `pkg/web/server.go` (`handleStream`: builds `state.Interest{MessageSessions: filter}` → `store.SubscribeWith(256, interest)`; downstream `sendable` kept as defensive compat check; gap protection + replay path unchanged), `pkg/state/subscribe_interest_test.go` (NEW — gates 1-4 + benchmarks gate 5), `pkg/web/integration_test.go` (+`TestStreamTreeOnlySurvivesTokenFlood` end-to-end wiring proof + helpers). Invariants preserved: subscribe-before-replay/snapshot gap protection; nonblocking fanout (full channel → close+remove, never blocks producer); active-session contract (A gets A's message/part, not B's); notice/ping unchanged; no FE/protocol payload change. Verified: `go build ./...` ok; `go test ./...` ok; `go test -race ./pkg/state/... ./pkg/web/...` ok; `gofmt -l` clean. Benchmark (gate 5): Apply(part.delta) ≈129µs/53allocs/370KB per event at 0 subs vs ≈130µs at 8 interested subs — fanout is noise-level (<2µs total); the REDUCER dominates → Option C (delta coalescing) recommended over Option D (fanout outside lock). No commit. | P1-AGG-001, P1-AGG-002 |
| P1-AGG-004 | done | AGG | Option C — coalesce `message.part.delta` / shrink the `appendPartDeltaLocked` reducer to cut residual `s.mu` lock-held time per token delta. Follow-up to P1-AGG-003. | build | 2026-07-01. DONE. Design C3a shipped (native accumulator + time-throttled emit, no new goroutine). Accumulator: `messageEntry.deltaBuf map[string]*strings.Builder` keyed by `partID+"\x00"+field` + `deltaLastEmit time.Time`; `appendPartDeltaLocked` appends each delta to a `strings.Builder` (amortized O(1), killing the old O(n²) full-text copy + per-delta full unmarshal/marshal) and only rebuilds+emits `part.upsert` when `time.Since(deltaLastEmit) >= deltaFlushInterval` (package var, 30ms; first delta of a burst always flushes). Reconciliation hooks: `upsertPartLocked` (message.part.updated snapshot) + `deletePartLocked` call `discardPartDeltaLocked`; `reconcileMessagesLocked` (history fetch via `SetSessionMessages`) resets `deltaBuf`/`deltaLastEmit`; `Snapshot` switched to WRITE lock + flushes buffered text (emit=false) so a point-in-time read reflects live text. BENCHMARK (acceptance gate): `BenchmarkApplyPartDeltaInterestedSubs` — BEFORE ~129,000 ns/op / 53 allocs / ~370 KB per single-char delta (P1-AGG-003 baseline) → AFTER 1,223 ns/op / 10 allocs / 467 B (~105× faster, ~5× fewer allocs; allocs no longer scale with accumulated text length). New `BenchmarkApplyPartDeltaFlushEveryDelta` (forces flush every delta) isolates the reducer floor at 134µs/53allocs — confirms marshal is O(n) per flush, justifying the throttle. Changed: `pkg/state/store.go`, `pkg/state/delta_coalesce_test.go` (NEW — 6 delta-coalesce tests: exact-text-500-chars, authoritative-reconciliation x2, throttle-bounded-emits, concurrent-no-race, history-fetch-resets). Verified: `go build`/`vet`/`test ./...` ok; `go test -race ./pkg/state/... ./pkg/web/...` ok; `gofmt -l` clean. RESIDUAL: the per-event `s.mu` serialization remains (every event still takes the global lock); a structural event can still wait behind ONE in-flight delta — but that delta is now ~1µs (was ~129µs), so the residual head-of-line latency is cut ~100× and is now bounded by fanout+ring (~1µs), the practical floor under Option A. Option D (fanout outside lock) remains unnecessary (fanout <2µs). No commit. | P1-AGG-003 |
| P1-AGG-005 | done | AGG | Classification-drift hardening test: assert store's `isMessageClassKind` returns true for every `Kind*` whose string starts with `message.`/`part.`/`messages.`. | build | 2026-07-01. DONE. Folded into the P1-AGG-004 slice (same test file). `TestMessageClassKind_NoDriftFromWebPrefix` in `pkg/state/delta_coalesce_test.go` enumerates all 19 exported `Kind*` constants (completeness guard `len==19`) and asserts `isMessageClassKind(k) == hasMessageClassPrefix(k)` where `hasMessageClassPrefix` replicates web's `sendable()` prefix match (`strings.HasPrefix` "message."/"part."/"messages."). A one-site-only addition of a new `message.*`/`part.*`/`messages.*` Kind now fails loudly. Verified green. No commit. | P1-AGG-003 |
| P1-WEB-017 | done | WEB | Fix unstable live/auto-follow scrolling that died on tab resume. While a tab is hidden the browser coalesces ResizeObserver callbacks into one delivery on resume; a turn-settle shrink (raw `md-stream`→compact `MarkdownHtml`) + regrow delivered as a single RO callback was mis-read by the contentEl re-pin guard (web/src/components/ChatView.tsx) as a genuine user scroll-up → armed the intent latch (`setUserScrolledUp(true)`/`setFollowing(false)`). The only auto-recovery (self-heal on `working()` false→true, gated `!userScrolledUp()`) couldn't recover it → Live stayed dead until manual scroll-back/"↓ Latest". Fix: additive `visibilitychange` listener that re-pins (`setFollowing(true)`+`pin()`) when ready and `!userScrolledUp()` before the queued RO delivers, refreshing `pinnedTop`/`pinnedScrollHeight` to the current post-hidden state. Minimal/additive; no per-frame work (Firefox/WebRender heat-safe). | build | 2026-07-01. Operator-reported ("live scrolling unstable, stops after some time, maybe at page resume"). Changed: web/src/components/ChatView.tsx (+26, additive visibilitychange re-engage listener ~L401-426), web/tests/e2e/scroll-follow.spec.ts (+155, test 13 resume-coalesce repro — fails pre-fix/passes post; test 14 intent-latch guard). Verified: `npm run typecheck` clean; `npx vitest run` 139/139; `npx playwright test tests/e2e/scroll-follow.spec.ts` 14 passed/1 skipped (pre-existing skip). Commit 90f6ef12 (commit-only; tag-driven release — no push). commit-reviewer: approve (4 leaves, high confidence, low risk, no blocks/splits). Follow-ups: P1-WEB-018 (self-heal mid-turn gap — real low-prob residual), P1-WEB-019 (ReasoningPart — investigated immune, no fix). |  |
| P1-AGG-006 | done | AGG | Polish bundle on the event-latency code: (F1) comment typo `deltaLastEit`→`deltaLastEmit` in store.go appendPartDeltaLocked throttle comment; (F2) delete the redundant package-level `func min(a,b int) int` in delta_coalesce_test.go that shadowed the Go 1.25 builtin; (F3) strengthen `TestDeltaCoalesce_AuthoritativeReconciliation_BufferedTextDropped` so it actually exercises the stale-buffer discard (fire ≥2 deltas so the 2nd stays genuinely unflushed, not just the always-immediate first-delta flush). Zero behavioral change — comment/typo + redundant-helper removal + test-strength only. | build | 2026-07-01. DONE. Advisory DROP findings F1/F2/F3 from the P1-AGG-003/004 commit-reviews (a researcher study confirmed only these three were worth doing). Changed: `pkg/state/store.go` (1-line comment typo), `pkg/state/delta_coalesce_test.go` (deleted package-level `min` helper — call sites at L75/L240 transparently resolve to the builtin, signature-compatible `min(int,int)`; +1 `applyDelta` in the buffered-text-dropped test so the 2nd delta stays genuinely unflushed when the authoritative snapshot arrives). Verified: `go build ./...`, `go test ./pkg/state/...`, `go test -race ./pkg/state/...` green; `gofmt -l pkg cmd main.go` clean. No commit (left for /commit-review → committer). | P1-AGG-003, P1-AGG-004 |

## Cancelled

| ID | Status | Area | Task | Owner | Notes | Links |
| --- | --- | --- | --- | --- | --- | --- |
