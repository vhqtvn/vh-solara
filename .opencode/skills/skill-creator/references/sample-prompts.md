# Sample prompts (copy/paste)

Use these to start repo-local OpenCode skill work. Keep them short and concrete.

```
Study archived OpenCode sessions in this repo and create a skill for the repeated workflow you find around runtime audits.
```

```
Create a repo-local skill that helps map a component's codepaths before implementation. Keep it read-only and explicit about when not to use it.
```

```
Update the promotion-audit skill so it better distinguishes benchmark validation from runtime rollout work.
```
